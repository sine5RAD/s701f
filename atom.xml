<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>五红的小窝</title>
  <subtitle>咱喜欢你喵</subtitle>
  <link href="https://s701f.top/" rel="alternate" type="text/html"/>
  <link href="https://s701f.top/atom.xml" rel="self" type="application/atom+xml"/>
  <id>https://s701f.top/</id>
  <updated>2026-07-07T05:47:37.624Z</updated>
  <language>zh_CN</language>
  <entry>
    <title>自我介绍</title>
    <link href="https://s701f.top/posts/%E8%87%AA%E6%88%91%E4%BB%8B%E7%BB%8D/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/%E8%87%AA%E6%88%91%E4%BB%8B%E7%BB%8D/</id>
    <published>2026-05-12T00:00:00.000Z</published>
    <updated>2026-05-12T00:00:00.000Z</updated>
    <summary>自我介绍</summary>
    <content type="html"><![CDATA[<h1>个人信息</h1>
<h2>我是谁？</h2>
<p>你可以称呼我为五红，如果想说的更可爱一点可以叫我五红酱。我是一个普通的游戏开发者，虽然不知道有没有资格这么称呼自己，但我确实打过Gamejam了。</p>
<h2>我在哪？</h2>
<p>北京，但也许在毕业之后会跑路，这个地方有点不适合人类生存。</p>
<h2>我在干什么？</h2>
<p>我在制作自己的游戏，也许会烂尾，但我还挺享受做的过程的。&lt;br&gt;
&lt;del&gt;点击前往长夜难明Wiki&lt;/del&gt;还没做</p>
<h1>我能干嘛？</h1>
<p>&lt;del&gt;可以&lt;/del&gt;</p>
<h2>软件工程</h2>
<h3>C++</h3>
<p>这是我最早接触的编程语言，在2018年的时候。当时我在打算法竞赛&lt;del&gt;虽然没有拿到什么名次&lt;/del&gt;&lt;br&gt;
不过现在来看我学的更像是C + STL</p>
<h3>HTML/CSS</h3>
<p>也是2018年（兴许是2019年？我不记得了），我的一个同学做了个人网页，我跟风自学的。没想到在6年之后派上了大用场。</p>
<h3>Python</h3>
<p>我的大伯教给我的，在2021年。已经忘光了。</p>
<h3>C#</h3>
<p>2024年，我来到了现在这个&lt;del&gt;疑似全北京最烂&lt;/del&gt;的大学，出于对就业的渴望以及表达欲望，我学习了Unity引擎。&lt;br&gt;
有关我就业的故事参见&lt;del&gt;未走上的奴役之路——上海宇宙游的故事&lt;/del&gt;还没写</p>
<h3>JAVA</h3>
<p>数据结构与算法课讲的（是的你没看错，数据结构与算法课用这种第五代编程语言，所以避雷北京工业大学），可能是恨乌及屋，给到拉完了。</p>
<h3>VUE/TypeScript</h3>
<p>这个世界上会有人在掌握HTML6年后才开始学习JS/TS的人嘛？是的就是我了。2025年末，我接了一个外包单，于是速成了前端开发相关内容。</p>
<h3>WXML</h3>
<p>同样是2025年末学会的。不建议学，以后也不会干相关的事情。给到拉完了。</p>
<h2>电子信息工程</h2>
<h3>电路分析</h3>
<h3>数字电路</h3>
<h3>模拟电路</h3>
<h3>PCB板绘制</h3>
<p>2026年4月为了打电赛速成的</p>
<h2>板绘</h2>
<p>为了做游戏学的，还不会呢，爬了</p>
<h2>新中国史</h2>
<p><em>“对！群众！群众！”——刘知侠，1991.9.3</em></p>
<h2>穿搭/cosplay</h2>
<h3>UMP45</h3>
<h3>樱羽艾玛</h3>
<h2>伪音</h2>
<p>？</p>
<h1>兴趣爱好</h1>
<h2>创作</h2>
<h3>长夜难明相关剧情</h3>
<p>游戏没做多少，剧情和世界观写了一大堆。</p>
<h3>这个网站</h3>
<h2>游戏</h2>
<h3>逃离塔科夫</h3>
<p>加好友<strong>Automatic_WH</strong>谢谢喵</p>
<h3>Pixel Gun 3D</h3>
<p>已逃离</p>
<h3>钢铁雄心4</h3>
<p>只会打原版</p>
<h3>杀戮尖塔2</h3>
<p>好玩爱玩</p>
<h2>吃饭</h2>
<p>很内向，吃饱了也不说，一直吃</p>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="日常"></category>
  </entry>
  <entry>
    <title>电磁学笔记</title>
    <link href="https://s701f.top/posts/%E7%94%B5%E7%A3%81%E5%AD%A6%E7%AC%94%E8%AE%B0/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/%E7%94%B5%E7%A3%81%E5%AD%A6%E7%AC%94%E8%AE%B0/</id>
    <published>2026-06-25T00:00:00.000Z</published>
    <updated>2026-06-25T00:00:00.000Z</updated>
    <summary>AI生成的电磁学笔记</summary>
    <content type="html"><![CDATA[<h1>一、电通量密度</h1>
<h2>电通量的概念</h2>
<p>法拉第通过同心金属球实验发现，内球到外球存在一种"电荷位移"，这种位移与中间介质无关，称为<strong>位移</strong>、<strong>位移通量</strong>或<strong>电通量</strong>。</p>
<ul>
<li>电通量 $\psi$ 的单位为库仑（C），其大小等于内球上的总电荷 $Q$：$\psi = Q$</li>
<li>电通量密度 $\mathbf{D}$ 的单位为 $\text{C/m}^2$，其方向为通量线在该点的方向</li>
<li>电通量密度的大小等于穿过垂直于通量线方向的单位面积的电通量</li>
</ul>
<h2>电通量密度与电场强度的关系</h2>
<p>在自由空间中，电通量密度 $\mathbf{D}$ 与电场强度 $\mathbf{E}$ 成正比：</p>
<p>$$\mathbf{D} = \varepsilon_0 \mathbf{E}$$</p>
<p>其中 $\varepsilon_0$ 为真空介电常数。</p>
<p>对点电荷，径向电通量密度为：</p>
<p>$$\mathbf{D} = \frac{Q}{4\pi r^2} \mathbf{a}_r$$</p>
<p>对一般体电荷分布：</p>
<p>$$\mathbf{D} = \int_{\text{vol}} \frac{\rho_v dv}{4\pi R^2} \mathbf{a}_R$$</p>
<h2>例题1：线电荷的电通量密度</h2>
<p><strong>题目：</strong> 求自由空间中，距 $z$ 轴上均匀线电荷（密度 $\rho_L = 8\text{ nC/m}$）3 m 处的电通量密度。</p>
<p><strong>解析：</strong></p>
<p>无限长线电荷产生的电场强度为：</p>
<p>$$\mathbf{E} = \frac{\rho_L}{2\pi\varepsilon_0 \rho} \mathbf{a}_\rho$$</p>
<p>因此电通量密度为：</p>
<p>$$\mathbf{D} = \frac{\rho_L}{2\pi\rho} \mathbf{a}<em>\rho = \frac{8 \times 10^{-9}}{2\pi \rho} \mathbf{a}</em>\rho$$</p>
<p>代入 $\rho = 3\text{ m}$：</p>
<p>$$\mathbf{D} = \frac{8 \times 10^{-9}}{2\pi \times 3} \mathbf{a}<em>\rho = 0.424 \mathbf{a}</em>\rho \text{ nC/m}^2$$</p>
<h2>例题2：面电荷的电通量密度</h2>
<p><strong>题目：</strong> 在平面 $x = 9$ 上有均匀面电荷密度 $\rho_s = 57.2;\mu\text{C/m}^2$，求点 $P(6,8,-10)$ 处的 $\mathbf{D}$。</p>
<p><strong>解析：</strong></p>
<p>无限大均匀带电平面产生的电通量密度为：</p>
<p>$$\mathbf{D} = \frac{\rho_s}{2} \mathbf{a}_N$$</p>
<p>平面 $x = 9$ 的法向为 $\mathbf{a}_N = \mathbf{a}_x$，所以：</p>
<p>$$\mathbf{D} = \frac{57.2 \times 10^{-6}}{2} \mathbf{a}_x = 28.6 \mathbf{a}_x ; \mu\text{C/m}^2$$</p>
<h1>二、高斯定律</h1>
<h2>高斯定律的表述</h2>
<p>法拉第实验可以推广为<strong>高斯定律</strong>：</p>
<blockquote>
<p>穿过任意闭合曲面的电通量等于该曲面所包围的总电荷。</p>
</blockquote>
<p>数学表达式为：</p>
<p>$$\oint_S \mathbf{D}<em>S \cdot d\mathbf{S} = Q</em>{\text{enc}} = \int_{\text{vol}} \rho_v dv$$</p>
<h2>高斯定律的应用条件</h2>
<p>利用高斯定律求解电场时，需要选择一个闭合曲面（高斯面），满足两个条件：</p>
<ol>
<li>$\mathbf{D}_S$ 处处与曲面垂直或相切——即 $\mathbf{D}_S \cdot d\mathbf{S}$ 要么为 $D_S dS$，要么为零</li>
<li>在 $\mathbf{D}_S \cdot d\mathbf{S}$ 不为零的部分上，$D_S$ 为常数</li>
</ol>
<h2>例题3：同轴电缆</h2>
<p><strong>题目：</strong> 一段 50 cm 长的同轴电缆，内导体半径 1 mm，外导体内半径 4 mm，导体间为空气。内导体总电荷 30 nC。求各导体上的电荷密度以及 $\mathbf{E}$ 和 $\mathbf{D}$ 的表达式。</p>
<p><strong>解析：</strong></p>
<p><strong>内导体上的电荷密度：</strong>
内导体表面积 $S_{\text{inner}} = 2\pi a L = 2\pi \times 0.001 \times 0.5 = \pi \times 10^{-3} \text{ m}^2$
$$\rho_{s,\text{inner}} = \frac{Q}{S_{\text{inner}}} = \frac{30 \times 10^{-9}}{\pi \times 10^{-3}} = 9.55 ;\mu\text{C/m}^2$$</p>
<p><strong>外导体上的电荷密度：</strong>
外导体内表面积 $S_{\text{outer}} = 2\pi b L = 4\pi \times 10^{-3} \text{ m}^2$
$$\rho_{s,\text{outer}} = \frac{-Q}{S_{\text{outer}}} = -2.39 ;\mu\text{C/m}^2$$</p>
<p><strong>导体间的 $\mathbf{D}$ 和 $\mathbf{E}$ 场（$a &lt; \rho &lt; b$）：</strong>
取长度为 $L$、半径为 $\rho$ 的高斯圆柱面，由高斯定律：
$$\mathbf{D} = \frac{Q/L}{2\pi\rho} \mathbf{a}<em>\rho = \frac{9.55 \times 10^{-9}}{\rho} \mathbf{a}</em>\rho \text{ C/m}^2$$
$$\mathbf{E} = \frac{\mathbf{D}}{\varepsilon_0} = \frac{1079}{\rho} \mathbf{a}_\rho \text{ V/m}$$</p>
<h1>三、高斯定律的应用：对称电荷分布</h1>
<h2>均匀线电荷</h2>
<p>取同轴圆柱面作为高斯面，高度 $L$，半径 $\rho$：
$$\oint_S \mathbf{D} \cdot d\mathbf{S} = D_\rho \cdot 2\pi\rho L = \rho_L L$$
$$\mathbf{D} = \frac{\rho_L}{2\pi\rho} \mathbf{a}_\rho$$</p>
<h2>同轴电缆</h2>
<p>内导体半径 $a$，外导体半径 $b$，内导体表面电荷密度 $\rho_S$。
取 $a &lt; \rho &lt; b$ 的高斯圆柱面：
$$Q_{\text{enc}} = 2\pi a L \rho_S$$
$$\mathbf{D} = \frac{\rho_L}{2\pi\rho} \mathbf{a}_\rho, \quad \rho_L = 2\pi a \rho_S$$</p>
<p>当 $\rho &gt; b$ 时，总包围电荷为零，$\mathbf{D} = 0$——同轴电缆的屏蔽原理。</p>
<h1>四、散度</h1>
<h2>散度的定义</h2>
<p>$$\nabla \cdot \mathbf{D} = \lim_{\Delta v \to 0} \frac{\oint_S \mathbf{D} \cdot d\mathbf{S}}{\Delta v}$$</p>
<ul>
<li>正散度 → 源点（source）</li>
<li>负散度 → 汇点（sink）</li>
<li>零散度 → 无源无汇</li>
</ul>
<h2>散度的表达式（三大坐标系）</h2>
<p><strong>直角坐标系：</strong> $\nabla \cdot \mathbf{D} = \frac{\partial D_x}{\partial x} + \frac{\partial D_y}{\partial y} + \frac{\partial D_z}{\partial z}$</p>
<p><strong>圆柱坐标系：</strong> $\nabla \cdot \mathbf{D} = \frac{1}{\rho}\frac{\partial}{\partial\rho}(\rho D_\rho) + \frac{1}{\rho}\frac{\partial D_\phi}{\partial\phi} + \frac{\partial D_z}{\partial z}$</p>
<p><strong>球坐标系：</strong> $\nabla \cdot \mathbf{D} = \frac{1}{r^2}\frac{\partial}{\partial r}(r^2 D_r) + \frac{1}{r\sin\theta}\frac{\partial}{\partial\theta}(D_\theta\sin\theta) + \frac{1}{r\sin\theta}\frac{\partial D_\phi}{\partial\phi}$</p>
<h2>例题4：散度与通量计算</h2>
<p><strong>题目：</strong> 已知 $\mathbf{D} = y^2 z^3 \mathbf{a}_x + 2xyz^3 \mathbf{a}_y + 3xy^2 z^2 \mathbf{a}_z \text{ pC/m}^2$，求：
(a) 穿过 $x=3$，$0 \le y \le 2$，$0 \le z \le 1$ 曲面的总电通量
(b) $P(3,2,1)$ 处的 $|\mathbf{E}|$
(c) 以 $P$ 为中心、半径 $2;\mu\text{m}$ 的微小球体内包含的总电荷</p>
<p><strong>解析：</strong></p>
<p><strong>(a)</strong> 曲面 $x=3$ 的外法向为 $\mathbf{a}<em>x$：
$$\psi = \int_S \mathbf{D} \cdot d\mathbf{S} = \int</em>{z=0}^1 \int_{y=0}^2 y^2 z^3 , dy , dz = \int_0^1 z^3 \cdot \frac{8}{3} , dz = \frac{2}{3} \text{ pC}$$</p>
<p><strong>(b)</strong> 在 $P(3,2,1)$ 处：
$$D_x = 4,; D_y = 12,; D_z = 36 \text{ pC/m}^2$$
$$|\mathbf{D}|_P = \sqrt{4^2+12^2+36^2} = 38.158 \text{ pC/m}^2$$
$$|\mathbf{E}|_P = \frac{|\mathbf{D}|_P}{\varepsilon_0} = 4.31 \text{ V/m}$$</p>
<p><strong>(c)</strong> 在 $P$ 点处散度：
$$\nabla \cdot \mathbf{D} = \frac{\partial D_x}{\partial x} + \frac{\partial D_y}{\partial y} + \frac{\partial D_z}{\partial z} = 0 + 2xz^3 + 6xy^2 z \bigg|<em>{P(3,2,1)} = 78 \text{ pC/m}^3$$
微小球体体积 $\Delta v = \frac{4}{3}\pi r^3 = 3.351 \times 10^{-17} \text{ m}^3$
$$Q</em>{\text{enc}} \approx (\nabla \cdot \mathbf{D}) \Delta v = 2.61 \times 10^{-27} \text{ C}$$</p>
<h2>例题5：散度的常量特性</h2>
<p><strong>题目：</strong> 验证场 $\mathbf{D} = 4xy \mathbf{a}_x + 2x^2 \mathbf{a}_y + 4z \mathbf{a}_z$ 的散度特性。</p>
<p><strong>解析：</strong>
$$\nabla \cdot \mathbf{D} = \frac{\partial}{\partial x}(4xy) + \frac{\partial}{\partial y}(2x^2) + \frac{\partial}{\partial z}(4z) = 4y + 4$$
此场的散度与 $y$ 有关，并非常数。</p>
<h1>五、散度定理</h1>
<p>$$\oint_S \mathbf{D} \cdot d\mathbf{S} = \int_{\text{vol}} (\nabla \cdot \mathbf{D}) , dv$$</p>
<h2>例题6：验证散度定理</h2>
<p><strong>题目：</strong> 对场 $\mathbf{D} = 2xy \mathbf{a}_x + x^2 \mathbf{a}_y \text{ C/m}^2$ 和平行六面体（$x=0,1$；$y=0,2$；$z=0,3$），验证散度定理。</p>
<p><strong>解析：</strong></p>
<p><strong>面积分：</strong></p>
<ul>
<li>$x=1$ 面：$\iint 2y , dy,dz = 12 \text{ C}$</li>
<li>$y=2$ 面：$\iint x^2 , dx,dz = 1 \text{ C}$</li>
<li>其余面贡献为零</li>
<li>总面积分 $= 13 \text{ C}$</li>
</ul>
<p><strong>体积分：</strong>
$\nabla \cdot \mathbf{D} = 2y$
$$\int_{\text{vol}} 2y , dv = \int_0^3 \int_0^2 \int_0^1 2y , dx,dy,dz = 13 \text{ C}$$</p>
<p>散度定理验证成立。</p>
<h1>六、麦克斯韦第一方程（静电学）</h1>
<p>高斯定律的微分形式：</p>
<p>$$\nabla \cdot \mathbf{D} = \rho_v$$</p>
<p>结合 $\mathbf{D} = \varepsilon \mathbf{E}$：
$$\nabla \cdot \mathbf{E} = \frac{\rho_v}{\varepsilon}$$</p>
<hr />
<h1>学习自测</h1>
<h2>一、单选题</h2>
<p><strong>1. 电通量密度的单位是？</strong>
A. V/m
B. C/m²
C. C/m³
D. N/C</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> 电通量密度 $\mathbf{D}$ 的单位是 $\text{C/m}^2$。
关联原文：[[Chp3 Gauss#电通量的概念]]</p>
<p><strong>2. 真空中 $\mathbf{D}$ 和 $\mathbf{E}$ 的关系是？</strong>
A. $\mathbf{D} = \varepsilon_0 \mathbf{E}$
B. $\mathbf{D} = \mathbf{E}/\varepsilon_0$
C. $\mathbf{D} = \varepsilon_0 \mathbf{E}^2$
D. 两者无关</p>
<p><strong>答案：</strong> A
<strong>解析：</strong> 在真空中 $\mathbf{D} = \varepsilon_0 \mathbf{E}$，其中 $\varepsilon_0 \approx 8.854 \times 10^{-12} \text{ F/m}$。
关联原文：[[Chp3 Gauss#电通量密度与电场强度的关系]]</p>
<p><strong>3. 高斯定律的数学表达式是？</strong>
A. $\oint_S \mathbf{D} \cdot d\mathbf{S} = Q_{\text{enc}}$
B. $\oint_S \mathbf{D} \cdot d\mathbf{S} = 0$
C. $\oint_S \mathbf{D} \cdot d\mathbf{S} = \rho_v$
D. $\oint_S \mathbf{D} \cdot d\mathbf{S} = \varepsilon_0 Q$</p>
<p><strong>答案：</strong> A
<strong>解析：</strong> 高斯定律指出穿过闭合曲面的电通量等于曲面内包围的总电荷。
关联原文：[[Chp3 Gauss#高斯定律的表述]]</p>
<h2>二、多选题</h2>
<p><strong>4. 选择高斯面需满足的条件包括？</strong>
A. $\mathbf{D}_S$ 处处与曲面垂直
B. $\mathbf{D}_S$ 在高斯面上处处为常数
C. $\mathbf{D}_S$ 在与曲面垂直的部分上为常数
D. 高斯面必须为球面</p>
<p><strong>答案：</strong> A、C
<strong>解析：</strong> 高斯面应使 $\mathbf{D}_S$ 处处垂直或相切于曲面，且在不为零的部分上 $D_S$ 为常数。
关联原文：[[Chp3 Gauss#高斯定律的应用条件]]</p>
<p><strong>5. 散度的物理意义包括？</strong>
A. 正散度表示源点
B. 负散度表示汇点
C. 零散度表示无源无汇
D. 散度是矢量</p>
<p><strong>答案：</strong> A、B、C
<strong>解析：</strong> 散度为标量，其正负指示场线的发出或汇聚。
关联原文：[[Chp3 Gauss#散度的定义]]</p>
<h2>三、判断题</h2>
<p><strong>6. 同轴电缆外部存在电场。</strong>
<strong>答案：</strong> 错误
<strong>解析：</strong> $\rho &gt; b$ 时包围净电荷为零，$\mathbf{D} = 0$，这是同轴电缆的屏蔽原理。
关联原文：[[Chp3 Gauss#同轴电缆]]</p>
<p><strong>7. 散度定理将面积分与体积分联系起来。</strong>
<strong>答案：</strong> 正确
<strong>解析：</strong> $\oint_S \mathbf{D} \cdot d\mathbf{S} = \int_{\text{vol}} (\nabla \cdot \mathbf{D}) dv$。
关联原文：[[Chp3 Gauss#五、散度定理]]</p>
<h2>四、简答题</h2>
<p><strong>8. 简述高斯定律的内容及其应用时的条件。</strong>
<strong>参考答案：</strong> 高斯定律：穿过任意闭合曲面的电通量等于该曲面所包围的总电荷。应用时需选择合适的高斯面，使 $\mathbf{D}_S$ 处处垂直或相切于曲面，且在 $\mathbf{D}_S \cdot d\mathbf{S} \neq 0$ 的部分上 $D_S$ 为常数。
关联原文：[[Chp3 Gauss#二、高斯定律]]</p>
<p><strong>9. 说明散度定理的物理意义。</strong>
<strong>参考答案：</strong> 散度定理表明，矢量场在闭合曲面上的通量等于其散度在闭合曲面内体积上的积分。它将面积分转化为体积分，是连接局部散度和全局通量的桥梁。
关联原文：[[Chp3 Gauss#五、散度定理]]</p>
<hr />
<h1>一、电场中移动点电荷的能量</h1>
<h2>功的定义</h2>
<p>在电场 $\mathbf{E}$ 中移动电荷 $Q$ 一段距离 $\mathbf{L}$，电场对电荷施加的力为：</p>
<p>$\mathbf{F} = Q\mathbf{E}$</p>
<p>外力需克服电场力做功。外力施加的力 $\mathbf{F}_{\text{appl}} = -Q\mathbf{E}$，微功为：</p>
<p>$dW = -Q\mathbf{E} \cdot d\mathbf{L}$</p>
<p>将电荷从初始点移动到终点的总功为：</p>
<p>$W = -Q\int_{\text{init}}^{\text{final}} \mathbf{E} \cdot d\mathbf{L}$</p>
<p><strong>结论：</strong></p>
<ul>
<li>$W &gt; 0$：外力做功（我们消耗能量）</li>
<li>$W &lt; 0$：电场做功（电场消耗能量）</li>
<li>当 $\mathbf{E} \perp d\mathbf{L}$ 时，微功为零</li>
</ul>
<h2>均匀电场中的功</h2>
<p>对于均匀电场 $\mathbf{E}$：</p>
<p>$W = -Q\mathbf{E} \cdot \mathbf{L}_{BA}$</p>
<p>功与路径无关，只取决于起点和终点的位置。</p>
<h2>例题1：非均匀电场中的功（圆弧路径）</h2>
<p><strong>题目：</strong> 给定非均匀电场 $\mathbf{E} = y\mathbf{a}_x + x\mathbf{a}_y + 2\mathbf{a}_z$，求将 $Q=2\text{ C}$ 电荷从 $(1,0,1)$ 移动到 $(0.8,0.6,1)$ 沿圆 $x^2 + y^2 = 1$ 的较短弧所做的功。</p>
<p><strong>解析：</strong></p>
<p>$W = -Q\int_B^A \mathbf{E} \cdot d\mathbf{L}$</p>
<p>$d\mathbf{L} = dx\mathbf{a}_x + dy\mathbf{a}_y + dz\mathbf{a}_z$</p>
<p>$\mathbf{E} \cdot d\mathbf{L} = y,dx + x,dy + 2,dz$</p>
<p>$W = -2\left(\int_1^{0.8} y,dx + \int_0^{0.6} x,dy + \int_1^1 2,dz\right)$</p>
<p>沿圆 $x^2 + y^2 = 1$，$y = \sqrt{1-x^2}$，$x = \sqrt{1-y^2}$：</p>
<p>$W = -2\left(\int_1^{0.8} \sqrt{1-x^2},dx + \int_0^{0.6} \sqrt{1-y^2},dy + 0\right)$</p>
<p>计算得 $W \approx 0.962 \text{ J}$</p>
<h2>例题2：沿直线路径的功</h2>
<p><strong>题目：</strong> 重复例题1，但沿从 $B$ 到 $A$ 的直线路径。</p>
<p><strong>解析：</strong></p>
<p>直线方程：$y = -3x + 3$，$x = 1 - y/3$</p>
<p>$W = -2\left(\int_1^{0.8} (-3x+3),dx + \int_0^{0.6} (1-y/3),dy + 0\right)$</p>
<p>计算得 $W = 0.962 \text{ J}$</p>
<p><strong>结论：</strong> 无论路径如何（圆弧或直线），功的结果相同——说明静电场是保守场。</p>
<h1>二、线积分</h1>
<h2>微分长度元</h2>
<p><strong>直角坐标系：</strong> $\mathbf{L} = dx,\mathbf{a}_x + dy,\mathbf{a}_y + dz,\mathbf{a}_z$</p>
<p><strong>圆柱坐标系：</strong> $\mathbf{L} = d\rho,\mathbf{a}<em>\rho + \rho,d\phi,\mathbf{a}</em>\phi + dz,\mathbf{a}_z$</p>
<p><strong>球坐标系：</strong> $\mathbf{L} = dr,\mathbf{a}<em>r + r,d\theta,\mathbf{a}</em>\theta + r\sin\theta,d\phi,\mathbf{a}_\phi$</p>
<h2>无限长线电荷附近的功</h2>
<p>对于无限长线电荷，$\mathbf{E} = \frac{\rho_L}{2\pi\varepsilon_0 \rho} \mathbf{a}_\rho$：</p>
<p>沿 $\phi$ 方向的路径：$W = 0$（$\mathbf{E} \perp d\mathbf{L}$）
沿 $\rho$ 方向的路径：$W = -\frac{Q\rho_L}{2\pi\varepsilon_0} \ln\frac{b}{a}$</p>
<h1>三、电位差与电位</h1>
<h2>电位差的定义</h2>
<p>电位差 $V_{AB}$ 定义为将单位正电荷从 $B$ 点移动到 $A$ 点外力所做的功：</p>
<p>$V_{AB} = -\int_B^A \mathbf{E} \cdot d\mathbf{L}$</p>
<p>电位差的单位为伏特（V），$1\text{ V} = 1\text{ J/C}$。</p>
<ul>
<li>$V_{AB} &gt; 0$：外力做功将正电荷从 $B$ 移到 $A$</li>
<li>$V_{AB} &lt; 0$：电场做功（正电荷从 $A$ 自然移动到 $B$）</li>
</ul>
<h2>电位</h2>
<p>电位是相对于参考点的电位差。常用参考点：</p>
<ul>
<li><strong>大地（ground）</strong></li>
<li><strong>无穷远（infinity）</strong></li>
<li>同轴电缆的外导体</li>
</ul>
<p>$V_A = -\int_{\text{ref}}^A \mathbf{E} \cdot d\mathbf{L}$</p>
<h2>线电荷的电位差</h2>
<p>从 $\rho = a$ 到 $\rho = b$：</p>
<p>$V_{ab} = \frac{\rho_L}{2\pi\varepsilon_0} \ln\frac{b}{a}$</p>
<h2>点电荷的电位</h2>
<p>取无穷远为零参考点：</p>
<p>$V = \frac{Q}{4\pi\varepsilon_0 r}$</p>
<h1>四、点电荷的电位场</h1>
<p>点电荷的电位差只取决于两点到电荷的距离，与路径无关：</p>
<p>$V_{AB} = \frac{Q}{4\pi\varepsilon_0}\left(\frac{1}{r_A} - \frac{1}{r_B}\right)$</p>
<h1>五、等位面</h1>
<p>等位面是电位相同的点组成的曲面。在等位面上移动电荷不做功。</p>
<ul>
<li>点电荷的等位面 → 以点电荷为中心的球面</li>
<li>线电荷的等位面 → 以线电荷为轴的圆柱面</li>
<li>面电荷的等位面 → 平行于带电平面的平面</li>
</ul>
<h1>六、电位场的保守性</h1>
<p>电位场具有<strong>可叠加性</strong>：</p>
<p>$V(\mathbf{r}) = \frac{1}{4\pi\varepsilon_0} \int \frac{\rho_L(\mathbf{r}') dL'}{|\mathbf{r} - \mathbf{r}'|}$</p>
<p>电位与路径无关——静电场为<strong>保守场</strong>。沿闭合路径的线积分为零：</p>
<p>$\oint \mathbf{E} \cdot d\mathbf{L} = 0$</p>
<h2>环形线电荷的电位</h2>
<p>求 $z$ 轴上距环形线电荷（半径 $a$，线密度 $\rho_L$，位于 $z=0$ 平面）的电位：</p>
<p>$V = \frac{\rho_L a}{2\varepsilon_0 \sqrt{a^2 + z^2}}$</p>
<h1>七、电位梯度</h1>
<p>电位梯度定义为标量位场的最大空间变化率方向：</p>
<p>$\mathbf{E} = -\nabla V$</p>
<p><strong>直角坐标系：</strong> $\nabla V = \frac{\partial V}{\partial x}\mathbf{a}_x + \frac{\partial V}{\partial y}\mathbf{a}_y + \frac{\partial V}{\partial z}\mathbf{a}_z$</p>
<p><strong>圆柱坐标系：</strong> $\nabla V = \frac{\partial V}{\partial \rho}\mathbf{a}<em>\rho + \frac{1}{\rho}\frac{\partial V}{\partial \phi}\mathbf{a}</em>\phi + \frac{\partial V}{\partial z}\mathbf{a}_z$</p>
<p><strong>球坐标系：</strong> $\nabla V = \frac{\partial V}{\partial r}\mathbf{a}<em>r + \frac{1}{r}\frac{\partial V}{\partial \theta}\mathbf{a}</em>\theta + \frac{1}{r\sin\theta}\frac{\partial V}{\partial \phi}\mathbf{a}_\phi$</p>
<h2>例题3：已知电位求场量</h2>
<p><strong>题目：</strong> 已知 $V = 2x^2 y - 5z$，及点 $(-4,3,6)$，求 $V$、$\mathbf{E}$、$\mathbf{E}$ 方向、$\mathbf{D}$ 和 $\rho_v$。</p>
<p><strong>解析：</strong></p>
<p>$V = 2(16)(3) - 5(6) = 96 - 30 = 66 \text{ V}$</p>
<p>$$\mathbf{E} = -\nabla V = -\left(\frac{\partial V}{\partial x}\mathbf{a}_x + \frac{\partial V}{\partial y}\mathbf{a}_y + \frac{\partial V}{\partial z}\mathbf{a}_z\right)$$</p>
<p>$$= -(4xy,\mathbf{a}_x + 2x^2,\mathbf{a}_y - 5,\mathbf{a}_z)$$</p>
<p>在 $(-4,3,6)$ 处：
$$\mathbf{E} = -(-48,\mathbf{a}_x + 32,\mathbf{a}_y - 5,\mathbf{a}_z) = 48\mathbf{a}_x - 32\mathbf{a}_y + 5\mathbf{a}_z \text{ V/m}$$</p>
<p>$|\mathbf{E}| = \sqrt{48^2 + 32^2 + 5^2} = \sqrt{3353} \approx 57.9 \text{ V/m}$</p>
<p>方向单位向量：$\mathbf{a}_E = \frac{48\mathbf{a}_x - 32\mathbf{a}_y + 5\mathbf{a}_z}{57.9}$</p>
<p>$$\mathbf{D} = \varepsilon_0 \mathbf{E}$$</p>
<p>$$\rho_v = \nabla \cdot \mathbf{D} = \varepsilon_0 \nabla \cdot \mathbf{E} = \varepsilon_0 \left(\frac{\partial E_x}{\partial x} + \frac{\partial E_y}{\partial y} + \frac{\partial E_z}{\partial z}\right)$$</p>
<p>$$\frac{\partial E_x}{\partial x} = -4y,; \frac{\partial E_y}{\partial y} = 0,; \frac{\partial E_z}{\partial z} = 0$$</p>
<p>$$\rho_v = -4y\varepsilon_0 = -12\varepsilon_0 \text{ C/m}^3$$</p>
<h1>八、电偶极子</h1>
<h2>电偶极子的定义</h2>
<p>两个大小相等、符号相反、间距很小的点电荷构成<strong>电偶极子</strong>。</p>
<h2>电偶极子的电位</h2>
<p>$$V = \frac{Qd\cos\theta}{4\pi\varepsilon_0 r^2} = \frac{\mathbf{p} \cdot \mathbf{a}_r}{4\pi\varepsilon_0 r^2}$$</p>
<p>其中 $\mathbf{p} = Q\mathbf{d}$ 为<strong>电偶极矩</strong>。</p>
<h2>电偶极子的电场</h2>
<p>$$\mathbf{E} = \frac{Qd}{4\pi\varepsilon_0 r^3}(2\cos\theta,\mathbf{a}<em>r + \sin\theta,\mathbf{a}</em>\theta)$$</p>
<hr />
<h1>学习自测</h1>
<h2>一、单选题</h2>
<p><strong>1. 电位差的单位是？</strong>
A. J
B. V
C. A
D. W</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> 电位差的单位是伏特（V），$1\text{ V} = 1\text{ J/C}$。
关联原文：[[Chp4#电位差的定义]]</p>
<p><strong>2. 电场强度 $\mathbf{E}$ 和电位 $V$ 的关系是？</strong>
A. $\mathbf{E} = \nabla V$
B. $\mathbf{E} = -\nabla V$
C. $\mathbf{E} = \nabla \times V$
D. $\mathbf{E} = -\nabla \times V$</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> $\mathbf{E} = -\nabla V$，电场指向电位下降最快的方向。
关联原文：[[Chp4#七、电位梯度]]</p>
<p><strong>3. 在等位面上移动电荷，电场力做功为？</strong>
A. 正功
B. 负功
C. 零
D. 取决于电场方向</p>
<p><strong>答案：</strong> C
<strong>解析：</strong> 等位面上电位相同，$\mathbf{E} \perp d\mathbf{L}$，故做功为零。
关联原文：[[Chp4#五、等位面]]</p>
<h2>二、多选题</h2>
<p><strong>4. 以下哪些是电位参考点的常用选择？</strong>
A. 大地
B. 无穷远
C. 同轴电缆的外导体
D. 点电荷所在位置</p>
<p><strong>答案：</strong> A、B、C
<strong>解析：</strong> 常用的电位参考点包括大地、无穷远和外导体。点电荷所在位置电位趋于无穷大，不可用作参考点。
关联原文：[[Chp4#电位]]</p>
<p><strong>5. 静电场中，电位的特点包括？</strong>
A. 可叠加
B. 与路径无关（保守场）
C. 沿闭合回路积分为零
D. 等位面与电场线平行</p>
<p><strong>答案：</strong> A、B、C
<strong>解析：</strong> 电位满足叠加原理，与路径无关（保守场），$\oint \mathbf{E} \cdot d\mathbf{L} = 0$。等位面与电场线垂直而非平行。
关联原文：[[Chp4#六、电位场的保守性]]</p>
<h2>三、判断题</h2>
<p><strong>6. 电偶极子的电位与距离 $r$ 成反比。</strong>
<strong>答案：</strong> 错误
<strong>解析：</strong> 电偶极子的电位 $V \propto 1/r^2$，而点电荷的电位 $V \propto 1/r$。
关联原文：[[Chp4#电偶极子的电位]]</p>
<p><strong>7. 均匀电场中移动电荷所做的功与路径无关。</strong>
<strong>答案：</strong> 正确
<strong>解析：</strong> 静电场为保守场，功只取决于起点和终点的位置，与路径无关。
关联原文：[[Chp4#均匀电场中的功]]</p>
<h2>四、简答题</h2>
<p><strong>8. 从电位梯度出发，推导已知 $V$ 求 $\mathbf{E}$ 的方法。</strong>
<strong>参考答案：</strong> $\mathbf{E} = -\nabla V$。在直角坐标系中，$\mathbf{E} = -\left(\frac{\partial V}{\partial x}\mathbf{a}_x + \frac{\partial V}{\partial y}\mathbf{a}_y + \frac{\partial V}{\partial z}\mathbf{a}_z\right)$。即电场强度等于电位梯度的负值，方向指向电位下降最快的方向。
关联原文：[[Chp4#七、电位梯度]]</p>
<hr />
<h1>一、电流与电流密度</h1>
<h2>电流的定义</h2>
<p>电荷的定向运动形成<strong>电流</strong>。电流的单位为安培（A）：</p>
<p>$$I = \frac{dQ}{dt}$$</p>
<p>电流定义为正电荷的运动方向（金属导体中实际为电子反向运动）。</p>
<h2>电流密度</h2>
<p><strong>电流密度</strong> $\mathbf{J}$ 是矢量，单位为 $\text{A/m}^2$：</p>
<p>$$\Delta I = J_N \Delta S \quad \Rightarrow \quad I = \int_S \mathbf{J} \cdot d\mathbf{S}$$</p>
<p>电流密度与体电荷密度 $\rho_v$ 和速度 $\mathbf{v}$ 的关系：</p>
<p>$$\mathbf{J} = \rho_v \mathbf{v}$$</p>
<p>这称为<strong>对流电流密度</strong>（convection current density）。</p>
<h1>二、电流连续性方程</h1>
<h2>积分形式</h2>
<p>电荷守恒定律——"电荷既不能创生也不能消灭"：</p>
<p>$$\oint_S \mathbf{J} \cdot d\mathbf{S} = -\frac{dQ_i}{dt}$$</p>
<p>即流出闭合曲面的电流等于曲面内电荷的减少率。</p>
<h2>微分形式（点形式）</h2>
<p>利用散度定理：</p>
<p>$$\nabla \cdot \mathbf{J} = -\frac{\partial \rho_v}{\partial t}$$</p>
<p>这是<strong>电流连续性方程的微分形式</strong>。</p>
<h2>例题1：电流密度</h2>
<p><strong>题目：</strong> 已知电流密度 $\mathbf{J} = \frac{2\cos\theta}{r^3} \mathbf{a}_r \text{ A/m}^2$，求相关物理量。</p>
<p><strong>解析：</strong>
具体求解步骤需根据题目要求进行。一般思路：</p>
<ul>
<li>通过 $I = \int_S \mathbf{J} \cdot d\mathbf{S}$ 求总电流</li>
<li>通过 $\nabla \cdot \mathbf{J} = -\partial \rho_v/\partial t$ 求体电荷密度的变化率</li>
</ul>
<h1>三、金属导体</h1>
<h2>能带结构</h2>
<p>金属、半导体和绝缘体在 0 K 时的能带结构不同。金属中，价带电子在电场作用下形成<strong>漂移速度</strong> $\mathbf{v}_d$：</p>
<p>$$\mathbf{v}_d = -\mu_e \mathbf{E}$$</p>
<p>其中 $\mu_e$ 为电子迁移率。</p>
<h2>欧姆定律的点形式</h2>
<p>$$\mathbf{J} = \sigma \mathbf{E}$$</p>
<p>其中 $\sigma$ 为电导率（单位：$\text{S/m}$）。</p>
<h2>宏观欧姆定律</h2>
<p>对于均匀电场中的圆柱导体：</p>
<p>$V = EL, \quad I = JS = \sigma E S$</p>
<p>$$R = \frac{V}{I} = \frac{L}{\sigma S}$$</p>
<h1>四、导体性质与边界条件</h1>
<h2>导体的静电性质</h2>
<p><strong>性质1：</strong> 导体内体电荷密度为零（$\rho_v = 0$），电荷只存在于外表面。
<strong>性质2：</strong> 静电场条件下，导体内电场强度为零（$\mathbf{E} = 0$）。</p>
<h2>导体表面的边界条件</h2>
<p><strong>切向分量：</strong> $E_t = 0$，$D_t = 0$（否则电荷将运动，不满足静电场条件）
<strong>法向分量：</strong> $D_N = \rho_S$，$E_N = \rho_S/\varepsilon_0$</p>
<p><strong>导体在静电场中的三个原则：</strong></p>
<ol>
<li>导体内 $E = 0$</li>
<li>导体表面电场处处垂直于表面</li>
<li>导体表面为等位面</li>
</ol>
<h2>例题2：导体边界条件</h2>
<p><strong>题目：</strong> 已知电位 $V = 100(x^2 - y^2)$，点 $P(2,-1,3)$ 位于导体-自由空间边界上，求 $V$、$\mathbf{E}$、$\mathbf{D}$ 和 $\rho_S$ 以及导体表面方程。</p>
<p><strong>解析：</strong></p>
<p>$V_P = 100(4 - 1) = 300 \text{ V}$</p>
<p>$$\mathbf{E} = -\nabla V = -[200x,\mathbf{a}_x - 200y,\mathbf{a}_y] = -200x,\mathbf{a}_x + 200y,\mathbf{a}_y$$</p>
<p>在 $P(2,-1,3)$ 处：
$\mathbf{E} = -400\mathbf{a}_x - 200\mathbf{a}_y \text{ V/m}$</p>
<p>$\mathbf{D} = \varepsilon_0 \mathbf{E} = \varepsilon_0(-400\mathbf{a}_x - 200\mathbf{a}_y)$</p>
<p>$\rho_S = D_N = |\mathbf{D} \cdot \mathbf{n}|$（需根据具体导体表面方向确定）</p>
<p>导体表面为等位面，方程为 $V = 300$，即 $x^2 - y^2 = 3$（在 $P$ 点处）。</p>
<h1>五、镜像法</h1>
<h2>镜像法的原理</h2>
<p>一个电荷附近的无限大接地导电平面上方的电场，可以通过去掉平面并在对称位置放置一个镜像电荷来等效。</p>
<p><strong>关键点：</strong></p>
<ul>
<li>镜像电荷与原电荷大小相等、符号相反</li>
<li>导电平面的电位为零（$V = 0$）</li>
<li>上半空间的电场不变</li>
</ul>
<h2>例题3：镜像法应用</h2>
<p><strong>题目：</strong> 在 $z=0$ 的接地导电平面上方 $x=0, z=3$ 处有一条 30 nC/m 的线电荷，求 $P(2,5,0)$ 处的面电荷密度。</p>
<p><strong>解析：</strong></p>
<p>去掉导电平面，在 $x=0, z=-3$ 处放置镜像线电荷（-30 nC/m）。</p>
<p>$P$ 点的电场由原线电荷和镜像线电荷叠加得到。由于 $P$ 在 $z=0$ 平面上，$E$ 的法向分量为两个线电荷贡献之和。</p>
<p>从镜像法原理可知，导电平面上的面电荷密度可由电场法向分量求得：</p>
<p>$$\rho_S = D_N = \varepsilon_0 E_N$$</p>
<h1>六、半导体</h1>
<h2>本征半导体</h2>
<p>本征半导体（如纯锗、纯硅）中，载流子有两种：</p>
<ul>
<li><strong>电子</strong>（从价带跃迁到导带）</li>
<li><strong>空穴</strong>（价带留下的空位，视为正电荷）</li>
</ul>
<p>禁带宽度约为 $1 \text{ eV}$。</p>
<h2>半导体的电导率</h2>
<p>$$\sigma = -\rho_e \mu_e + \rho_h \mu_h$$</p>
<p>其中 $\mu_e$、$\mu_h$ 分别为电子和空穴的迁移率。</p>
<ul>
<li>半导体电导率随温度升高而增大（载流子密度快速增加）</li>
<li>金属电导率随温度升高而减小</li>
<li>半导体也满足欧姆定律的点形式：$\mathbf{J} = \sigma \mathbf{E}$</li>
</ul>
<h1>七、电介质材料</h1>
<h2>电介质的极化</h2>
<p>电介质中的电荷为<strong>束缚电荷</strong>（bound charges），在电场作用下只能做微小位移。电介质具有储存电场能量的能力。</p>
<p><strong>极性分子：</strong> 具有永久电偶极矩，外场使其定向排列。
<strong>非极性分子：</strong> 无永久电偶极矩，外场使其正负电荷中心分离。</p>
<h2>极化强度</h2>
<p><strong>极化强度 $\mathbf{P}$</strong> 定义为每单位体积的电偶极矩：</p>
<p>$$\mathbf{P} = \lim_{\Delta v \to 0} \frac{1}{\Delta v} \sum_{i=1}^{n\Delta v} \mathbf{p}_i$$</p>
<p>单位：$\text{C/m}^2$</p>
<h2>电通量密度的一般形式</h2>
<p>$$ \mathbf{D} = \varepsilon_0 \mathbf{E} + \mathbf{P} $$</p>
<h2>极化率与相对介电常数</h2>
<p>在各向同性材料中，$\mathbf{P}$ 与 $\mathbf{E}$ 线性相关：</p>
<p>$$\mathbf{P} = \chi_e \varepsilon_0 \mathbf{E}$$</p>
<p>其中 $\chi_e$ 为电极化率。于是：</p>
<p>$$\mathbf{D} = \varepsilon_0(1 + \chi_e)\mathbf{E} = \varepsilon_0 \varepsilon_r \mathbf{E} = \varepsilon \mathbf{E}$$</p>
<p>其中 $\varepsilon_r = 1 + \chi_e$ 为相对介电常数。</p>
<h2>例题4：电介质中的场</h2>
<p><strong>题目：</strong> 特氟龙（Teflon） slab 位于 $0 \le x \le a$ 区域，外部为自由空间。Teflon 外有均匀电场 $\mathbf{E}_{\text{out}} = E_0 \mathbf{a}_x \text{ V/m}$，求各区域的 $\mathbf{D}$、$\mathbf{E}$ 和 $\mathbf{P}$。</p>
<p><strong>解析：</strong></p>
<p><strong>区域 I（$x &lt; 0$）：</strong>
$\mathbf{E}_I = E_0 \mathbf{a}_x,\quad \mathbf{D}_I = \varepsilon_0 E_0 \mathbf{a}_x,\quad \mathbf{P}_I = 0$</p>
<p><strong>区域 II（$0 \le x \le a$，Teflon 内部）：</strong>
由边界条件，$D_{N}$ 连续：
$\mathbf{D}_{II} = \varepsilon_0 E_0 \mathbf{a}_x$</p>
<p>$$\mathbf{E}<em>{II} = \frac{\mathbf{D}</em>{II}}{\varepsilon} = \frac{\varepsilon_0 E_0}{\varepsilon} \mathbf{a}_x = \frac{E_0}{\varepsilon_r} \mathbf{a}_x$$</p>
<p>$$\mathbf{P}<em>{II} = \mathbf{D}</em>{II} - \varepsilon_0 \mathbf{E}_{II} = \varepsilon_0 E_0 \left(1 - \frac{1}{\varepsilon_r}\right) \mathbf{a}_x$$</p>
<p><strong>区域 III（$x &gt; a$）：</strong>
$\mathbf{E}_{III} = E_0 \mathbf{a}<em>x,\quad \mathbf{D}</em>{III} = \varepsilon_0 E_0 \mathbf{a}<em>x,\quad \mathbf{P}</em>{III} = 0$</p>
<h1>八、理想电介质的边界条件</h1>
<h2>切向分量</h2>
<p>电场强度的切向分量连续：</p>
<p>$$E_{t1} = E_{t2}$$</p>
<p>电通量密度的切向分量一般不连续：</p>
<p>$$\frac{D_{t1}}{\varepsilon_1} = \frac{D_{t2}}{\varepsilon_2}$$</p>
<h2>法向分量</h2>
<p>若无自由面电荷（$\rho_S = 0$），电通量密度的法向分量连续：</p>
<p>$$D_{N1} = D_{N2}$$</p>
<p>电场强度的法向分量一般不连续：</p>
<p>$$\varepsilon_1 E_{N1} = \varepsilon_2 E_{N2}$$</p>
<h2>电场方向和大小关系</h2>
<p>$$\frac{\tan\theta_1}{\tan\theta_2} = \frac{\varepsilon_1}{\varepsilon_2}$$</p>
<h2>例题5：Teflon 中的场（续）</h2>
<p><strong>题目：</strong> 利用边界条件完成例题4，求 Teflon 内部的场。</p>
<p><strong>解析：</strong></p>
<p>由边界条件，Teflon 与自由空间边界上：</p>
<ul>
<li>法向 $\mathbf{D}$ 连续：$D_{N,\text{Teflon}} = D_{N,\text{free}} = \varepsilon_0 E_0$</li>
<li>切向 $\mathbf{E}$ 连续：$E_{t,\text{Teflon}} = E_{t,\text{free}}$</li>
</ul>
<p>因此 Teflon 内部的 $\mathbf{D}$、$\mathbf{E}$、$\mathbf{P}$ 与例题4中区域 II 的结果一致。</p>
<h2>导体-电介质边界条件</h2>
<p>$$\mathbf{E}<em>{\text{inside}} = 0,\quad \mathbf{D}</em>{\text{inside}} = 0$$
$$E_t = 0,\quad D_t = 0$$
$$D_N = \varepsilon E_N = \rho_S$$</p>
<h2>导体内电荷弛豫</h2>
<p>任何在导体内部引入的电荷都将快速到达表面：
$$\rho_v = \rho_0 e^{-t/(\varepsilon/\sigma)}$$</p>
<p>时间常数 $\tau = \varepsilon/\sigma$：</p>
<ul>
<li>良导体：时间常数很小（$\sim 10^{-19}\text{ s}$），电荷迅速衰减到表面</li>
<li>不良导体：时间常数较大</li>
</ul>
<hr />
<h1>学习自测</h1>
<h2>一、单选题</h2>
<p><strong>1. 电流密度的单位是？</strong>
A. A/m
B. A/m²
C. C/m²
D. V/m</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> 电流密度 $\mathbf{J}$ 的单位是 $\text{A/m}^2$。
关联原文：[[Chp5#电流密度]]</p>
<p><strong>2. 欧姆定律点形式 $\mathbf{J} = \sigma \mathbf{E}$ 中 $\sigma$ 表示？</strong>
A. 介电常数
B. 磁导率
C. 电导率
D. 电极化率</p>
<p><strong>答案：</strong> C
<strong>解析：</strong> $\sigma$ 为电导率，单位 $\text{S/m}$。
关联原文：[[Chp5#欧姆定律的点形式]]</p>
<p><strong>3. 静电场中导体内部的电场强度为？</strong>
A. 无穷大
B. 零
C. 等于表面电场
D. 由外部电场决定</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> 静电场条件下导体内 $\mathbf{E} = 0$。
关联原文：[[Chp5#导体的静电性质]]</p>
<p><strong>4. 电介质中，$\mathbf{D}$ 与 $\mathbf{E}$ 和 $\mathbf{P}$ 的关系是？</strong>
A. $\mathbf{D} = \varepsilon_0 \mathbf{E} + \mathbf{P}$
B. $\mathbf{D} = \varepsilon_0 \mathbf{E} - \mathbf{P}$
C. $\mathbf{D} = \varepsilon_0 \mathbf{E}$
D. $\mathbf{D} = \mathbf{P} - \varepsilon_0 \mathbf{E}$</p>
<p><strong>答案：</strong> A
<strong>解析：</strong> $\mathbf{D} = \varepsilon_0 \mathbf{E} + \mathbf{P}$，这是电通量密度在电介质中的一般形式。
关联原文：[[Chp5#电通量密度的一般形式]]</p>
<h2>二、多选题</h2>
<p><strong>5. 导体的静电性质包括？</strong>
A. 导体内 $\rho_v = 0$
B. 导体内 $\mathbf{E} = 0$
C. 导体表面为等位面
D. 导体表面电场与表面相切</p>
<p><strong>答案：</strong> A、B、C
<strong>解析：</strong> 导体表面电场处处垂直于表面（法向），而非相切。
关联原文：[[Chp5#导体的静电性质]]</p>
<p><strong>6. 电流连续性方程的微分形式表明？</strong>
A. $\nabla \cdot \mathbf{J} = -\partial \rho_v/\partial t$
B. 电荷守恒
C. 电流密度散度为零
D. 电荷可以创生</p>
<p><strong>答案：</strong> A、B
<strong>解析：</strong> $\nabla \cdot \mathbf{J} = -\partial \rho_v/\partial t$ 是电荷守恒定律的微分形式。只有稳恒条件下 $\nabla \cdot \mathbf{J} = 0$。
关联原文：[[Chp5#微分形式（点形式）]]</p>
<h2>三、判断题</h2>
<p><strong>7. 镜像法中去掉导电平面后，需在原电荷的对称位置放置等量同号电荷。</strong>
<strong>答案：</strong> 错误
<strong>解析：</strong> 镜像电荷与原电荷大小相等、符号相反，以保证导电平面的电位为零。
关联原文：[[Chp5#镜像法的原理]]</p>
<p><strong>8. 半导体的电导率随温度升高而降低。</strong>
<strong>答案：</strong> 错误
<strong>解析：</strong> 半导体电导率随温度升高而增大（载流子密度快速增加）。金属才随温度升高而电导率降低。
关联原文：[[Chp5#半导体的电导率]]</p>
<h2>四、简答题</h2>
<p><strong>9. 简述理想电介质边界条件的法向和切向关系。</strong>
<strong>参考答案：</strong> 切向电场 $E_t$ 连续，切向 $D_t$ 一般不连续；法向 $D_N$ 连续（无自由面电荷时），法向 $E_N$ 一般不连续。综合得 $\tan\theta_1/\tan\theta_2 = \varepsilon_1/\varepsilon_2$。
关联原文：[[Chp5#八、理想电介质的边界条件]]</p>
<hr />
<h1>一、电容的定义</h1>
<h2>电容的基本概念</h2>
<p>两个导体嵌入均匀电介质中，导体 $M_2$ 带正电荷 $+Q$，导体 $M_1$ 带等量负电荷 $-Q$。两导体间的电位差为 $V_0$。</p>
<p><strong>电容</strong>定义为任一导体上的总电荷绝对值与导体间电位差之比：</p>
<p>$$C = \frac{Q}{V_0} = \frac{Q}{\int \mathbf{E} \cdot d\mathbf{L}}$$</p>
<ul>
<li>电容与电位和总电荷的比值恒定</li>
<li>电容仅取决于导体的几何尺寸和介质的介电常数</li>
<li>单位：法拉（F），$1 \text{ F} = 1 \text{ C/V}$</li>
</ul>
<h2>平行板电容器</h2>
<p>两无限大平行平面导体（间距 $d \ll$ 平面线度），介质介电常数为 $\varepsilon$：</p>
<p>$$C = \frac{\varepsilon S}{d}$$</p>
<p>其中 $S$ 为极板面积。</p>
<h2>例题1：平行板电容计算</h2>
<p><strong>题目：</strong> 计算以云母为介质（$\varepsilon_r = 6$），极板面积 $10 \text{ in}^2$，间距 $0.01 \text{ in}$ 的平行板电容器的电容。</p>
<p><strong>解析：</strong></p>
<p>首先进行单位换算：
$10 \text{ in}^2 = 10 \times (0.0254)^2 = 6.452 \times 10^{-3} \text{ m}^2$
$0.01 \text{ in} = 0.01 \times 0.0254 = 2.54 \times 10^{-4} \text{ m}$</p>
<p>$$C = \frac{\varepsilon S}{d} = \frac{\varepsilon_0 \varepsilon_r S}{d} = \frac{8.854 \times 10^{-12} \times 6 \times 6.452 \times 10^{-3}}{2.54 \times 10^{-4}} = 1.35 \text{ nF}$$</p>
<h1>二、几种典型的电容</h1>
<h2>同轴电缆（圆柱电容器）</h2>
<p>内导体半径 $a$，外导体内半径 $b$，长度 $L$：</p>
<p>$$C = \frac{2\pi\varepsilon L}{\ln(b/a)}$$</p>
<h2>球形电容器</h2>
<p>两个同心导体球壳，内球半径 $a$，外球内半径 $b$：</p>
<p>$$C = \frac{4\pi\varepsilon ab}{b - a}$$</p>
<h2>孤立导体球的电容</h2>
<p>当 $b \to \infty$：</p>
<p>$$C = 4\pi\varepsilon a$$</p>
<p>一个直径 1 cm 的孤立导体球电容约为 $0.556 \text{ pF}$。</p>
<h2>多层介质电容器</h2>
<h3>介质分界面平行于极板（串联）</h3>
<p>$$\frac{1}{C} = \frac{1}{C_1} + \frac{1}{C_2}$$</p>
<h3>介质分界面垂直于极板（并联）</h3>
<p>$$C = C_1 + C_2$$</p>
<h1>三、静电场的能量密度</h1>
<h2>电场能量</h2>
<p>$$W_E = \frac{1}{2}QV = \frac{1}{2}CV^2 = \frac{Q^2}{2C}$$</p>
<h2>电场能量密度</h2>
<p>$$w_E = \frac{1}{2}\mathbf{D} \cdot \mathbf{E} = \frac{1}{2}\varepsilon E^2$$</p>
<p>总静电能：$W_E = \int_{\text{vol}} \frac{1}{2}\mathbf{D} \cdot \mathbf{E} , dv$</p>
<h1>四、双线传输线的电容</h1>
<h2>电位分布</h2>
<p>采用两根平行线电荷（$\pm\rho_L$ 位于 $x = \pm a$）等效：</p>
<p>$$V = \frac{\rho_L}{2\pi\varepsilon} \ln\frac{R_2}{R_1}$$</p>
<p>圆柱表面的电位 $V_0 = \frac{\rho_L}{2\pi\varepsilon} \ln K_1$</p>
<p>其中 $a = \sqrt{h^2 - b^2}$，$K_1 = \frac{h + \sqrt{h^2 - b^2}}{b}$</p>
<h3>单导线-地平面的电容</h3>
<p>$$C = \frac{2\pi\varepsilon L}{\ln(2h/b)}$$</p>
<h3>双线传输线的电容</h3>
<p>$$C = \frac{\pi\varepsilon L}{\ln(2h/b)}$$</p>
<h2>例题2：圆柱-平面系统确认参数</h2>
<p><strong>题目：</strong> 半径 5 m 的圆柱体电位 100 V，轴线距零电位平面 13 m。在自由空间中，求相关参数。</p>
<p><strong>解析：</strong></p>
<p>$a = \sqrt{h^2 - b^2} = \sqrt{13^2 - 5^2} = 12 \text{ m}$
$K_1 = (h + a)/b = (13 + 12)/5 = 5$
$\rho_L = 2\pi\varepsilon_0 V_0/\ln K_1$</p>
<p>可进一步确定 50 V 等位面的参数。</p>
<h1>五、泊松方程与拉普拉斯方程</h1>
<h2>方程形式</h2>
<p>泊松方程：$\nabla^2 V = -\rho_v/\varepsilon$
拉普拉斯方程：$\nabla^2 V = 0$</p>
<h2>拉普拉斯算子</h2>
<p>直角坐标系：$\nabla^2 V = \frac{\partial^2 V}{\partial x^2} + \frac{\partial^2 V}{\partial y^2} + \frac{\partial^2 V}{\partial z^2}$</p>
<p>圆柱坐标系：$\nabla^2 V = \frac{1}{\rho}\frac{\partial}{\partial\rho}(\rho\frac{\partial V}{\partial\rho}) + \frac{1}{\rho^2}\frac{\partial^2 V}{\partial\phi^2} + \frac{\partial^2 V}{\partial z^2}$</p>
<p>球坐标系：$\nabla^2 V = \frac{1}{r^2}\frac{\partial}{\partial r}(r^2\frac{\partial V}{\partial r}) + \frac{1}{r^2\sin\theta}\frac{\partial}{\partial\theta}(\sin\theta\frac{\partial V}{\partial\theta}) + \frac{1}{r^2\sin^2\theta}\frac{\partial^2 V}{\partial\phi^2}$</p>
<h2>例题3：平行板电容器（拉普拉斯方程求解）</h2>
<p><strong>题目：</strong> 平行板电容器间距 $d$，$V(0) = 0$，$V(d) = V_0$。求电位分布、电场强度和电容。</p>
<p><strong>解析：</strong></p>
<p>$$\frac{d^2V}{dx^2} = 0 \Rightarrow V = Ax + B$$</p>
<p>边界条件：$B = 0$，$A = V_0/d$</p>
<p><strong>电位分布：</strong> $V(x) = (V_0/d)x$</p>
<p><strong>电场强度：</strong> $\mathbf{E} = -\frac{V_0}{d}\mathbf{a}_x$</p>
<p><strong>电容：</strong> $C = \varepsilon S/d$</p>
<h2>例题4：同轴传输线</h2>
<p><strong>题目：</strong> 同轴电缆内导体半径 $a$（电位 $V_0$），外导体内半径 $b$（电位 0），求电位分布和电容。</p>
<p><strong>解析：</strong></p>
<p>$$\frac{1}{\rho}\frac{d}{d\rho}(\rho\frac{dV}{d\rho}) = 0 \Rightarrow V = A\ln\rho + B$$</p>
<p>边界条件：$V(b) = 0$，$V(a) = V_0$</p>
<p>$A = V_0/\ln(a/b)$，$B = -A\ln b$</p>
<p><strong>电位分布：</strong> $V(\rho) = V_0\frac{\ln(b/\rho)}{\ln(b/a)}$</p>
<p><strong>电场强度：</strong> $\mathbf{E} = \frac{V_0}{\rho\ln(b/a)}\mathbf{a}_\rho$</p>
<p><strong>电容：</strong> $C = \frac{2\pi\varepsilon L}{\ln(b/a)}$</p>
<h2>例题5：扇形极板</h2>
<p><strong>题目：</strong> 两扇形极板 $\phi=0$（电位 0）和 $\phi=\alpha$（电位 $V_0$），求电位分布。</p>
<p><strong>解析：</strong></p>
<p>$$\frac{d^2V}{d\phi^2} = 0 \Rightarrow V = A\phi + B$$</p>
<p>边界条件：$V(0)=0 \Rightarrow B=0$，$V(\alpha)=V_0 \Rightarrow A=V_0/\alpha$</p>
<p><strong>电位分布：</strong> $V(\phi) = V_0\phi/\alpha$</p>
<p><strong>电场强度：</strong> $\mathbf{E} = -\frac{V_0}{\alpha\rho}\mathbf{a}_\phi$</p>
<h2>例题6：同心球</h2>
<p><strong>题目：</strong> 内球半径 $a$（电位 $V_0$），外球半径 $b$（电位 0），求电位分布和电容。</p>
<p><strong>解析：</strong></p>
<p>$$\frac{1}{r^2}\frac{d}{dr}(r^2\frac{dV}{dr}) = 0 \Rightarrow V = -\frac{A}{r} + B$$</p>
<p>边界条件：$V(b)=0$，$V(a)=V_0$</p>
<p>$A = \frac{abV_0}{a-b}$，$B = \frac{aV_0}{a-b}$</p>
<p><strong>电位分布：</strong> $V(r) = V_0\frac{a(b-r)}{r(b-a)}$</p>
<p><strong>电容：</strong> $C = \frac{4\pi\varepsilon ab}{b-a}$</p>
<h2>例题7：锥形结构</h2>
<p><strong>题目：</strong> 锥体悬浮于导电平面上，电位随 $\theta$ 变化，求解电容。</p>
<p><strong>解析：</strong></p>
<p>从拉普拉斯方程解得电位随 $\theta$ 的分布。</p>
<p>电场强度 $\mathbf{E} = -\nabla V$，面电荷密度 $\rho_S = \mathbf{D} \cdot \mathbf{n}$。</p>
<p>总电荷通过 $Q = \int \rho_S dS$ 求得，电容 $C = Q/V_0$。</p>
<p>注意这是近似结果，忽略了锥体边缘的边缘效应（fringing fields）。</p>
<hr />
<h1>学习自测</h1>
<h2>一、单选题</h2>
<p><strong>1. 电容的单位是？</strong>
A. 亨利（H）
B. 法拉（F）
C. 韦伯（Wb）
D. 特斯拉（T）</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> 电容的单位是法拉（F），$1\text{ F} = 1\text{ C/V}$。
关联原文：[[updatedChp6#电容的基本概念]]</p>
<p><strong>2. 平行板电容器的电容与极板间距的关系是？</strong>
A. 正比
B. 反比
C. 无关
D. 平方正比</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> $C = \varepsilon S/d$，电容与极板间距 $d$ 成反比。
关联原文：[[updatedChp6#平行板电容器]]</p>
<p><strong>3. 泊松方程 $\nabla^2 V = -\rho_v/\varepsilon$ 中，当 $\rho_v = 0$ 时变为？</strong>
A. 麦克斯韦方程
B. 拉普拉斯方程
C. 欧姆定律
D. 高斯定律</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> 当 $\rho_v = 0$ 时，泊松方程简化为拉普拉斯方程 $\nabla^2 V = 0$。
关联原文：[[updatedChp6#方程形式]]</p>
<h2>二、多选题</h2>
<p><strong>4. 以下关于电容的说法正确的是？</strong>
A. 电容取决于导体的几何尺寸
B. 电容与介质的介电常数有关
C. 电容与外加电压成正比
D. 电容与总电荷成正比</p>
<p><strong>答案：</strong> A、B
<strong>解析：</strong> 电容仅取决于导体的几何尺寸和介质的介电常数，与电压和电荷无关。
关联原文：[[updatedChp6#电容的基本概念]]</p>
<p><strong>5. 电场能量密度的表达式包括？</strong>
A. $\frac{1}{2}\varepsilon E^2$
B. $\frac{1}{2}\mathbf{D} \cdot \mathbf{E}$
C. $\frac{1}{2}CV^2$
D. $\frac{1}{2}QV$</p>
<p><strong>答案：</strong> A、B
<strong>解析：</strong> 电场能量密度 $w_E = \mathbf{D}\cdot\mathbf{E}/2 = \varepsilon E^2/2$。
关联原文：[[updatedChp6#电场能量密度]]</p>
<h2>三、判断题</h2>
<p><strong>6. 同轴电缆的电容 $C = 2\pi\varepsilon L / \ln(b/a)$。</strong>
<strong>答案：</strong> 正确
<strong>解析：</strong> 这是同轴电缆电容的标准公式。
关联原文：[[updatedChp6#同轴电缆（圆柱电容器）]]</p>
<p><strong>7. 孤立导体球的电容与球的半径的平方成正比。</strong>
<strong>答案：</strong> 错误
<strong>解析：</strong> 孤立导体球的电容 $C = 4\pi\varepsilon a$，与半径 $a$ 成正比。
关联原文：[[updatedChp6#孤立导体球的电容]]</p>
<h2>四、简答题</h2>
<p><strong>8. 简述泊松方程和拉普拉斯方程的物理意义。</strong>
<strong>参考答案：</strong> 泊松方程 $\nabla^2 V = -\rho_v/\varepsilon$ 描述有体电荷分布时电位与电荷密度的关系。拉普拉斯方程 $\nabla^2 V = 0$ 是无源区域中电位的控制方程。两者结合边界条件可用于求解电位场。
关联原文：[[updatedChp6#五、泊松方程与拉普拉斯方程]]</p>
<hr />
<h1>一、毕奥-萨伐尔定律</h1>
<h2>定律表述</h2>
<p><strong>毕奥-萨伐尔定律</strong>描述了一个微分电流元 $Id\mathbf{L}$ 产生的磁场：</p>
<p>$$d\mathbf{H} = \frac{I d\mathbf{L} \times \mathbf{a}_R}{4\pi R^2}$$</p>
<p>其中 $d\mathbf{H}$ 的单位为 $\text{A/m}$。</p>
<p>对于体电流密度 $\mathbf{J}$ 和面电流密度 $\mathbf{K}$：</p>
<p>$$d\mathbf{H} = \frac{\mathbf{J} dv \times \mathbf{a}_R}{4\pi R^2}, \quad d\mathbf{H} = \frac{\mathbf{K} dS \times \mathbf{a}_R}{4\pi R^2}$$</p>
<h2>无限长直导线的磁场</h2>
<p>无限长直导线载流 $I$，距离导线 $\rho$ 处的磁场：</p>
<p>$$\mathbf{H} = \frac{I}{2\pi\rho} \mathbf{a}_\phi$$</p>
<ul>
<li>磁场大小与距离成反比</li>
<li>磁场方向为环绕导线的圆周方向（右手定则）</li>
</ul>
<h2>有限长导线的磁场</h2>
<p>对于有限长直线电流，磁场公式需积分求解：</p>
<p>$$\mathbf{H} = \frac{I}{4\pi\rho}(\sin\alpha_2 - \sin\alpha_1)\mathbf{a}_\phi$$</p>
<p>其中 $\alpha_1$ 和 $\alpha_2$ 分别为导线两端到场点的方位角。</p>
<h2>例题1：折线电流的磁场</h2>
<p><strong>题目：</strong> 求 $P_2(0.4, 0.3, 0)$ 处的 $\mathbf{H}$。一根 8 A 的线电流从正 $x$ 轴无穷远沿 $x$ 轴流入原点，再沿 $y$ 轴流向无穷远。</p>
<p><strong>解析：</strong></p>
<p>利用毕奥-萨伐尔定律分别计算两段导线在 $P_2$ 处的磁场贡献，然后叠加。</p>
<p>$x$ 轴段（从 $-\infty$ 到 0）：在 $P_2(0.4,0.3,0)$ 处的 $d\mathbf{L} = dx\mathbf{a}_x$，
$\mathbf{R} = 0.4\mathbf{a}_x + 0.3\mathbf{a}_y$
$| \mathbf{R} | = \sqrt{0.4^2 + 0.3^2} = 0.5$</p>
<p>$y$ 轴段（从 0 到 $\infty$）：在 $P_2$ 处的 $d\mathbf{L} = dy\mathbf{a}_y$</p>
<p>分别计算两段在 $P_2$ 处的 $d\mathbf{H}$ 并积分，然后求和。</p>
<h1>二、安培环路定律</h1>
<h2>定律表述</h2>
<p><strong>安培环路定律</strong>：磁场强度 $\mathbf{H}$ 沿任意闭合路径的线积分等于该路径所包围的总电流：</p>
<p>$$\oint \mathbf{H} \cdot d\mathbf{L} = I_{\text{enc}}$$</p>
<p>安培环路定律常用于求解具有高度对称性的磁场问题。</p>
<h2>应用示例1：无限长直导线</h2>
<p>以导线为中心取圆形路径（半径 $\rho$）：</p>
<p>$$\oint \mathbf{H} \cdot d\mathbf{L} = H_\phi \cdot 2\pi\rho = I$$</p>
<p>$$H_\phi = \frac{I}{2\pi\rho}$$</p>
<h2>应用示例2：同轴传输线</h2>
<p>同轴传输线内导体半径 $a$，外导体内半径 $b$，外半径 $c$。</p>
<p><strong>区域 $0 &lt; \rho &lt; a$（内导体内部）：</strong>
$$I_{\text{enc}} = I\frac{\rho^2}{a^2}, \quad H_\phi = \frac{I\rho}{2\pi a^2}$$</p>
<p><strong>区域 $a &lt; \rho &lt; b$（导体间）：</strong>
$$I_{\text{enc}} = I, \quad H_\phi = \frac{I}{2\pi\rho}$$</p>
<p><strong>区域 $b &lt; \rho &lt; c$（外导体内部）：</strong>
$$I_{\text{enc}} = I\frac{c^2 - \rho^2}{c^2 - b^2}, \quad H_\phi = \frac{I}{2\pi\rho}\frac{c^2 - \rho^2}{c^2 - b^2}$$</p>
<p><strong>区域 $\rho &gt; c$（外部）：</strong>
$$I_{\text{enc}} = 0, \quad H_\phi = 0$$</p>
<p>同轴电缆对外部不产生磁场——具有<strong>屏蔽效应</strong>。</p>
<h2>应用示例3：电流片</h2>
<p>无限大电流片位于 $z = 0$ 平面，面电流密度 $\mathbf{K} = K_y \mathbf{a}_y$：</p>
<p>$$\mathbf{H} = \pm\frac{K_y}{2}\mathbf{a}_x$$</p>
<p>上方取 $+$，下方取 $-$。</p>
<p>两片反向电流片（间距 $h$）之间：
$$\mathbf{H} = K_y \mathbf{a}_x$$
外部为零。</p>
<h2>理想螺线管</h2>
<p>无限长螺线管，单位长度匝数 $n$，电流 $I$：</p>
<p><strong>内部：</strong> $H = nI$（均匀）
<strong>外部：</strong> $H = 0$</p>
<h2>理想环形螺线管</h2>
<p>$N$ 匝环形螺线管：</p>
<p><strong>内部：</strong> $H_\phi = \frac{NI}{2\pi\rho}$（沿圆周方向）
<strong>外部：</strong> $H = 0$</p>
<h1>三、旋度</h1>
<h2>旋度的定义</h2>
<p>旋度是矢量场的"环量面密度"（每单位面积的环量）：</p>
<p>$$(\nabla \times \mathbf{H})<em>N = \lim</em>{\Delta S_N \to 0} \frac{\oint \mathbf{H} \cdot d\mathbf{L}}{\Delta S_N}$$</p>
<p><strong>物理意义：</strong> 旋度描述了矢量场的旋转特性。</p>
<ul>
<li>静电场：$\nabla \times \mathbf{E} = 0$（无旋场）</li>
<li>稳恒磁场：$\nabla \times \mathbf{H} = \mathbf{J}$（有旋场）</li>
</ul>
<h2>旋度的表达式</h2>
<p><strong>直角坐标系：</strong>
$$\nabla \times \mathbf{H} = \begin{vmatrix} \mathbf{a}_x &amp; \mathbf{a}_y &amp; \mathbf{a}_z \ \frac{\partial}{\partial x} &amp; \frac{\partial}{\partial y} &amp; \frac{\partial}{\partial z} \ H_x &amp; H_y &amp; H_z \end{vmatrix}$$</p>
<p><strong>圆柱坐标系：</strong>
$$\nabla \times \mathbf{H} = \frac{1}{\rho}\begin{vmatrix} \mathbf{a}<em>\rho &amp; \rho\mathbf{a}</em>\phi &amp; \mathbf{a}<em>z \ \frac{\partial}{\partial\rho} &amp; \frac{\partial}{\partial\phi} &amp; \frac{\partial}{\partial z} \ H</em>\rho &amp; \rho H_\phi &amp; H_z \end{vmatrix}$$</p>
<h2>安培环路定律的点形式</h2>
<p>$$\nabla \times \mathbf{H} = \mathbf{J}$$</p>
<p>这是安培环路定律的微分形式，也是<strong>麦克斯韦第二方程</strong>（非时变条件）。</p>
<p>同时，$\nabla \times \mathbf{E} = 0$ 是<strong>麦克斯韦第三方程</strong>（非时变条件）。</p>
<h2>例题2：旋度计算</h2>
<p><strong>题目：</strong> 已知磁场 $\mathbf{H}$ 的表达式，求 $\nabla \times \mathbf{H}$。</p>
<p><strong>解析：</strong> 根据旋度定义，计算 $\mathbf{H}$ 的各分量偏导，代入坐标系的旋度公式即可。</p>
<h1>四、斯托克斯定理</h1>
<p>$$\oint \mathbf{H} \cdot d\mathbf{L} = \int_S (\nabla \times \mathbf{H}) \cdot d\mathbf{S}$$</p>
<p>斯托克斯定理将闭合路径的线积分与其所围曲面的旋度面积分联系起来。</p>
<h2>例题3：验证斯托克斯定理</h2>
<p><strong>题目：</strong> 球面 $r = 4$，$0 \le \theta \le 0.1\pi$，$0 \le \phi \le 0.3\pi$ 上，$\mathbf{H} = 6r\sin\phi,\mathbf{a}<em>r + 18r\sin\theta\cos\phi,\mathbf{a}</em>\phi$，验证斯托克斯定理。</p>
<p><strong>解析：</strong></p>
<p><strong>左侧——线积分：</strong> 沿球面边界的三段圆弧分别计算 $\oint \mathbf{H} \cdot d\mathbf{L}$，求和得约 $22.2 \text{ A}$。</p>
<p><strong>右侧——面积分：</strong> 计算 $\nabla \times \mathbf{H}$ 在球面上的面积分，结果应与左侧相等。</p>
<h1>五、磁通量与磁通密度</h1>
<h2>磁通密度</h2>
<p>在自由空间中，定义磁通密度 $\mathbf{B}$：</p>
<p>$$\mathbf{B} = \mu_0 \mathbf{H}$$</p>
<p>其中 $\mu_0 = 4\pi \times 10^{-7} \text{ H/m}$ 为真空磁导率。</p>
<p>$\mathbf{B}$ 的单位为 $\text{Wb/m}^2$（韦伯/平方米）或特斯拉（T）。</p>
<h2>磁通量</h2>
<p>磁通量 $\Phi$ 定义为穿过某面积的磁通密度通量：</p>
<p>$$\Phi = \int_S \mathbf{B} \cdot d\mathbf{S}$$</p>
<p>单位：韦伯（Wb）。</p>
<h2>磁通的高斯定律</h2>
<p>$$\oint_S \mathbf{B} \cdot d\mathbf{S} = 0$$</p>
<p>即<strong>穿过任意闭合曲面的磁通量为零</strong>——说明不存在磁单极子（磁荷）。</p>
<p>微分形式：</p>
<p>$$\nabla \cdot \mathbf{B} = 0$$</p>
<p>这是<strong>麦克斯韦第四方程</strong>（静电场和稳恒磁场条件）。</p>
<h2>例题4：同轴电缆的磁通</h2>
<p><strong>题目：</strong> 求同轴电缆在 $a \le \rho \le b$，$0 \le z \le d$ 区域中的磁通。</p>
<p><strong>解析：</strong></p>
<p>$$\mathbf{B} = \mu_0 \mathbf{H} = \frac{\mu_0 I}{2\pi\rho} \mathbf{a}_\phi$$</p>
<p>$$\Phi = \int_S \mathbf{B} \cdot d\mathbf{S} = \int_0^d \int_a^b \frac{\mu_0 I}{2\pi\rho} , d\rho, dz = \frac{\mu_0 I d}{2\pi} \ln\frac{b}{a}$$</p>
<h1>六、标量磁位与矢量磁位</h1>
<h2>标量磁位</h2>
<p>$\mathbf{H} = -\nabla V_m$ 定义标量磁位 $V_m$。</p>
<p>由于 $\nabla \times \mathbf{H} = \mathbf{J}$，标量磁位仅在无电流区域（$\mathbf{J} = 0$）有效。</p>
<h2>矢量磁位</h2>
<p>由于 $\nabla \cdot \mathbf{B} = 0$，而 $\nabla \cdot (\nabla \times \mathbf{A}) = 0$，可定义：</p>
<p>$$\mathbf{B} = \nabla \times \mathbf{A}$$</p>
<p>其中 $\mathbf{A}$ 称为<strong>矢量磁位</strong>，单位为 $\text{Wb/m}$。</p>
<p>为了唯一确定 $\mathbf{A}$，需附加条件（<strong>规范条件</strong>）：</p>
<ul>
<li>库仑规范：$\nabla \cdot \mathbf{A} = 0$</li>
<li>洛伦兹规范：$\nabla \cdot \mathbf{A} = -\mu\varepsilon\frac{\partial V}{\partial t}$</li>
</ul>
<h2>矢量磁位的泊松方程</h2>
<p>由安培定律 $\nabla \times \mathbf{H} = \mathbf{J}$ 和 $\mathbf{B} = \nabla \times \mathbf{A}$：</p>
<p>$$\nabla^2 \mathbf{A} = -\mu \mathbf{J}$$</p>
<p>这是矢量形式的泊松方程。</p>
<h2>麦克斯韦方程总结（静电场和稳恒磁场）</h2>
<p><strong>积分形式：</strong>
$$\oint_S \mathbf{D} \cdot d\mathbf{S} = Q, \quad \oint_S \mathbf{B} \cdot d\mathbf{S} = 0$$
$$\oint \mathbf{E} \cdot d\mathbf{L} = 0, \quad \oint \mathbf{H} \cdot d\mathbf{L} = I$$</p>
<p><strong>微分形式：</strong>
$$\nabla \cdot \mathbf{D} = \rho_v, \quad \nabla \cdot \mathbf{B} = 0$$
$$\nabla \times \mathbf{E} = 0, \quad \nabla \times \mathbf{H} = \mathbf{J}$$</p>
<hr />
<h1>学习自测</h1>
<h2>一、单选题</h2>
<p><strong>1. 毕奥-萨伐尔定律中，磁场强度 $\mathbf{H}$ 的单位是？</strong>
A. A/m
B. T
C. Wb/m²
D. H/m</p>
<p><strong>答案：</strong> A
<strong>解析：</strong> 磁场强度 $\mathbf{H}$ 的单位是 $\text{A/m}$。
关联原文：[[Chp7+Magneti Field#定律表述]]</p>
<p><strong>2. 无限长直导线载流 $I$，距导线 $\rho$ 处的磁场强度为？</strong>
A. $\frac{I}{2\pi\rho}$
B. $\frac{I}{4\pi\rho}$
C. $\frac{I}{2\pi\rho^2}$
D. $\frac{I}{4\pi\rho^2}$</p>
<p><strong>答案：</strong> A
<strong>解析：</strong> $H = I/(2\pi\rho)$，方向由右手定则确定。
关联原文：[[Chp7+Magneti Field#无限长直导线的磁场]]</p>
<p><strong>3. $\nabla \times \mathbf{H} = \mathbf{J}$ 是什么方程？</strong>
A. 高斯定律
B. 欧姆定律
C. 安培环路定律的点形式
D. 毕奥-萨伐尔定律</p>
<p><strong>答案：</strong> C
<strong>解析：</strong> $\nabla \times \mathbf{H} = \mathbf{J}$ 是安培环路定律的微分形式。
关联原文：[[Chp7+Magneti Field#安培环路定律的点形式]]</p>
<h2>二、多选题</h2>
<p><strong>4. 以下哪些是磁场的无源性质表现？</strong>
A. $\oint_S \mathbf{B} \cdot d\mathbf{S} = 0$
B. $\nabla \cdot \mathbf{B} = 0$
C. $\nabla \cdot \mathbf{E} = \rho_v/\varepsilon_0$
D. $\oint_S \mathbf{B} \cdot d\mathbf{S} = \mu_0 I$</p>
<p><strong>答案：</strong> A、B
<strong>解析：</strong> 磁通的高斯定律 $\oint_S \mathbf{B} \cdot d\mathbf{S} = 0$ 和 $\nabla \cdot \mathbf{B} = 0$ 表明不存在磁单极子。
关联原文：[[Chp7+Magneti Field#磁通的高斯定律]]</p>
<p><strong>5. 斯托克斯定理表述正确的是？</strong>
A. $\oint \mathbf{H} \cdot d\mathbf{L} = \int_S (\nabla \times \mathbf{H}) \cdot d\mathbf{S}$
B. 将线积分与面积分联系起来
C. $\oint_S \mathbf{D} \cdot d\mathbf{S} = \int_{\text{vol}} (\nabla \cdot \mathbf{D}) dv$
D. $\oint \mathbf{H} \cdot d\mathbf{L} = I_{\text{enc}}$</p>
<p><strong>答案：</strong> A、B
<strong>解析：</strong> 斯托克斯定理将闭合路径的线积分与其所围曲面的旋度面积分联系起来。C 是散度定理，D 是安培环路定律积分形式。
关联原文：[[Chp7+Magneti Field#四、斯托克斯定理]]</p>
<h2>三、判断题</h2>
<p><strong>6. 同轴电缆对外部空间会产生磁场。</strong>
<strong>答案：</strong> 错误
<strong>解析：</strong> 同轴电缆中 $\rho &gt; c$ 时 $I_{\text{enc}} = 0$，因此 $\mathbf{H} = 0$，具有屏蔽效应。
关联原文：[[Chp7+Magneti Field#应用示例2：同轴传输线]]</p>
<p><strong>7. $\nabla \cdot \mathbf{B} = 0$ 说明存在磁单极子。</strong>
<strong>答案：</strong> 错误
<strong>解析：</strong> $\nabla \cdot \mathbf{B} = 0$ 表明磁通线是闭合的，不存在磁单极子。
关联原文：[[Chp7+Magneti Field#磁通的高斯定律]]</p>
<h2>四、简答题</h2>
<p><strong>8. 简述安培环路定律的物理意义及其应用条件。</strong>
<strong>参考答案：</strong> 安培环路定律：$\mathbf{H}$ 沿任意闭合路径的线积分等于路径所包围的总电流。适用于具有高度对称性的磁场问题（如无限长导线、同轴电缆、螺线管等），需选择合适路径使 $\mathbf{H}$ 与 $d\mathbf{L}$ 平行或垂直且在路径上 $\mathbf{H}$ 为常数。
关联原文：[[Chp7+Magneti Field#二、安培环路定律]]</p>
<p><strong>9. 什么是斯托克斯定理？它与散度定理有何区别？</strong>
<strong>参考答案：</strong> 斯托克斯定理：$\oint \mathbf{H} \cdot d\mathbf{L} = \int_S (\nabla \times \mathbf{H}) \cdot d\mathbf{S}$，将闭合路径的线积分转化为曲面积分。散度定理将闭合曲面的面积分转化为体积分，而斯托克斯定理将闭合路径的线积分转化为曲面积分。
关联原文：[[Chp7+Magneti Field#四、斯托克斯定理]]</p>
<hr />
<h1>一、运动电荷的受力</h1>
<h2>洛伦兹力定律</h2>
<p>电荷在电磁场中受到的总力为：</p>
<p>$$\mathbf{F} = \mathbf{F}_e + \mathbf{F}_m = Q\mathbf{E} + Q(\mathbf{v} \times \mathbf{B})$$</p>
<p>这就是<strong>洛伦兹力定律</strong>（Lorentz Force Law），有时被称为"第五个麦克斯韦方程"。</p>
<p><strong>电场力 $\mathbf{F}_e$：</strong> 沿电场方向（正电荷）
<strong>磁场力 $\mathbf{F}_m$：</strong> 垂直于速度 $\mathbf{v}$ 和磁通密度 $\mathbf{B}$（右手定则）</p>
<p>磁场力的大小：</p>
<p>$$F_m = |Q|vB\sin\theta$$</p>
<p>其中 $\theta$ 为 $\mathbf{v}$ 与 $\mathbf{B}$ 之间的夹角（$&lt; 180^\circ$）。</p>
<p><strong>重要结论：</strong> 静止电荷或平行于磁场运动的电荷不受磁场力。</p>
<p>当电场和磁场同时存在时，电场使电荷加速从而与磁场线垂直交叉，产生垂直于纸面的磁场力分量。</p>
<h1>二、微分电流元受力</h1>
<h2>体电流</h2>
<p>微分电流元 $Id\mathbf{L}$ 在磁场 $\mathbf{B}$ 中受到的力：</p>
<p>$$d\mathbf{F} = Id\mathbf{L} \times \mathbf{B}$$</p>
<p>对于体电流密度 $\mathbf{J}$：$Id\mathbf{L} = \mathbf{J} dv$</p>
<p>$$d\mathbf{F} = \mathbf{J} \times \mathbf{B} , dv$$</p>
<p>对于面电流密度 $\mathbf{K}$：$Id\mathbf{L} = \mathbf{K} dS$</p>
<p>$$d\mathbf{F} = \mathbf{K} \times \mathbf{B} , dS$$</p>
<h2>总力</h2>
<p>对体电流或面电流积分求总力：</p>
<p>$$\mathbf{F} = \int_{\text{vol}} \mathbf{J} \times \mathbf{B} , dv, \quad \mathbf{F} = \int_S \mathbf{K} \times \mathbf{B} , dS$$</p>
<p>对线电流（闭合回路）：</p>
<p>$$\mathbf{F} = \oint I , d\mathbf{L} \times \mathbf{B}$$</p>
<p>对于均匀磁场中的直导线（长度 $L$，电流 $I$）：</p>
<p>$$\mathbf{F} = I\mathbf{L} \times \mathbf{B}$$</p>
<h1>三、闭合回路受力与直流电机</h1>
<p>闭合载流回路在磁场中受到的总力可由上述公式积分求得。这是**直流电机（DC Motor）**的基本原理：载流线圈在磁场中受到力矩而转动。</p>
<h1>四、磁性材料的性质</h1>
<h2>原子磁矩</h2>
<p>磁性材料按原子磁矩的配置和相互作用分类。原子磁矩的来源包括：</p>
<ul>
<li><strong>电子轨道运动</strong>（$m_{\text{orb}}$）</li>
<li><strong>电子自旋</strong>（$m_{\text{spin}}$）</li>
<li>原子核自旋（贡献较小）</li>
</ul>
<p>总磁矩为轨道磁矩和自旋磁矩的矢量和。</p>
<h2>顺磁性与反磁性</h2>
<p><strong>顺磁性：</strong> 原子具有净磁矩，在外磁场中趋于定向排列，磁化方向与磁场同向。
<strong>反磁性：</strong> 轨道电子在外磁场中产生感应磁矩，方向与外磁场相反。</p>
<h2>铁磁性</h2>
<p>铁磁性材料具有较强的原子磁矩，相邻分子的磁矩通过相互作用在小区域（<strong>磁畴</strong>，domain）内部分对齐。</p>
<p>无外磁场时，各磁畴磁矩取向随机，总磁矩为零。
施加外磁场 $\mathbf{B}_0$ 后，与外磁场方向接近的磁畴生长，产生显著的磁通密度增强。</p>
<p><strong>铁磁性元素：</strong> 铁（Fe）、镍（Ni）、钴（Co）（室温）；钆（Gd）、镝（Dy）（低温）。</p>
<h1>五、磁化与磁导率</h1>
<h2>磁偶极子集合</h2>
<p>束缚电流 $I_b$ 环绕微分面积 $d\mathbf{S}$，产生磁矩：</p>
<p>$$d\mathbf{m} = I_b , d\mathbf{S}$$</p>
<p>总磁矩为每个偶极子的矢量和：</p>
<p>$$\mathbf{m}<em>{\text{total}} = \sum</em>{i=1}^{n\Delta v} \mathbf{m}_i$$</p>
<h2>磁化强度</h2>
<p><strong>磁化强度 $\mathbf{M}$</strong> 定义为单位体积的磁偶极矩：</p>
<p>$$\mathbf{M} = \lim_{\Delta v \to 0} \frac{1}{\Delta v} \sum_{i=1}^{n\Delta v} \mathbf{m}_i$$</p>
<p>单位：$\text{A/m}$（与 $\mathbf{H}$ 相同）。</p>
<p>若所有偶极子相同且同向：$\mathbf{M} = n\mathbf{m}$</p>
<h2>束缚电流</h2>
<p>磁偶极子排列产生束缚电流（magnetization current 或 bound current）。</p>
<p>沿微分长度 $d\mathbf{L}$ 的束缚电流为：</p>
<p>$$dI_b = \mathbf{M} \cdot d\mathbf{L}$$</p>
<p>体束缚电流密度：$\mathbf{J}_b = \nabla \times \mathbf{M}$
面束缚电流密度：$\mathbf{K}_b = \mathbf{M} \times \mathbf{a}_n$</p>
<h2>磁场强度与磁化</h2>
<p>在磁性材料中，磁通密度 $\mathbf{B}$、磁场强度 $\mathbf{H}$ 和磁化强度 $\mathbf{M}$ 的关系为：</p>
<p>$$\mathbf{B} = \mu_0(\mathbf{H} + \mathbf{M})$$</p>
<p>对各向同性线性介质：</p>
<p>$$\mathbf{M} = \chi_m \mathbf{H}$$</p>
<p>其中 $\chi_m$ 为磁化率。</p>
<p>$$\mathbf{B} = \mu_0(1 + \chi_m)\mathbf{H} = \mu_0\mu_r \mathbf{H} = \mu \mathbf{H}$$</p>
<p>其中 $\mu_r = 1 + \chi_m$ 为相对磁导率。</p>
<hr />
<h1>学习自测</h1>
<h2>一、单选题</h2>
<p><strong>1. 洛伦兹力定律中，磁场力 $\mathbf{F}_m$ 的方向？</strong>
A. 沿电场方向
B. 垂直于 $\mathbf{v}$ 和 $\mathbf{B}$
C. 沿速度方向
D. 沿磁场方向</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> $\mathbf{F}_m = Q(\mathbf{v} \times \mathbf{B})$，方向垂直于 $\mathbf{v}$ 和 $\mathbf{B}$ 所在平面。
关联原文：[[Chp8 Magnetic Materials 2023-lxl#洛伦兹力定律]]</p>
<p><strong>2. 磁场力的大小 $F_m = qvB\sin\theta$ 中，$\theta$ 是什么？</strong>
A. $\mathbf{v}$ 与 $\mathbf{F}$ 的夹角
B. $\mathbf{v}$ 与 $\mathbf{B}$ 的夹角
C. $\mathbf{L}$ 与 $\mathbf{B}$ 的夹角
D. $\mathbf{E}$ 与 $\mathbf{B}$ 的夹角</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> $\theta$ 为速度 $\mathbf{v}$ 与磁通密度 $\mathbf{B}$ 之间的夹角。
关联原文：[[Chp8 Magnetic Materials 2023-lxl#洛伦兹力定律]]</p>
<p><strong>3. 磁化强度 $\mathbf{M}$ 的单位是？</strong>
A. A/m
B. T
C. Wb/m
D. H/m</p>
<p><strong>答案：</strong> A
<strong>解析：</strong> 磁化强度 $\mathbf{M}$ 的单位是 $\text{A/m}$，与 $\mathbf{H}$ 相同。
关联原文：[[Chp8 Magnetic Materials 2023-lxl#磁化强度]]</p>
<p><strong>4. 铁磁性材料在无外磁场时？</strong>
A. 具有强磁性
B. 磁畴取向随机，总磁矩为零
C. 所有原子磁矩完全对齐
D. 磁畴不存在</p>
<p><strong>答案：</strong> B
<strong>解析：</strong> 铁磁性材料内部磁畴磁矩随机取向，无外磁场时总磁矩为零。
关联原文：[[Chp8 Magnetic Materials 2023-lxl#铁磁性]]</p>
<h2>二、多选题</h2>
<p><strong>5. 铁磁性元素包括？</strong>
A. 铁（Fe）
B. 镍（Ni）
C. 钴（Co）
D. 铜（Cu）</p>
<p><strong>答案：</strong> A、B、C
<strong>解析：</strong> 铁、镍、钴是室温下的铁磁性元素。铜是非铁磁性材料。
关联原文：[[Chp8 Magnetic Materials 2023-lxl#铁磁性]]</p>
<p><strong>6. $\mathbf{B} = \mu_0(\mathbf{H} + \mathbf{M})$ 中各量的含义是？</strong>
A. $\mathbf{B}$ 为磁通密度
B. $\mathbf{H}$ 为磁场强度
C. $\mathbf{M}$ 为磁化强度
D. $\mathbf{M}$ 为磁导率</p>
<p><strong>答案：</strong> A、B、C
<strong>解析：</strong> $\mathbf{B} = \mu_0(\mathbf{H} + \mathbf{M})$ 是磁性材料中三个磁场量的关系式。
关联原文：[[Chp8 Magnetic Materials 2023-lxl#磁场强度与磁化]]</p>
<h2>三、判断题</h2>
<p><strong>7. 静止电荷在磁场中会受到磁场力。</strong>
<strong>答案：</strong> 错误
<strong>解析：</strong> 磁场力 $\mathbf{F}_m = Q(\mathbf{v} \times \mathbf{B})$，只有当电荷运动（$v \neq 0$）时才不为零。
关联原文：[[Chp8 Magnetic Materials 2023-lxl#洛伦兹力定律]]</p>
<p><strong>8. 顺磁性材料的磁化方向与外磁场方向相反。</strong>
<strong>答案：</strong> 错误
<strong>解析：</strong> 顺磁性材料的磁化方向与外磁场方向相同。反磁性材料的磁化方向才与外磁场方向相反。
关联原文：[[Chp8 Magnetic Materials 2023-lxl#顺磁性与反磁性]]</p>
<h2>四、简答题</h2>
<p><strong>9. 简述铁磁性材料的特点。</strong>
<strong>参考答案：</strong> 铁磁性材料具有较强原子磁矩，相邻分子相互作用形成磁畴。无外场时磁畴取向随机，总磁矩为零。有外场时磁畴沿外场方向对齐，产生显著磁化。典型铁磁性元素有 Fe、Ni、Co。
关联原文：[[Chp8 Magnetic Materials 2023-lxl#四、磁性材料的性质]]</p>
<p><strong>10. 写出 $\mathbf{B}$、$\mathbf{H}$、$\mathbf{M}$ 之间的关系，并说明各量的意义。</strong>
<strong>参考答案：</strong> $\mathbf{B} = \mu_0(\mathbf{H} + \mathbf{M})$，其中 $\mathbf{B}$ 为磁通密度，$\mathbf{H}$ 为磁场强度，$\mathbf{M}$ 为磁化强度，$\mu_0$ 为真空磁导率。对于线性各向同性介质，$\mathbf{M} = \chi_m \mathbf{H}$，$\mathbf{B} = \mu \mathbf{H}$，$\mu = \mu_0(1 + \chi_m) = \mu_0\mu_r$。
关联原文：[[Chp8 Magnetic Materials 2023-lxl#磁场强度与磁化]]</p>
<hr />
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="电子信息工程"></category>
  </entry>
  <entry>
    <title>固体电子学公式总结</title>
    <link href="https://s701f.top/posts/%E5%9B%BA%E4%BD%93%E7%94%B5%E5%AD%90%E5%AD%A6%E5%85%AC%E5%BC%8F%E6%80%BB%E7%BB%93/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/%E5%9B%BA%E4%BD%93%E7%94%B5%E5%AD%90%E5%AD%A6%E5%85%AC%E5%BC%8F%E6%80%BB%E7%BB%93/</id>
    <published>2026-06-23T00:00:00.000Z</published>
    <updated>2026-06-23T00:00:00.000Z</updated>
    <summary>一些公式</summary>
    <content type="html"><![CDATA[<h1>固态电子学公式总结</h1>
<blockquote>
<p>本文件汇总固体电子学课程 (Lecture 5-20, Week 9-10) 的全部核心公式，按主题模块整理，便于复习查阅。</p>
</blockquote>
<hr />
<h1>一、晶体结构与量子力学基础</h1>
<h2>Bloch 定理</h2>
<p>晶体中电子波函数：</p>
<p>$\psi(x) = u(x) \cdot e^{ikx}$</p>
<p>其中 $u(x)$ 为单胞波函数，与势函数 $U(x)$ 具有相同周期性：$u(x + d) = u(x)$</p>
<h2>晶胞原子数</h2>
<table>
<thead>
<tr>
<th>晶格类型</th>
<th>原子数</th>
<th>计算方法</th>
</tr>
</thead>
<tbody>
<tr>
<td>简单立方 (SC)</td>
<td>1</td>
<td>$8 \times 1/8 = 1$</td>
</tr>
<tr>
<td>体心立方 (BCC)</td>
<td>2</td>
<td>$8 \times 1/8 + 1 = 2$</td>
</tr>
<tr>
<td>面心立方 (FCC)</td>
<td>4</td>
<td>$8 \times 1/8 + 6 \times 1/2 = 4$</td>
</tr>
<tr>
<td>金刚石结构</td>
<td>8</td>
<td>两个 FCC 子晶格叠加</td>
</tr>
</tbody>
</table>
<h2>Miller 指数晶面间距</h2>
<p>$d_{hkl} = a / \sqrt{h^2 + k^2 + l^2}$</p>
<hr />
<h1>二、电导率与载流子输运</h1>
<h2>电导率</h2>
<p>$\sigma = n e \mu_n + p e \mu_p$</p>
<h2>漂移电流密度</h2>
<p>$J_{\text{drift}} = \sigma E = (n e \mu_n + p e \mu_p) E$</p>
<h2>扩散电流密度</h2>
<p>$J_n = e D_n \frac{dn}{dx}$（电子扩散电流）</p>
<p>$J_p = -e D_p \frac{dp}{dx}$（空穴扩散电流）</p>
<h2>Einstein 关系</h2>
<p>$\frac{D}{\mu} = \frac{kT}{e}$</p>
<p>300 K 下：$D/\mu = 0.0259$ V</p>
<h2>漂移速度</h2>
<p>$v_d = \mu E$</p>
<hr />
<h1>三、半导体载流子统计</h1>
<h2>本征载流子浓度</h2>
<p>$n_i^2 = N_c N_v \exp\left(-\frac{E_g}{kT}\right)$</p>
<h2>本征 Fermi 能级</h2>
<p>$E_i = \frac{E_c + E_v}{2} + \frac{kT}{2} \ln\left(\frac{N_v}{N_c}\right)$</p>
<h2>质量作用定律</h2>
<p>$n p = n_i^2$（热平衡下）</p>
<h2>电中性条件</h2>
<p>$n + N_A^- = p + N_D^+$</p>
<p>N 型（$N_D \gg N_A$）：$n \approx N_D$</p>
<p>P 型（$N_A \gg N_D$）：$p \approx N_A$</p>
<h2>Fermi 能级位置</h2>
<p>N 型：$E_F - E_i = kT \ln(N_D / n_i)$</p>
<p>P 型：$E_i - E_F = kT \ln(N_A / n_i)$</p>
<hr />
<h1>四、PN 结</h1>
<h2>接触势（内建电势）</h2>
<p>$V_{bi} = \frac{kT}{e} \ln\left(\frac{N_A N_D}{n_i^2}\right)$</p>
<p>$V_{bi} = \frac{1}{e}(E_{Fn} - E_{Fp})$</p>
<h2>耗尽层宽度</h2>
<p>$W = \sqrt{\frac{2\varepsilon_s}{e}\left(\frac{1}{N_A} + \frac{1}{N_D}\right)(V_{bi} - V)}$</p>
<p>单边突变结（$N_A \gg N_D$）：$W \approx \sqrt{\frac{2\varepsilon_s}{e N_D}(V_{bi} - V)}$，$W \approx x_n$</p>
<h2>电场分布</h2>
<p>P 侧：$E(x) = -\frac{qN_A}{\varepsilon_s}(x + x_p)$</p>
<p>N 侧：$E(x) = -\frac{qN_D}{\varepsilon_s}(x_n - x)$</p>
<h2>最大电场</h2>
<p>$|E_{\max}| = \sqrt{\frac{2e N_A N_D}{\varepsilon_s(N_A + N_D)}(V_{bi} - V)}$</p>
<h2>耗尽层电容（势垒电容）</h2>
<p>$C_j = A \sqrt{\frac{e \varepsilon_s N_A N_D}{2(N_A + N_D)(V_{bi} - V)}}$</p>
<h2>扩散电容</h2>
<p>$C_{\text{diff}} \propto I_F \tau$</p>
<h2>I-V 特性</h2>
<p>$J = J_s (e^{eV/kT} - 1)$</p>
<p>$J_s = e\left(\frac{D_p n_i^2}{L_p N_D} + \frac{D_n n_i^2}{L_n N_A}\right)$</p>
<h2>击穿电压</h2>
<p>$V_{BR} \propto E_g^{3/2} N_D^{-3/4}$</p>
<hr />
<h1>五、MOSFET</h1>
<h2>阈值电压</h2>
<p>$V_t = V_{FB} + 2\phi_F + \frac{Q_B}{C_{ox}}$</p>
<p>$\phi_F = \frac{kT}{e} \ln\left(\frac{N_A}{n_i}\right)$，$C_{ox} = \frac{\varepsilon_{ox}}{t_{ox}}$</p>
<h2>DC 工作区</h2>
<table>
<thead>
<tr>
<th>工作区</th>
<th>条件</th>
<th>$I_D$</th>
</tr>
</thead>
<tbody>
<tr>
<td>截止区</td>
<td>$V_{GS} &lt; V_t$</td>
<td>$0$</td>
</tr>
<tr>
<td>线性区</td>
<td>$V_{GS} &gt; V_t$, $V_{DS} &lt; V_{GS} - V_t$</td>
<td>$\mu_n C_{ox} \frac{W}{L}[(V_{GS} - V_t)V_{DS} - \frac{1}{2}V_{DS}^2]$</td>
</tr>
<tr>
<td>饱和区</td>
<td>$V_{GS} &gt; V_t$, $V_{DS} \ge V_{GS} - V_t$</td>
<td>$\frac{1}{2}\mu_n C_{ox} \frac{W}{L}(V_{GS} - V_t)^2$</td>
</tr>
</tbody>
</table>
<h2>沟道长度调制</h2>
<p>$I_D = \frac{1}{2}\mu_n C_{ox} \frac{W}{L}(V_{GS} - V_t)^2(1 + \lambda V_{DS})$</p>
<h2>跨导</h2>
<p>$g_m = \frac{\partial I_D}{\partial V_{GS}}$</p>
<p>饱和区：$g_m = \mu_n C_{ox} \frac{W}{L}(V_{GS} - V_t)$</p>
<h2>衬底偏置效应</h2>
<p>$V_t = V_{t0} + \gamma(\sqrt{2\phi_F + V_{SB}} - \sqrt{2\phi_F})$</p>
<p>$\gamma = \frac{\sqrt{2e \varepsilon_s N_A}}{C_{ox}}$</p>
<h2>CMOS 对称条件</h2>
<p>$\mu_n (W/L)_N = \mu_p (W/L)_P$</p>
<hr />
<h1>六、BJT</h1>
<h2>电流关系</h2>
<p>$I_E = I_C + I_B$</p>
<p>$\beta = I_C / I_B$，$\alpha = I_C / I_E$</p>
<p>$\beta = \frac{\alpha}{1 - \alpha}$，$\alpha = \frac{\beta}{1 + \beta}$</p>
<h2>集电极电流（正向有源）</h2>
<p>$I_C = I_S e^{V_{BE} / V_T}$，$V_T = kT/e \approx 0.0259$ V</p>
<h2>Ebers-Moll 模型</h2>
<p>$I_C = \alpha_F I_E + I_{C0}$</p>
<p>$I_E = I_{ES}(e^{V_{BE}/V_T} - 1) - \alpha_R I_{CS}(e^{V_{BC}/V_T} - 1)$</p>
<p>$I_C = \alpha_F I_{ES}(e^{V_{BE}/V_T} - 1) - I_{CS}(e^{V_{BC}/V_T} - 1)$</p>
<h2>Early 效应</h2>
<p>$I_C = I_S e^{V_{BE}/V_T} \left(1 + \frac{V_{CE}}{V_A}\right)$</p>
<hr />
<h1>七、连续性方程</h1>
<p>$\frac{\partial n}{\partial t} = \frac{1}{e}\frac{\partial J_n}{\partial x} + G_n - R_n$</p>
<p>$\frac{\partial p}{\partial t} = -\frac{1}{e}\frac{\partial J_p}{\partial x} + G_p - R_p$</p>
<h2>复合寿命</h2>
<p>$\Delta n(t) = \Delta n(0) e^{-t/\tau}$</p>
<h2>扩散长度</h2>
<p>$L_n = \sqrt{D_n \tau_n}$，$L_p = \sqrt{D_p \tau_p}$</p>
<hr />
<h1>八、常用物理常数 (300 K)</h1>
<table>
<thead>
<tr>
<th>常数</th>
<th>符号</th>
<th>数值</th>
</tr>
</thead>
<tbody>
<tr>
<td>电子电荷</td>
<td>$e$</td>
<td>$1.602 \times 10^{-19}$ C</td>
</tr>
<tr>
<td>Boltzmann 常数</td>
<td>$k$</td>
<td>$1.381 \times 10^{-23}$ J/K</td>
</tr>
<tr>
<td>热电压</td>
<td>$kT/e$</td>
<td>$0.0259$ V</td>
</tr>
<tr>
<td>真空介电常数</td>
<td>$\varepsilon_0$</td>
<td>$8.854 \times 10^{-14}$ F/cm</td>
</tr>
<tr>
<td>硅相对介电常数</td>
<td>$\varepsilon_r$</td>
<td>$11.7$</td>
</tr>
<tr>
<td>硅介电常数</td>
<td>$\varepsilon_s$</td>
<td>$1.036 \times 10^{-12}$ F/cm</td>
</tr>
<tr>
<td>二氧化硅介电常数</td>
<td>$\varepsilon_{ox}$</td>
<td>$3.9$</td>
</tr>
<tr>
<td>硅禁带宽度</td>
<td>$E_g$</td>
<td>$1.12$ eV</td>
</tr>
<tr>
<td>硅本征载流子浓度</td>
<td>$n_i$</td>
<td>$1.5 \times 10^{10}$ cm$^{-3}$</td>
</tr>
</tbody>
</table>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="电子信息工程"></category>
  </entry>
  <entry>
    <title>固体电子学样卷解析</title>
    <link href="https://s701f.top/posts/%E5%9B%BA%E4%BD%93%E7%94%B5%E5%AD%90%E5%AD%A6%E6%A0%B7%E5%8D%B7%E8%A7%A3%E6%9E%90/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/%E5%9B%BA%E4%BD%93%E7%94%B5%E5%AD%90%E5%AD%A6%E6%A0%B7%E5%8D%B7%E8%A7%A3%E6%9E%90/</id>
    <published>2026-06-18T00:00:00.000Z</published>
    <updated>2026-06-18T00:00:00.000Z</updated>
    <summary>下周考试，所以让AI写了个样卷解析www</summary>
    <content type="html"><![CDATA[<h1>Solid State Electronics 期末试卷详细解析</h1>
<blockquote>
<p><strong>EEEN2004J Solid State Electronics — Semester 2 Final Examination</strong>
科目协调人：Xiulong Bao | 考试时间：120 分钟</p>
<p>本文档为样卷完整解析，包含每道题的详细计算步骤、公式推导和概念解释。</p>
</blockquote>
<hr />
<h2>目录</h2>
<ul>
<li><a href="#question-1">Question 1</a> — Q1A &amp; Q1B</li>
<li><a href="#question-2">Question 2</a>
<ul>
<li><a href="#q2a-%E7%AA%81%E5%8F%98-np-%E7%BB%93">Q2A. 突变 N+P 结</a></li>
<li><a href="#q2b-%E5%8D%95%E8%BE%B9-pn-%E7%BB%93">Q2B. 单边 P+N 结</a></li>
</ul>
</li>
<li><a href="#question-3">Question 3</a>
<ul>
<li><a href="#q3a-%E7%90%86%E6%83%B3-mos-%E7%94%B5%E5%AE%B9%E5%99%A8%E8%83%BD%E5%B8%A6%E5%9B%BE">Q3A. MOS 能带图</a></li>
<li><a href="#q3b-mosfet-%E5%8F%82%E6%95%B0%E8%AE%A1%E7%AE%97">Q3B. MOSFET 参数</a></li>
</ul>
</li>
<li><a href="#question-4">Question 4</a>
<ul>
<li><a href="#q4a-pnp-bjt-%E8%83%BD%E5%B8%A6%E5%9B%BE">Q4A. PNP BJT 能带图</a></li>
<li><a href="#q4b-early-%E6%95%88%E5%BA%94">Q4B. Early 效应</a></li>
</ul>
</li>
<li><a href="#%E9%99%84%E5%BD%95%E6%89%80%E7%94%A8%E5%B8%B8%E6%95%B0%E4%B8%8E%E5%85%AC%E5%BC%8F%E7%B4%A2%E5%BC%95">附录</a></li>
</ul>
<hr />
<h2>Question 1 (15%) — 半导体基础</h2>
<h3>Q1A. Si 掺杂硼（p 型）</h3>
<p>硼是第三族元素，掺入 Si 中充当<strong>受主</strong>（acceptor）。掺杂浓度 $N_A = 2 \times 10^{17},\text{cm}^{-3}$。</p>
<p><strong>1. 空穴浓度（多数载流子）</strong></p>
<p>$$p \approx N_A = \mathbf{2.000 \times 10^{17}},\text{cm}^{-3}$$</p>
<p>室温下杂质完全电离，空穴浓度近似等于受主浓度。</p>
<p><strong>2. 电子浓度（少数载流子）</strong></p>
<p>$$n = \frac{n_i^2}{p} = \frac{(1.5\times10^{10})^2}{2\times10^{17}} = \mathbf{1.125 \times 10^{3}},\text{cm}^{-3}$$</p>
<p>利用质量作用定律 $n \cdot p = n_i^2$。</p>
<p><strong>3. 费米能级位置</strong></p>
<p>相对本征费米能级 $E_i$：</p>
<p>$$E_f - E_i = -kT \ln\frac{N_A}{n_i} = -0.026 \times \ln\frac{2\times10^{17}}{1.5\times10^{10}} = -0.026 \times 16.405 = -\mathbf{0.4266},\text{eV}$$</p>
<p>$E_f$ 在 $E_i$ <strong>下方 0.4266 eV</strong>，靠近价带顶 $E_v$，确认是 p 型半导体。</p>
<p><strong>4. 简化能带图</strong></p>
<pre><code>    Ec ────────────────
                       |  Eg = 1.1 eV
    Ei ────────────────   (Ef 上方 0.4266 eV)
    Ef ─*──────────────
    Ev ────────────────
</code></pre>
<ul>
<li>$E_c - E_f = 0.55 + 0.4266 = 0.9766,\text{eV}$</li>
<li>$E_f - E_v = 0.55 - 0.4266 = 0.1234,\text{eV}$</li>
</ul>
<blockquote>
<p>$E_f$ 靠近 $E_v$ 而远离 $E_c$，表明空穴是多数载流子。</p>
</blockquote>
<hr />
<h3>Q1B. 掺杂 Si 棒的电阻</h3>
<p><strong>已知参数</strong>：</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>数值</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>长度 $L$</td>
<td>$1,\text{mm} = 0.1,\text{cm}$</td>
<td></td>
</tr>
<tr>
<td>截面积 $A$</td>
<td>$10,\mu\text{m}^2 = 10^{-7},\text{cm}^2$</td>
<td></td>
</tr>
<tr>
<td>磷掺杂 $N_D$</td>
<td>$3\times10^{17},\text{cm}^{-3}$</td>
<td>施主，n 型</td>
</tr>
<tr>
<td>$\mu_n$</td>
<td>$700,\text{cm}^2/(\text{V}\cdot\text{s})$</td>
<td>题中给定</td>
</tr>
</tbody>
</table>
<p><strong>1. 电导率</strong></p>
<p>n 型半导体中 $n \approx N_D$：</p>
<p>$$\sigma = q\mu_n n \approx q\mu_n N_D = 1.6\times10^{-19} \times 700 \times 3\times10^{17} = \mathbf{33.6},(\Omega\cdot\text{cm})^{-1}$$</p>
<p><strong>2. 电阻率</strong></p>
<p>$$\rho = \frac{1}{\sigma} = \mathbf{0.02976},\Omega\cdot\text{cm}$$</p>
<p><strong>3. 电阻</strong></p>
<p>$$R = \rho\frac{L}{A} = 0.02976 \times \frac{0.1}{10^{-7}} = \mathbf{2.976 \times 10^{4}},\Omega = \mathbf{29.76},\text{k}\Omega$$</p>
<hr />
<h2>Question 2 (35%) — PN 结</h2>
<h3>Q2A. 突变 N+P 结</h3>
<p><strong>已知参数</strong>：$N_A = 10^{15},\text{cm}^{-3}$（P 侧），$N_D = 10^{17},\text{cm}^{-3}$（N+ 侧），$T = 300,\text{K}$</p>
<p><strong>1. 接触电势（内建电势）$V_0$</strong></p>
<p>$$V_0 = \frac{kT}{q}\ln\frac{N_A N_D}{n_i^2} = 0.026 \times \ln\frac{10^{15}\times10^{17}}{(1.5\times10^{10})^2}$$</p>
<p>$$\frac{N_A N_D}{n_i^2} = \frac{10^{32}}{2.25\times10^{20}} = 4.444\times10^{11}$$</p>
<p>$$V_0 = 0.026 \times \ln(4.444\times10^{11}) = 0.026 \times 26.82 = \mathbf{0.6973},\text{V}$$</p>
<p><strong>2. 热平衡下耗尽层宽度 $W$</strong></p>
<p>$$\varepsilon_s = \varepsilon_{r,Si},\varepsilon_0 = 11.8 \times 8.85\times10^{-14} = 1.0443\times10^{-12},\text{F/cm}$$</p>
<p>$$\frac{1}{N_A}+\frac{1}{N_D} = 10^{-15} + 10^{-17} = 1.01\times10^{-15},\text{cm}^3$$</p>
<p>$$
\begin{aligned}
W &amp;= \sqrt{\frac{2\varepsilon_s V_0}{q}!\left(\frac{1}{N_A}+\frac{1}{N_D}\right)} \
&amp;= \sqrt{9.099\times10^{6} \times 1.01\times10^{-15}} \
&amp;= \sqrt{9.19\times10^{-9}} \
&amp;= \mathbf{9.59\times10^{-5}},\text{cm} \
&amp;= \mathbf{0.96},\mu\text{m}
\end{aligned}
$$</p>
<p>由于 $N_D \gg N_A$，耗尽区主要向P 侧扩展：</p>
<ul>
<li>P 侧：$x_p \approx 0.96,\mu\text{m}$</li>
<li>N+ 侧：$x_n \approx 0.0095,\mu\text{m} \ll x_p$</li>
</ul>
<p><strong>3. 零偏耗尽电容（$5\mu\text{m}\times5\mu\text{m}$ 结面积）</strong></p>
<p>$A = 25,\mu\text{m}^2 = 2.5\times10^{-7},\text{cm}^2$</p>
<p>$$
\begin{aligned}
C_{j0} &amp;= \frac{\varepsilon_s A}{W} = \frac{1.0443\times10^{-12}\times2.5\times10^{-7}}{9.59\times10^{-5}} \
&amp;= \mathbf{2.72\times10^{-15}},\text{F} \
&amp;= \mathbf{2.72},\text{fF}
\end{aligned}
$$</p>
<p><strong>4. 平衡能带图</strong></p>
<pre><code>P-side (NA = 1e15)            N-side (ND = 1e17)
       Ec                                Ec
   ──────────\                      /──────────
              \                    /
               \__________________/
   ──────────  /                  \  ──────────
       Ef     /                    \     Ef
   ──────────/                      \──────────
       Ev                                Ev

        &lt;──── 耗尽区 ────&gt;
        |&lt;── W = 0.96 μm ──&gt;|
            qV0 = 0.6973 eV
</code></pre>
<h3>Q2B. 单边 P+N 结</h3>
<p><strong>已知参数</strong>：$A = 10^{-4},\text{cm}^2$，N 侧 $N_D = 10^{15},\text{cm}^{-3}$，$\tau_p = 0.4,\mu\text{s}$</p>
<blockquote>
<p>对于 P+N 结，N 侧轻掺杂，电流由 N 侧少数载流子（空穴）的扩散主导。</p>
</blockquote>
<p><strong>1. 反向饱和电流 $I_s$</strong></p>
<p>公式：$I = qA(D_p p_{n0}/L_p)(e^{qV/kT}-1)$</p>
<p>$$p_{n0} = \frac{n_i^2}{N_D} = \frac{(1.5\times10^{10})^2}{10^{15}} = 2.25\times10^{5},\text{cm}^{-3}$$</p>
<p>$$D_p = \frac{kT}{q}\mu_p = 0.026 \times 450 = 11.7,\text{cm}^2/\text{s}$$</p>
<p>$$L_p = \sqrt{D_p\tau_p} = \sqrt{11.7 \times 0.4\times10^{-6}} = 2.163\times10^{-3},\text{cm}$$</p>
<p>$$
\begin{aligned}
I_s &amp;= qA\frac{D_p p_{n0}}{L_p} \
&amp;= 1.6\times10^{-19} \times 10^{-4} \times \frac{11.7 \times 2.25\times10^{5}}{2.163\times10^{-3}} \
&amp;= \mathbf{1.95\times10^{-14}},\text{A} = \mathbf{0.02},\text{pA}
\end{aligned}
$$</p>
<p><strong>2. 正向偏压 $V = 0.4,\text{V}$</strong></p>
<p>$$\frac{qV}{kT} = \frac{0.4}{0.026} = 15.385,\quad e^{15.385}=4.81\times10^{6}$$</p>
<p>$$
\begin{aligned}
I_f &amp;= I_s(e^{qV/kT}-1) = 1.95\times10^{-14} \times 4.81\times10^{6} \
&amp;= \mathbf{9.35\times10^{-8}},\text{A} = \mathbf{93.5},\text{nA}
\end{aligned}
$$</p>
<p><strong>3. 反向偏压 $V = -4,\text{V}$</strong></p>
<p>$$\frac{qV}{kT} = \frac{-4}{0.026} = -153.85,\quad e^{-153.85} \approx 0$$</p>
<p>$$I_r = I_s(e^{-153.85}-1) \approx -I_s = \mathbf{-1.95\times10^{-14}},\text{A}$$</p>
<blockquote>
<p>在 $4,\text{V}$ 反向偏压下，电流已完全饱和，值为 $-I_s$。</p>
</blockquote>
<hr />
<h2>Question 3 (35%) — MOS 电容器与 MOSFET</h2>
<h3>Q3A. 理想 MOS 电容器能带图</h3>
<p>条件：p 型硅衬底，$V_{GS} &lt; 0$（栅极接负压）</p>
<pre><code>金属        氧化层           p 型硅
───       ─────────        ──────────────
  E_Fm ──────────
                            Ec ────────────
                        \  Ei ────────────  界面处
                         \ Ef ────────────  向上弯曲
                          \Ev ────────────
</code></pre>
<p>$V_{GS} &lt; 0$ 时 MOS 结构工作在<strong>积累模式</strong>：</p>
<ol>
<li>栅极负电压吸引空穴（p-Si 多数载流子）到 Si/SiO2 界面</li>
<li>能带在界面处<strong>向上弯曲</strong></li>
<li>界面空穴浓度 &gt; 体内空穴浓度（积累）</li>
<li>无耗尽层——积累的空穴层完全屏蔽电场</li>
</ol>
<hr />
<h3>Q3B. MOSFET 参数计算</h3>
<p><strong>已知参数</strong>：$N_A = 2\times10^{15},\text{cm}^{-3}$，$t_{ox}=12,\text{nm}$，$\varepsilon_{r,s}=11.8$，$\varepsilon_{r,ox}=3.9$，$W/L=90$，$\mu_n=1100,\text{cm}^2/(\text{V}\cdot\text{s})$</p>
<p><strong>1. 总能带弯曲与阈值电压</strong></p>
<p>费米势：</p>
<p>$$
\begin{aligned}
\phi_F &amp;= \frac{kT}{q}\ln\frac{N_A}{n_i} = 0.026 \times \ln\frac{2\times10^{15}}{1.5\times10^{10}} \
&amp;= 0.026 \times 11.80 = \mathbf{0.3068},\text{V}
\end{aligned}
$$</p>
<p>强反型起始：$\psi_s = 2\phi_F = \mathbf{0.6136},\text{V}$</p>
<p>最大耗尽宽度：</p>
<p>$$
\begin{aligned}
W_{dep,\max} &amp;= \sqrt{\frac{4\varepsilon_s\phi_F}{qN_A}} \
&amp;= \sqrt{\frac{4\times1.0443\times10^{-12}\times0.3068}{1.6\times10^{-19}\times2\times10^{15}}} \
&amp;= \sqrt{4.005\times10^{-9}} \
&amp;= \mathbf{6.33\times10^{-5}},\text{cm} = \mathbf{0.633},\mu\text{m}
\end{aligned}
$$</p>
<p>最大耗尽电荷密度：</p>
<p>$$
\begin{aligned}
Q_{dep,\max} &amp;= qN_A W_{dep,\max} = 1.6\times10^{-19}\times2\times10^{15}\times6.33\times10^{-5} \
&amp;= \mathbf{2.03\times10^{-8}},\text{C/cm}^2
\end{aligned}
$$</p>
<p>单位面积氧化层电容：</p>
<p>$$
\begin{aligned}
C_{ox} &amp;= \frac{\varepsilon_{ox}}{t_{ox}} = \frac{3.9\times8.85\times10^{-14}}{12\times10^{-7}} \
&amp;= \mathbf{2.876\times10^{-7}},\text{F/cm}^2
\end{aligned}
$$</p>
<p>氧化层压降：</p>
<p>$$V_{ox} = \frac{Q_{dep,\max}}{C_{ox}} = \frac{2.03\times10^{-8}}{2.876\times10^{-7}} = \mathbf{0.0704},\text{V}$$</p>
<p>阈值电压（理想 MOS，$V_{FB}=0$）：</p>
<p>$$V_T = V_{FB} + 2\phi_F + V_{ox} = 0 + 0.6136 + 0.0704 = \mathbf{0.684},\text{V}$$</p>
<p><strong>2. 工作模式与漏极电流</strong></p>
<p>给定 $V_G=2\text{V}$，$V_D=0.5\text{V}$：</p>
<table>
<thead>
<tr>
<th>比较项</th>
<th>数值</th>
</tr>
</thead>
<tbody>
<tr>
<td>$V_{GS}$</td>
<td>$2.0,\text{V}$</td>
</tr>
<tr>
<td>$V_{DS}$</td>
<td>$0.5,\text{V}$</td>
</tr>
<tr>
<td>$V_{GS}-V_T$</td>
<td>$2.0 - 0.684 = 1.316,\text{V}$</td>
</tr>
<tr>
<td>比较</td>
<td>$0.5 &lt; 1.316$</td>
</tr>
</tbody>
</table>
<p>$\because V_{DS}=0.5,\text{V} &lt; V_{GS}-V_T=1.316,\text{V}$</p>
<p>$\therefore$ <strong>MOSFET 工作在三极管（线性）区</strong>。</p>
<p>漏极电流：</p>
<p>$$I_D = \mu_n C_{ox}\frac{W}{L}!\left[(V_{GS}-V_T)V_{DS}-\frac{V_{DS}^2}{2}\right]$$</p>
<p>$$= 1100 \times 2.876\times10^{-7} \times 90 \times \left[1.316\times0.5 - \frac{0.5^2}{2}\right]$$</p>
<p>$$= 1100 \times 2.876\times10^{-7} \times 90 \times 0.533$$</p>
<p>$$= \mathbf{1.52\times10^{-2}},\text{A} = \mathbf{15.2},\text{mA}$$</p>
<blockquote>
<p><strong>关于 $\lambda$ 的说明</strong>：沟道长度调制参数 $\lambda = 0.1,\text{V}^{-1}$ 通常仅在饱和区起作用，三极管区一般不适用。</p>
</blockquote>
<hr />
<h2>Question 4 (15%) — BJT</h2>
<h3>Q4A. PNP BJT 能带图</h3>
<p><strong>1. 热平衡态</strong></p>
<pre><code>  发射极 (P+)         基极 (N)       集电极 (P)
  ────────────    ────────────    ────────────
      Ec              Ec              Ec
  ────────────    ────\  /────    ────────────
      Ei              Ei \/           Ei
  ────────────    ────/\────    ────────────
  Ef ────────────  Ef ─────────   Ef ─────────
      Ev              Ev              Ev
  ────────────    ────/  \────    ────────────
</code></pre>
<ul>
<li>热平衡下 $E_f$ 处处相等</li>
<li>E-B 结和 B-C 结均存在内建势垒和耗尽区</li>
</ul>
<p><strong>2. 正向有源模式</strong>（$V_{EB}&gt;0$，$V_{CB}&lt;0$）</p>
<pre><code>  发射极 (P+)         基极 (N)         集电极 (P)

  Ec ─────────    Ec ──/\──────    Ec ───────\───
  Ei ─────────    Ei ──────────    Ei ────────────
  Ef ────────     Ef ──────────    Ef ────────────
  Ev ─────────    Ev ──\/──────    Ev ───────/───

    E-B 正偏         窄基区扩散        B-C 反偏
    势垒降低         空穴扩散输运      势垒升高
    空穴注入                            空穴被扫入
</code></pre>
<p><strong>工作机理</strong>：</p>
<table>
<thead>
<tr>
<th>步骤</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>① 发射</td>
<td>$V_{EB}&gt;0$ 使 E-B 结正偏，势垒降低，空穴从发射极注入基区</td>
</tr>
<tr>
<td>② 扩散</td>
<td>空穴在窄基区中靠浓度梯度扩散</td>
</tr>
<tr>
<td>③ 收集</td>
<td>$V_{CB}&lt;0$ 使 B-C 结反偏，空穴被强电场扫入集电极</td>
</tr>
<tr>
<td>④ 电流</td>
<td>$I_E = I_B + I_C$</td>
</tr>
</tbody>
</table>
<hr />
<h3>Q4B. Early 效应</h3>
<p><strong>定义</strong>：Early 效应（基区宽度调制效应）指 BJT 在正向有源区工作时，$I_C$ 随 $V_{CE}$ 增大而增大的现象。</p>
<p><strong>物理机制</strong>：</p>
<pre><code>  N+ 发射极         P 基区          N- 集电极
 ┌─────────────┬──────────────┬──────────────────┐
 │             │  ##########  │                  │
 │             │  ##########  │  B-C 耗尽区     │
 │             │  有效基区    │  ←─────────→     │
 │  注入电子   │  宽度 W_B    │  随 V_CB ↑ 展宽 │
 └─────────────┴──────────────┴──────────────────┘
                   ↑ W_B ↓ 当 V_CB ↑
</code></pre>
<ol>
<li>正向有源模式，B-C 结反偏</li>
<li>增大 $V_{CB}$（即 $V_{CE}$）→ B-C 结耗尽区<strong>加宽</strong></li>
<li>耗尽区向基区扩展 → 有效中性基区宽度 $W_B$ <strong>减小</strong></li>
<li>少子浓度梯度增大 → $I_C$ <strong>增大</strong></li>
</ol>
<p><strong>定量描述</strong>：</p>
<p>$$I_C = I_S e^{V_{BE}/V_T}\left(1 + \frac{V_{CE}}{V_A}\right)$$</p>
<p>其中 $V_A$ 为 <strong>Early 电压</strong>，典型值 50–100 V。</p>
<p><strong>后果</strong>：</p>
<table>
<thead>
<tr>
<th>后果</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>有限输出电阻</td>
<td>$r_o = \dfrac{V_A + V_{CE}}{I_C} \approx \dfrac{V_A}{I_C}$</td>
</tr>
<tr>
<td>$I_C$-$V_{CE}$ 特性斜率</td>
<td>输出特性曲线在饱和区有正斜率</td>
</tr>
<tr>
<td>基区越窄，效应越显著</td>
<td>现代 BJT 需考虑 Early 效应</td>
</tr>
</tbody>
</table>
<hr />
<h2>附录：所用常数与公式索引</h2>
<p><strong>物理常数</strong>（试卷末页提供）：</p>
<table>
<thead>
<tr>
<th>常数</th>
<th>符号</th>
<th>数值</th>
</tr>
</thead>
<tbody>
<tr>
<td>玻尔兹曼常数</td>
<td>$k$</td>
<td>$1.3807\times10^{-23},\text{J/K}$</td>
</tr>
<tr>
<td>电子电荷</td>
<td>$q$</td>
<td>$1.60\times10^{-19},\text{C}$</td>
</tr>
<tr>
<td>真空介电常数</td>
<td>$\varepsilon_0$</td>
<td>$8.85\times10^{-14},\text{F/cm}$</td>
</tr>
<tr>
<td>室温热电压</td>
<td>$kT/q$</td>
<td>$0.026,\text{V}$</td>
</tr>
<tr>
<td>Si 相对介电常数</td>
<td>$\varepsilon_{r,Si}$</td>
<td>11.8</td>
</tr>
<tr>
<td>Si 本征载流子浓度</td>
<td>$n_i$</td>
<td>$1.5\times10^{10},\text{cm}^{-3}$</td>
</tr>
<tr>
<td>Si 电子迁移率</td>
<td>$\mu_n$</td>
<td>$1300,\text{cm}^2/(\text{V}\cdot\text{s})$</td>
</tr>
<tr>
<td>Si 空穴迁移率</td>
<td>$\mu_p$</td>
<td>$450,\text{cm}^2/(\text{V}\cdot\text{s})$</td>
</tr>
</tbody>
</table>
<p><strong>核心公式索引</strong>：</p>
<table>
<thead>
<tr>
<th>公式</th>
<th>应用</th>
</tr>
</thead>
<tbody>
<tr>
<td>$p = N_A$，$n = n_i^2/p$</td>
<td>Q1A</td>
</tr>
<tr>
<td>$\sigma = q\mu n$，$R = \rho L/A$</td>
<td>Q1B</td>
</tr>
<tr>
<td>$V_0 = (kT/q)\ln(N_A N_D/n_i^2)$</td>
<td>Q2A(i)</td>
</tr>
<tr>
<td>$W = \sqrt{2\varepsilon_s V_0/q\cdot(1/N_A+1/N_D)}$</td>
<td>Q2A(ii)</td>
</tr>
<tr>
<td>$C_{j0} = \varepsilon_s A/W$</td>
<td>Q2A(iii)</td>
</tr>
<tr>
<td>$I_s = qA(D_p p_{n0}/L_p)$</td>
<td>Q2B(i)</td>
</tr>
<tr>
<td>$I = I_s(e^{qV/kT}-1)$</td>
<td>Q2B(ii)(iii)</td>
</tr>
<tr>
<td>$\phi_F = (kT/q)\ln(N_A/n_i)$，$V_T = 2\phi_F + Q_{dep,\max}/C_{ox}$</td>
<td>Q3B(i)</td>
</tr>
<tr>
<td>$I_D = \mu_n C_{ox}(W/L)[(V_{GS}-V_T)V_{DS}-V_{DS}^2/2]$（三极管区）</td>
<td>Q3B(ii)</td>
</tr>
<tr>
<td>$I_C = I_S e^{V_{BE}/V_T}(1+V_{CE}/V_A)$（Early 效应）</td>
<td>Q4B</td>
</tr>
</tbody>
</table>
<hr />
<blockquote>
<p><strong>制作说明</strong>：本文档基于样卷 PDF 自动提取题目内容后生成解析，所有数值计算均经过 Python 验算。
解析中的能带图均为 ASCII 示意，实际答题时需在答题纸上画出带有正确曲率和间距的能带图。</p>
</blockquote>
<hr />
<h1>Solid State Electronics 备考公式清单</h1>
<blockquote>
<p>试卷末页<strong>未提供</strong>但在答题中必须使用的公式，按题目编号排列。
试卷已给出的公式（接触势、阈值电压、PN结饱和电流）不再重复列出。</p>
</blockquote>
<hr />
<h2>目录</h2>
<ul>
<li><a href="#%E9%80%9A%E7%94%A8%E5%B8%B8%E6%95%B0%E4%B8%8E%E5%8D%95%E4%BD%8D%E6%8D%A2%E7%AE%97">通用常数与单位换算</a></li>
<li><a href="#q1-%E5%8D%8A%E5%AF%BC%E4%BD%93%E5%9F%BA%E7%A1%80">Q1 半导体基础</a></li>
<li><a href="#q2-pn%E7%BB%93">Q2 PN结</a></li>
<li><a href="#q3-mos-%E7%94%B5%E5%AE%B9%E5%99%A8%E4%B8%8Emosfet">Q3 MOS 电容器与MOSFET</a></li>
<li><a href="#q4-bjt">Q4 BJT</a></li>
</ul>
<hr />
<h2>通用常数与单位换算</h2>
<p>试卷给出的常数部分以 SI 制（m, F/m）为标准，但计算中常用 cm 制：</p>
<table>
<thead>
<tr>
<th>换算</th>
<th>公式</th>
</tr>
</thead>
<tbody>
<tr>
<td>长度</td>
<td>$\text{m} = 100\ \text{cm}$</td>
</tr>
<tr>
<td>介电常数</td>
<td>$\varepsilon_0 = 8.85\times10^{-12}\ \text{F/m} = 8.85\times10^{-14}\ \text{F/cm}$</td>
</tr>
<tr>
<td>面积</td>
<td>$\mu\text{m}^2 = 10^{-8}\ \text{cm}^2$</td>
</tr>
<tr>
<td>热电压</td>
<td>$\dfrac{kT}{q} = 0.026\ \text{V}$</td>
</tr>
</tbody>
</table>
<hr />
<h2>Q1 半导体基础</h2>
<h3>质量作用定律</h3>
<p>$n \cdot p = n_i^2$</p>
<h3>多子近似（完全电离，室温）</h3>
<ul>
<li>p 型： $\approx N_A$ $， = n_i^2 / N_A$</li>
<li>n 型： $\approx N_D$ $， = n_i^2 / N_D$</li>
</ul>
<h3>费米能级位置</h3>
<p>$E_f - E_i = \pm kT\ln\frac{N}{n_i}$</p>
<p>（$+$ 号对应 n 型，$-$ 号对应 p 型）</p>
<h3>电导率与电阻</h3>
<p>$\sigma = q(\mu_n n + \mu_p p) \approx q\mu_{\text{majority}} N_{\text{doping}}$</p>
<p>$\rho = \frac{1}{\sigma}$</p>
<p>$R = \rho\frac{L}{A}$</p>
<hr />
<h2>Q2 PN结</h2>
<h3>爱因斯坦关系</h3>
<p>$D_n = \frac{kT}{q}\mu_n,\qquad D_p = \frac{kT}{q}\mu_p$</p>
<h3>扩散长度</h3>
<p>$L_n = \sqrt{D_n\tau_n},\qquad L_p = \sqrt{D_p\tau_p}$</p>
<h3>少子平衡浓度（单边结）</h3>
<p>对于 P+N 结，N 侧少子（空穴）：${n0} = \dfrac{n_i^2}{N_D}$
对于 N+P 结，P 侧少子（电子）：${p0} = \dfrac{n_i^2}{N_A}$</p>
<h3>耗尽电容（平行板电容模型）</h3>
<p>$C_j = \frac{\varepsilon_s A}{W}$</p>
<p>其中 $\varepsilon_s = \varepsilon_{r,Si}\cdot\varepsilon_0$，$ 为耗尽层宽度。</p>
<h3>介电常数</h3>
<p>$\varepsilon_s = \varepsilon_{r,Si}\cdot\varepsilon_0 = 11.8 \times 8.85\times10^{-14} = 1.0443\times10^{-12}\ \text{F/cm}$</p>
<hr />
<h2>Q3 MOS 电容器与 MOSFET</h2>
<h3>费米势</h3>
<p>$\phi_F = \frac{kT}{q}\ln\frac{N_A}{n_i}$</p>
<h3>单位面积氧化层电容</h3>
<p>$C_{ox} = \frac{\varepsilon_{ox}}{t_{ox}} = \frac{\varepsilon_{r,ox}\varepsilon_0}{t_{ox}}$</p>
<p>SiO2 相对介电常数：$\varepsilon_{r,ox} = 3.9$</p>
<h3>耗尽层宽度（MOS 单边，p 衬底）</h3>
<p>$W_{dep} = \sqrt{\frac{2\varepsilon_s\psi_s}{qN_A}}$</p>
<p>其中 $\psi_s$ 为表面势。</p>
<h3>最大耗尽宽度（强反型起始点）</h3>
<p>$\psi_s(\text{th}) = 2\phi_F$</p>
<p>$W_{dep,\max} = \sqrt{\frac{4\varepsilon_s\phi_F}{qN_A}}$</p>
<h3>最大耗尽电荷密度</h3>
<p>$Q_{dep,\max} = qN_A W_{dep,\max}$</p>
<h3>阈值电压（完整形式，含平带电压）</h3>
<p>$V_T = V_{FB} + 2\phi_F + \frac{Q_{dep,\max}}{C_{ox}}$</p>
<p>理想 MOS：${FB}=0$</p>
<h3>漏极电流</h3>
<p><strong>三极管（线性）区</strong>（${DS} &lt; V_{GS} - V_T$）：</p>
<p>$I_D = \mu_n C_{ox}\frac{W}{L}\left[(V_{GS}-V_T)V_{DS} - \frac{V_{DS}^2}{2}\right]$</p>
<p><strong>饱和区</strong>（$V_{DS} \geq V_{GS} - V_T$）：
$I_D = \frac{1}{2}\mu_n C_{ox}\frac{W}{L}(V_{GS}-V_T)^2(1 + \lambda V_{DS})$</p>
<hr />
<h2>Q4 BJT</h2>
<h3>考虑 Early 效应的集电极电流</h3>
<p>$I_C = I_S e^{V_{BE}/V_T}\left(1 + \frac{V_{CE}}{V_A}\right)$</p>
<p>其中 $ 为 Early 电压（典型值 50–100 V）， = kT/q = 0.026\ \text{V}$。</p>
<h3>输出电阻</h3>
<p>$r_o = \frac{V_A + V_{CE}}{I_C} \approx \frac{V_A}{I_C}$</p>
<hr />
<h2>常见陷阱提示</h2>
<table>
<thead>
<tr>
<th>陷阱</th>
<th>正确做法</th>
</tr>
</thead>
<tbody>
<tr>
<td>$\varepsilon_0$ 直接用 F/m 代入</td>
<td>先换算为 F/cm: $\times 10^{-2}$</td>
</tr>
<tr>
<td>面积 $\mu\text{m}^2$ 不换算</td>
<td>\ \mu\text{m}^2 = 10^{-8}\ \text{cm}^2$</td>
</tr>
<tr>
<td>MOS 耗尽公式用了 PN 结双边公式</td>
<td>MOSFET 是单边耗尽，/N_D \to 0$</td>
</tr>
<tr>
<td>Early 参数 $\lambda$ 在三极管区也乘上去</td>
<td>$\lambda$ 只在饱和区有效</td>
</tr>
</tbody>
</table>
<hr />
<blockquote>
<p>整理自样卷解析中所有使用但试卷末页<strong>未提供</strong>的公式。</p>
</blockquote>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="电子信息工程"></category>
  </entry>
  <entry>
    <title>stm32学习记录1</title>
    <link href="https://s701f.top/posts/stm32%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%951/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/stm32%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%951/</id>
    <published>2026-06-11T00:00:00.000Z</published>
    <updated>2026-06-11T00:00:00.000Z</updated>
    <summary></summary>
    <content type="html"><![CDATA[<h1>前言</h1>
<p>反正以后做毕设的时候也要学，不如现在学...&lt;br&gt;
依然只记录老师不教的内容</p>
<h1>记录</h1>
<h2>在VSCode中烧录KeilV5以上版本的程序</h2>
<p>在扩展商店里面安装Keil Assistant扩展，然后在设置里面设置Keil路径即可</p>
<h2>选择GPIO的方式</h2>
<p>江协科技的视频里面，点亮板子自带的LED需要C13端口&lt;br&gt;
但很显然我贪便宜，买了正点原子的&lt;br&gt;
查阅资料表明，这块最小系统板的LED是A8端口&lt;br&gt;
<img src="%E6%AD%A3%E7%82%B9%E5%8E%9F%E5%AD%90LED.png" alt="LED位置" />
所以代码改成</p>
<pre><code>RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);// 开启总线时钟
GPIO_InitTypeDef GPIO_InitStructure;
GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;// 标准推挽输出（那么问题来了，什么是推挽输出）
GPIO_InitStructure.GPIO_Pin = GPIO_Pin_8;// 选择引脚8
GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;// 选择速度50MHz（为什么要选择50MHz）
GPIO_Init(GPIOA, &amp;GPIO_InitStructure);// 初始化GPIOA的引脚8
GPIO_SetBits(GPIOA, GPIO_Pin_8);// 点亮LED
//GPIO_ResetBits(GPIOA, GPIO_Pin_8); // 熄灭LED
</code></pre>
<h2>什么是推挽输出？</h2>
<p>必应了一下，看不懂&lt;br&gt;
本着不懂原理也能用的精神，这个问题转化为了“为什么要用推挽输出”</p>
<h2>为什么要用推挽输出？</h2>
<p>我们需要输出电流驱动LED负载，只有推挽输出能做到。开漏输出需要上拉电阻到VDD</p>
<h2>为什么选择速度50MHz？</h2>
<p>首先，GPIO_Speed说明了这个GPIO口信号最高翻转频率&lt;br&gt;
这个数值越高，信号的上升沿和下降沿时间就越短，波形更接近方波&lt;br&gt;
当然我们只是控制一个LED，2MHz完全够用&lt;br&gt;
那么就引出了下一个问题</p>
<h2>如何选择合适的速度？</h2>
<p>从波形角度来看，速度越高越好。&lt;br&gt;
但是高速信号有几个问题。首先是能耗高，其次是信号边沿可能发生振铃，造成误判（振铃是指上升沿过冲，比方说最大输出电压是3.3V，过冲的时候输出变为3.9V；以及下降沿下冲，比方说低电平本来是0，下冲的时候输出变为负值了）甚至损坏IO口或电路，最后是电磁干扰（EMI）高频信号可能会干扰其它电路&lt;br&gt;
所以对于普通输入输出或者长走线我们一般只需要2MHz或者10MHz&lt;br&gt;</p>
<h2>流水灯踩坑记录</h2>
<p>GPIO_Init那些东西太麻烦了，对于每一个引脚都要执行相同的操作，咱要把逻辑抽离出来，写成函数（智将）&lt;br&gt;
咱真是面向对象高手啊</p>
<pre><code>GPIO_InitTypeDef registPin(GPIOMode_TypeDef mode, GPIOSpeed_TypeDef speed, const char* pinName)
{
	GPIO_InitTypeDef GPIO_InitStructure;
	GPIO_InitStructure.GPIO_Mode = mode;
	GPIO_InitStructure.GPIO_Speed = speed;
	GPIO_InitStructure.GPIO_Pin = 1 &lt;&lt; atoi(pinName + 1);
	switch (pinName[0])
	{
		case 'A':
			RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);
			GPIO_Init(GPIOA, &amp;GPIO_InitStructure);
			break;
		case 'B':
			RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB, ENABLE);
			GPIO_Init(GPIOB, &amp;GPIO_InitStructure);
			break;
		case 'C':
			RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOC, ENABLE);
			GPIO_Init(GPIOC, &amp;GPIO_InitStructure);
			break;
		case 'D':
			RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOD, ENABLE);
			GPIO_Init(GPIOD, &amp;GPIO_InitStructure);
			break;
		case 'E':
			RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOE, ENABLE);
			GPIO_Init(GPIOE, &amp;GPIO_InitStructure);
			break;
		default:
			break;
	}

	return GPIO_InitStructure;
}
</code></pre>
<p>然后继续看视频&lt;br&gt;
诶我去怎么还能按位与的&lt;br&gt;</p>
<pre><code>GPIO_InitTypeDef GPIO_InitStructure;
RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);
GPIO_InitStructure.GPIO_Pin = GPIO_Pin_4 | GPIO_Pin_5 | GPIO_Pin_6 | GPIO_Pin_7;
GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;
GPIO_InitStructure.GPIO_Speed = GPIO_Speed_2MHz;
GPIO_Init(GPIOA, &amp;GPIO_InitStructure);
</code></pre>
<p>解决</p>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="电子信息工程"></category>
  </entry>
  <entry>
    <title>kruskal最小生成树</title>
    <link href="https://s701f.top/posts/kruskal%E6%9C%80%E5%B0%8F%E7%94%9F%E6%88%90%E6%A0%91/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/kruskal%E6%9C%80%E5%B0%8F%E7%94%9F%E6%88%90%E6%A0%91/</id>
    <published>2026-06-08T00:00:00.000Z</published>
    <updated>2026-06-08T00:00:00.000Z</updated>
    <summary>老师课上讲的是kruskal...我prim白学了</summary>
    <content type="html"><![CDATA[<h1>前言</h1>
<p>今天看ppt，才发现老师讲的是kruskal算法...</p>
<h1>介绍</h1>
<p>这是一种每次将分属不同树的最小边加入图的算法&lt;br&gt;
所以我们不难联想到并查集&lt;br&gt;</p>
<h1>实现</h1>
<h2>前置数据</h2>
<pre><code>struct edge
{
    int f, t, l;
};// 边，是的，我们甚至不需要链式前向星
struct cmp
{
    bool operator()(edge a, edge b)
    {
        return a.l &gt; b.l;
    }
};

std::priority_queue&lt;edge, std::vector&lt;edge&gt;, cmp&gt; pq;// 存储边的优先队列，队顶是最短的边

std::vector&lt;int&gt;disjoint_set;// 并查集
int find(int x)// 查询
{
    if(disjoint_set[x] != x)
        disjoint_set[x] = find(disjoint_set[x]);
    return disjoint_set[x];
}
void init_disjoint_set(int n)// 初始化并查集
{
    disjoint_set.resize(n);
    for(int i = 0; i &lt; n; i++)
        disjoint_set[i] = i;
}

</code></pre>
<h2>合并子树</h2>
<pre><code>while(!pq.empty())
{
    // std::clog&lt;&lt;"RUA!"&lt;&lt;std::endl;
    edge e = pq.top();// 获取最短边
    pq.pop();
    int f = find(e.f);
    int t = find(e.t);
    if(f == t)// 如果这条边属于同一个子最小生成树，那么不进行任何操作
        continue;
    if(f != t)
    {
        disjoint_set[f] = t;// 合并子树
        cnt += e.l;
    }
}
</code></pre>
<p>判断是否联通</p>
<pre><code>for(int i = 1; i &lt;= N; i++)
{
    if(find(i) != find(1))
    {
        std::cout &lt;&lt; "orz" &lt;&lt; std::endl;
        return 0;
    }
}
std::cout &lt;&lt; cnt &lt;&lt; std::endl;
</code></pre>
<p>没什么难点，就这么简单</p>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="算法"></category>
  </entry>
  <entry>
    <title>lua学习记录1</title>
    <link href="https://s701f.top/posts/lua%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%951/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/lua%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%951/</id>
    <published>2026-06-07T00:00:00.000Z</published>
    <updated>2026-06-07T00:00:00.000Z</updated>
    <summary>学习lua的第一天</summary>
    <content type="html"><![CDATA[<h1>前言</h1>
<p>因为一些神秘原因（指玩的游戏做mod需要lua），我决定把它学了&lt;br&gt;
这里不会记录一些语法之类的可以从网上找到的教程内容，只会记录那些教程不讲，咱自己摸索出的东西</p>
<h1>table中有nil会发生什么</h1>
<p>教程当中讲，对于一个lua表</p>
<pre><code>local array = {1, nil, 2}
print(#array)
</code></pre>
<p>输出结果应该是3&lt;br&gt;
但是咱在实操当中结果是2&lt;br&gt;
AI的回答是在lua表中添加nil会导致#运算符和ipair的结果不可预测&lt;br&gt;
lua把#table定义为table的第一个border（t[i]返回nil）&lt;br&gt;
但是问题在于，对于我们给出的array，t[2]（赋值为nil）和t[4]（超出表边界，不存在）的返回值都是nil&lt;br&gt;
所以#array在寻找border时，既可能找到2，也可能找到4&lt;br&gt;
比如对于下面这行代码&lt;br&gt;</p>
<pre><code>local array = {5, nil, "红酱自动机", 6, 7, 8}
print(#array)
</code></pre>
<p>输出结果就是6而不是2&lt;br&gt;
所以实践当中最好不要在lua表里面加nil</p>
<h1>pair, ipair的区别以及pair的一些特性</h1>
<p>对于下面这段代码</p>
<pre><code>do
    local table = {
        [false] = "false",
        [true] = "true",
        [1] = "one",
        [2] = "two",
        [3] = "three",
        name = "张三",
        age = 18,
        friend = {
            name = "李四",
            age = 20
        }
    }
    for k, v in pairs(table) do
        print(k, v)
    end
    print("-------------")
    for k, v in ipairs(table) do
        print(k, v)
    end
    print("-------------")
    print(#table) -- 3
end
</code></pre>
<p>其输出为</p>
<pre><code>false   false
true    true
2       two
3       three
name    张三
friend  table: 0000000000fba550
age     18
1       one
-------------
1       one
2       two
3       three
-------------
3
</code></pre>
<p>lua的table被分为数组区和哈希区&lt;br&gt;
ipair只遍历数组区，而pair把数组区和哈希区一起遍历&lt;br&gt;
我们可以看到</p>
<pre><code>[1] = "one",
[2] = "two",
[3] = "three",
</code></pre>
<p>这些是被存在数组区的&lt;br&gt;
那我们不禁要问了</p>
<pre><code>int luaH_next (lua_State *L, Table *t, StkId key) {
  unsigned int asize = t-&gt;asize;
  unsigned int i = findindex(L, t, s2v(key), asize);  /* find original key */
  for (; i &lt; asize; i++) {  /* try first array part（数组区遍历） */
    lu_byte tag = *getArrTag(t, i);
    if (!tagisempty(tag)) {  /* a non-empty entry? */
      setivalue(s2v(key), cast_int(i) + 1);
      farr2val(t, i, tag, s2v(key + 1));
      return 1;
    }
  }
  for (i -= asize; i &lt; sizenode(t); i++) {  /* hash part（哈希区遍历） */
    if (!isempty(gval(gnode(t, i)))) {  /* a non-empty entry? */
      Node *n = gnode(t, i);
      getnodekey(L, s2v(key), n);
      setobj2s(L, key + 1, gval(n));
      return 1;
    }
  }
  return 0;  /* no more elements */
}
</code></pre>
<p>源码里面写的是先顺序遍历数组区，再顺序遍历哈希区啊，怎么用pair进行遍历的时候，数组区的元素也没有按顺序输出呢？&lt;br&gt;
答案是数组区是一个会随着插入元素数量变化的变长数组，而pair不保证后加入的数据一定在数组区</p>
<pre><code>/*
** Insert a key in a table where there is space for that key, the
** key is valid, and the value is not nil.
*/
static void newcheckedkey (Table *t, const TValue *key, TValue *value) {
  unsigned i = keyinarray(t, key);
  if (i &gt; 0)  /* is key in the array part? */
    obj2arr(t, i - 1, value);  /* set value in the array */
  else {
    int done = insertkey(t, key, value);  /* insert key in the hash part */
    lua_assert(done);  /* it cannot fail */
    cast(void, done);  /* to avoid warnings */
  }
}
</code></pre>
<p>插入的源码是这么写的www&lt;br&gt;
如果keyinarray（一个用来检测key是否在数组区范围内的宏）告诉我们新元素的范围还在数组区内，就将其插入数组区，否则将其插入哈希区&lt;br&gt;
那么如何让pair按顺序遍历呢（不建议这么做，因为插入新元素的时候会rehash，不保证新元素一定在数组区，会发生问题）?&lt;br&gt;
将table按照默认方式初始化就行</p>
<pre><code>do
    local table = {
        "one",
        "two",
        "three",
        [false] = "false",
        [true] = "true",
        name = "张三",
        age = 18,
        friend = {
            name = "李四",
            age = 20
        }
    }
    for k, v in pairs(table) do
        print(k, v)
    end
    print("-------------")
    for k, v in ipairs(table) do
        print(k, v)
    end
    print("-------------")
    print(#table) -- 3
end
</code></pre>
<p>输出为</p>
<pre><code>1       one
2       two
3       three
false   false
true    true
age     18
name    张三
friend  table: 0000000000eca1d0
-------------
1       one
2       two
3       three
-------------
3
</code></pre>
<p>两相对比，我们还可以得到另外一个结论，lua表中非默认元素是在数组区初始化完成后再加入表的</p>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="lua"></category>
  </entry>
  <entry>
    <title>prim最小生成树</title>
    <link href="https://s701f.top/posts/prim%E6%9C%80%E5%B0%8F%E7%94%9F%E6%88%90%E6%A0%91/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/prim%E6%9C%80%E5%B0%8F%E7%94%9F%E6%88%90%E6%A0%91/</id>
    <published>2026-06-01T00:00:00.000Z</published>
    <updated>2026-06-01T00:00:00.000Z</updated>
    <summary>其实我之前会这个，但是没有加堆优化www</summary>
    <content type="html"><![CDATA[<h1>前言</h1>
<p>没什么可说的了，总之不要逃数据结构与算法（</p>
<h1>介绍</h1>
<p>我对prim的理解是贪心算法，每一次从已加入最小生成树的点与未加入最小生成树的点之间的连线中选择最短的那条边，然后把这条边加入已选择的边中</p>
<h1>实现（c++）</h1>
<h2>前置数据</h2>
<pre><code>std::priority_queue&lt;std::pair&lt;int, int&gt;, std::vector&lt;std::pair&lt;int, int&gt;&gt;, std::greater&lt;std::pair&lt;int, int&gt;&gt;&gt; pq;// 堆优化用的优先队列，它始终保持pair的第一个值最小的元素在队列顶端
std::vector&lt;int&gt; dist;// 当前最小生成树到每个点的距离
std::vector&lt;bool&gt; vis;// 点是否已经加入最小生成树
</code></pre>
<h2>链式前向星（存图）</h2>
<pre><code>struct edge
{
    int u, v, w;
    int next;
};
std::vector&lt;edge&gt; edges;
std::vector&lt;int&gt; head;
void Add(int u, int v, int w)
{
    edges.push_back({u, v, w, head[u]});
    head[u] = edges.size() - 1;
    edges.push_back({v, u, w, head[v]});
    head[v] = edges.size() - 1;
}
</code></pre>
<h2>初始化</h2>
<p>就像dijkstra那样，我们首先要把距离数组设置为inf</p>
<pre><code>dist.resize(N + 1, 0x3f3f3f3f);
</code></pre>
<p>然后我们随便选一个点开始计算最小生成树，就选1吧</p>
<pre><code>dist[1] = 0;
vis.resize(N + 1, false);
externed(1);
</code></pre>
<h2>对新增顶点链接的边进行松弛操作</h2>
<p>externed函数的内容:</p>
<pre><code>/// @brief 在新加入点n后，更新与n相邻的点的距离
/// @param n 新加入的点
void externed(int n)
{
    vis[n] = true;// 首先将该点设置为已加入最小生成树
    for (int u = head[n]; u != -1; u = edges[u].next)// 遍历所有从该点出发的边
    {
        int v = edges[u].v;
        int w = edges[u].w;
        if (vis[v])// 如果v点已经加入最小生成树，则跳过
        {
            continue;
        }
        if (dist[v] &gt; w)// 如果现有的最小生成树到v点的距离大于n点到v点的距离，则更新距离
        {
            dist[v] = w;
            pq.push({w, v});// 把更新后的距离加入优先队列中。这个w, v的意思是现在的最小生成树连接到v点的距离是w
        }
    }
}
</code></pre>
<h2>确定新增的顶点</h2>
<p>我们要持续把优先队列顶端且未加入最小生成树的顶点加入最小生成树集合中</p>
<pre><code>while (!pq.empty())
{
    auto [d, i] = pq.top();
    pq.pop();
    if (vis[i])
    {
        continue;
    }
    res += d;
    externed(i);
}
</code></pre>
<h2>判断联通与输出</h2>
<pre><code>for (int i = 1; i &lt;= N; ++i)
{
    if (!vis[i])
    {
        std::cout &lt;&lt; "orz" &lt;&lt; std::endl;
        return 0;
    }
}
std::cout &lt;&lt; res &lt;&lt; std::endl;
</code></pre>
<h1>完成</h1>
<p><a href="https://www.luogu.com.cn/record/280280172">LGP3366通过</a></p>
<h1>完整代码</h1>
<pre><code>#include &lt;iostream&gt;
#include &lt;vector&gt;
#include &lt;algorithm&gt;
#include &lt;queue&gt;

struct edge
{
    int u, v, w;
    int next;
};
std::vector&lt;edge&gt; edges;
std::vector&lt;int&gt; head;
void Add(int u, int v, int w)
{
    edges.push_back({u, v, w, head[u]});
    head[u] = edges.size() - 1;
    edges.push_back({v, u, w, head[v]});
    head[v] = edges.size() - 1;
}
std::priority_queue&lt;std::pair&lt;int, int&gt;, std::vector&lt;std::pair&lt;int, int&gt;&gt;, std::greater&lt;std::pair&lt;int, int&gt;&gt;&gt; pq;
std::vector&lt;int&gt; dist;
std::vector&lt;bool&gt; vis;

/// @brief 在新加入点n后，更新与n相邻的点的距离
/// @param n 新加入的点
void externed(int n)
{
    vis[n] = true;
    for (int u = head[n]; u != -1; u = edges[u].next)
    {
        int v = edges[u].v;
        int w = edges[u].w;
        if (vis[v])
        {
            continue;
        }
        if (dist[v] &gt; w)
        {
            dist[v] = w;
            pq.push({w, v});
        }
    }
}

int main()
{
    int N, M;
    std::cin &gt;&gt; N &gt;&gt; M;
    edges.reserve(2 * M + 5);
    long long res = 0;
    head.resize(N + 1, -1);
    for (int i = 0; i &lt; M; ++i)
    {
        int u, v, w;
        std::cin &gt;&gt; u &gt;&gt; v &gt;&gt; w;
        Add(u, v, w);
    }
    dist.resize(N + 1, 0x3f3f3f3f);
    dist[1] = 0;
    vis.resize(N + 1, false);
    externed(1);
    while (!pq.empty())
    {
        auto [d, i] = pq.top();
        pq.pop();
        if (vis[i])
        {
            continue;
        }
        res += d;
        externed(i);
    }
    for (int i = 1; i &lt;= N; ++i)
    {
        if (!vis[i])
        {
            std::cout &lt;&lt; "orz" &lt;&lt; std::endl;
            return 0;
        }
    }
    std::cout &lt;&lt; res &lt;&lt; std::endl;
}
</code></pre>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="算法"></category>
  </entry>
  <entry>
    <title>哈夫曼树</title>
    <link href="https://s701f.top/posts/%E5%93%88%E5%A4%AB%E6%9B%BC%E6%A0%91/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/%E5%93%88%E5%A4%AB%E6%9B%BC%E6%A0%91/</id>
    <published>2026-06-01T00:00:00.000Z</published>
    <updated>2026-06-01T00:00:00.000Z</updated>
    <summary>孩子们，不要逃数据结构与算法课...</summary>
    <content type="html"><![CDATA[<h1>前言</h1>
<p>数据结构与算法课不如杀戮尖塔好玩，所以咱没去（</p>
<h1>介绍</h1>
<p>在我的理解中，哈夫曼树是一种用来压缩数据大小的前缀树。233dada说这是用在zip压缩里面的&lt;br&gt;</p>
<h1>实现（c++）</h1>
<h2>数据结构</h2>
<p>我们给一个哈夫曼树节点的结构体，其中叶子节点只存一个单词及其出现次数，非叶子节点的count值是其左右两个节点的count之和</p>
<pre><code>struct HuffmanNode
{
    std::string word;
    unsigned long long count;
    HuffmanNode* left;
    HuffmanNode* right;
    HuffmanNode* parent;
    bool operator&lt;(const HuffmanNode&amp; other) const
    {
        return count &gt; other.count; // For min-heap
    }
};
</code></pre>
<p>手写树很麻烦，好在c++提供了优先队列的模板&lt;br&gt;
所以我们需要一个Cmp</p>
<pre><code>struct Cmp {
    bool operator()(HuffmanNode* a, HuffmanNode* b) const {
        return a-&gt;count &gt; b-&gt;count; // min-heap by count
    }
};
</code></pre>
<h2>建树</h2>
<p>我们用优先队列存我们即将建的树</p>
<pre><code>static std::priority_queue&lt;HuffmanNode*, std::vector&lt;HuffmanNode*&gt;, Cmp&gt; pq;
static std::vector&lt;std::string&gt; words;
static std::map&lt;std::string, std::string&gt; wordCode;
</code></pre>
<p>在输入时把所有节点推到优先队列里面</p>
<pre><code>std::string word;
unsigned long long count;
std::cin &gt;&gt; word &gt;&gt; count;
words.push_back(word);
wordCode[word] = "";
pq.push(new HuffmanNode{word, count, nullptr, nullptr, nullptr});
</code></pre>
<h2>处理</h2>
<p>每一次操作，我们取出顶部的两个节点，将其合并为一个树，再把新生成的根节点插入优先队列里面</p>
<pre><code>while(pq.size() &gt; 1)
{
    HuffmanNode* w1 = pq.top();
    pq.pop();
    HuffmanNode* w2 = pq.top();
    pq.pop();
    HuffmanNode* node = new HuffmanNode{w1-&gt;word + w2-&gt;word, w1-&gt;count + w2-&gt;count, w1, w2, nullptr};
    w1-&gt;parent = node;
    w2-&gt;parent = node;
    pq.push(node);
}
</code></pre>
<h2>分析</h2>
<p>然后我们需要把建成的树转化为编码&lt;br&gt;
从根节点到叶子节点的路径，每当选择了左孩子节点，哈夫曼编码的对应位置为0，反之为1&lt;br&gt;
所有我们可以对其进行深度优先搜索，每当搜索到叶子节点，就记录其对应的编码</p>
<pre><code>void anal(HuffmanNode* node, std::string code = "")
{
    if(node-&gt;left == nullptr &amp;&amp; node-&gt;right == nullptr)
    {
        wordCode[node-&gt;word] = code;
        return;
    }
    anal(node-&gt;left, code + "0");
    anal(node-&gt;right, code + "1");
}
</code></pre>
<h2>防止内存泄露</h2>
<p>在一切都做完之后，我们要递归地把分配的内存delete掉</p>
<pre><code>void del(HuffmanNode* node)
{
    if(node == nullptr) return;
    del(node-&gt;left);
    del(node-&gt;right);
    node = nullptr;
    delete node;
}
</code></pre>
<h2>完成</h2>
<p><a href="https://www.luogu.com.cn/record/280277639">LGB2168通过</a></p>
<h1>完整代码</h1>
<pre><code>#include &lt;iostream&gt;
#include &lt;vector&gt;
#include &lt;queue&gt;
#include &lt;map&gt;

struct HuffmanNode
{
    std::string word;
    unsigned long long count;
    HuffmanNode* left;
    HuffmanNode* right;
    HuffmanNode* parent;
    bool operator&lt;(const HuffmanNode&amp; other) const
    {
        return count &gt; other.count; // For min-heap
    }
};

struct Cmp {
    bool operator()(HuffmanNode* a, HuffmanNode* b) const {
        return a-&gt;count &gt; b-&gt;count; // min-heap by count
    }
};

static std::priority_queue&lt;HuffmanNode*, std::vector&lt;HuffmanNode*&gt;, Cmp&gt; pq;
static std::vector&lt;std::string&gt; words;
static std::map&lt;std::string, std::string&gt; wordCode;

void anal(HuffmanNode* node, std::string code = "")
{
    if(node-&gt;left == nullptr &amp;&amp; node-&gt;right == nullptr)
    {
        wordCode[node-&gt;word] = code;
        return;
    }
    anal(node-&gt;left, code + "0");
    anal(node-&gt;right, code + "1");
}
void del(HuffmanNode* node)
{
    if(node == nullptr) return;
    del(node-&gt;left);
    del(node-&gt;right);
    node = nullptr;
    delete node;
}

int main()
{
    int n;
    std::cin &gt;&gt; n;
    if(n == 1)
    {
        std::string word;
        int count;
        std::cin &gt;&gt; word &gt;&gt; count;
        std::cout &lt;&lt; word &lt;&lt; " " &lt;&lt; "0" &lt;&lt; std::endl;
        return 0;
    }
    for(int index = 0;index &lt; n;index++)
    {
        std::string word;
        unsigned long long count;
        std::cin &gt;&gt; word &gt;&gt; count;
        words.push_back(word);
        wordCode[word] = "";
        pq.push(new HuffmanNode{word, count, nullptr, nullptr, nullptr});
    }
    while(pq.size() &gt; 1)
    {
        HuffmanNode* w1 = pq.top();
        pq.pop();
        HuffmanNode* w2 = pq.top();
        pq.pop();
        HuffmanNode* node = new HuffmanNode{w1-&gt;word + w2-&gt;word, w1-&gt;count + w2-&gt;count, w1, w2, nullptr};
        w1-&gt;parent = node;
        w2-&gt;parent = node;
        pq.push(node);
    }
    HuffmanNode* root = pq.top();
    pq.pop();
    anal(root);
    for(auto&amp; word : words)
    {
        std::cout &lt;&lt; word &lt;&lt; " " &lt;&lt; wordCode[word] &lt;&lt; std::endl;
    }
    del(root);
    return 0;
}
</code></pre>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="算法"></category>
  </entry>
  <entry>
    <title>AIHistorySimulator源码学习</title>
    <link href="https://s701f.top/posts/aihistorysimulator%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/aihistorysimulator%E6%BA%90%E7%A0%81%E5%AD%A6%E4%B9%A0/</id>
    <published>2026-05-30T00:00:00.000Z</published>
    <updated>2026-05-30T00:00:00.000Z</updated>
    <summary>咱的第一个vibe coding项目</summary>
    <content type="html"><![CDATA[<h1>前言</h1>
<p>最近与以前在推特上认识的朋友玩文游。讲几个外星国家走向宇宙的故事&lt;br&gt;
由于一共有九个人，关系过于复杂，于是乎想着AI推演&lt;br&gt;
但是手动让AI推演太过麻烦，就想着能不能自己做一个交互网页，让推进游戏更简单一点&lt;br&gt;
于是就有了这个项目&lt;br&gt;
<a href="https://github.com/sine5RAD/AIHistorySimulator">下载链接</a></p>
<h1>难点</h1>
<h2>墨卡托投影地图转球面地图</h2>
<p>项目的世界生成页面需要实现一个墨卡托投影地图转球面地图的功能&lt;br&gt;</p>
<h3>我们需要知道的参数</h3>
<ol>
<li>图片</li>
</ol>
<pre><code>const pixels = context.createImageData(width, height)
</code></pre>
<ol>
<li>画布中心，一个坐标，咱们要把球面中心放在这里</li>
</ol>
<pre><code>const centerX = width / 2
const centerY = height / 2
</code></pre>
<ol>
<li>球体半径，实践中半径要比画布的长宽最小值小一点</li>
</ol>
<pre><code>const radius = Math.min(width, height) * 0.42
</code></pre>
<ol>
<li>旋转角，包括左右旋转角（绕y轴旋转，逆时针为正）和俯仰旋转角（绕x轴旋转，逆时针为正）</li>
</ol>
<pre><code>const yaw = rotation.value
const pitch = tilt.value
</code></pre>
<p>其中rotation是全局的经度偏移量，pitch是全局的纬度偏移量
5. 可选的光照方向，让球体更立体。咱的设置如下</p>
<pre><code>const lightX = -0.35
const lightY = 0.35
const lightZ = 0.87
</code></pre>
<h3>将图片映射到球面</h3>
<p>对于我们生成的图片pixel，其中的每一个像素都要被映射到球面上对应的位置&lt;br&gt;
首先我们归一化图片，将宽和高映射到$[-1, 1]$区间内&lt;br&gt;</p>
<pre><code>for (let y = 0; y &lt; height; y += 1) 
  // 将画布 y 坐标归一化到球面坐标系中，值域约为 [-1, 1]。
  const normalizedY = (centerY - y) / radius
  for (let x = 0; x &lt; width; x += 1) 
    // 将画布 x 坐标归一化到球面坐标系中，值域约为 [-1, 1]。
    const normalizedX = (x - centerX) / radius
</code></pre>
<h1>为什么normalizedY和normalizedX的计算方式不一样呢？&lt;br&gt;
我们需要知道canvas和context.createImageData(width, height)的坐标系&lt;br&gt;
<a href="https://www.runoob.com/html/html5-canvas.html">菜鸟教程</a> 告诉我们，canvas的(0, 0)坐标位于左上角&lt;br&gt;
也就是说，canvas的x轴正半轴向右，y轴正半轴向下&lt;br&gt;
然而在球面坐标系中，x轴正半轴向右，而y轴正半轴向上&lt;br&gt;
&lt;br&gt;
接下来，由于球面上所有点同球心的距离都是1，根据勾股定理，我们可以计算出在归一化的球面中，这个像素对应的normalizedZ的值，即$z = \sqrt{1 - x^2 - y^2}$&lt;br&gt;
&lt;br&gt;
下一步是计算旋转&lt;br&gt;
yaw和pitch实际上组成了一个欧拉角&lt;br&gt;
&lt;del&gt;如果是unity我们可以调用一行transform.rotate解决&lt;/del&gt;&lt;br&gt;
但在这里，我们必须自己计算&lt;br&gt;
这里要用到线性代数的相关知识&lt;br&gt;
yaw的旋转矩阵是
$
Y =
\begin{bmatrix}
\cos(\text{yaw}) &amp; 0 &amp; \sin(\text{yaw})\
0 &amp; 1 &amp; 0\
-\sin(\text{yaw}) &amp; 0 &amp; \cos(\text{yaw})
\end{bmatrix}
$, 而pitch的旋转矩阵是
$
P =
\begin{bmatrix}
1 &amp; 0 &amp; 0\
0 &amp; \cos(\text{pitch}) &amp; \sin(\text{pitch})\
0 &amp; -\sin(\text{pitch}) &amp; \cos(\text{pitch})
\end{bmatrix}
$
&lt;br&gt;
为了达到我们想要的效果，咱们先做pitch旋转，再做yaw旋转&lt;br&gt;
即
$$
% 先做 pitch（绕 x 轴旋转）
\begin{bmatrix}
x_1\
y_1\
z_1
\end{bmatrix}</h1>
<h1>\begin{bmatrix}
1 &amp; 0 &amp; 0\
0 &amp; \cos(\text{pitch}) &amp; -\sin(\text{pitch})\
0 &amp; \sin(\text{pitch}) &amp; \cos(\text{pitch})
\end{bmatrix}
\begin{bmatrix}
x_n\
y_n\
z_n
\end{bmatrix}</h1>
<h1>\begin{bmatrix}
x_n\
y_n\cos(\text{pitch}) - z_n\sin(\text{pitch})\
y_n\sin(\text{pitch}) + z_n\cos(\text{pitch})
\end{bmatrix}
$$
$$
% 再做 yaw（绕 y 轴旋转）
\begin{bmatrix}
x_r\
y_r\
z_r
\end{bmatrix}</h1>
<h1>\begin{bmatrix}
\cos(\text{yaw}) &amp; 0 &amp; \sin(\text{yaw})\
0 &amp; 1 &amp; 0\
-\sin(\text{yaw}) &amp; 0 &amp; \cos(\text{yaw})
\end{bmatrix}
\begin{bmatrix}
x_1\
y_1\
z_1
\end{bmatrix}</h1>
<p>\begin{bmatrix}
x_1\cos(\text{yaw}) + z_1\sin(\text{yaw})\
y_1\
-x_1\sin(\text{yaw}) + z_1\cos(\text{yaw})
\end{bmatrix}
$$</p>
<p>旋转代码为</p>
<pre><code>const rotatedY = normalizedY * cosPitch - normalizedZ * sinPitch
const pitchZ = normalizedY * sinPitch + normalizedZ * cosPitch
const rotatedX = normalizedX * cosYaw + pitchZ * sinYaw
const rotatedZ = -normalizedX * sinYaw + pitchZ * cosYaw
</code></pre>
<p>其中pitchZ为中间量&lt;br&gt;
&lt;br&gt;
然后我们需要将旋转后的点渲染在球面上&lt;br&gt;
首先获取其经纬度&lt;br&gt;</p>
<pre><code>const latitude = Math.asin(Math.max(-1, Math.min(1, rotatedY)))
const longitude = Math.atan2(rotatedX, rotatedZ)
</code></pre>
<p>由于墨卡托投影在接近极点时拉伸严重，故剪裁(但是这会导致极区不被渲染，咱也没有什么比较好的解决方案)</p>
<pre><code>// Mercator 投影对纬度做上限裁剪，避免极区无限拉伸。
const clampedLatitude = Math.max(
  -maxMercatorLatitude,
  Math.min(maxMercatorLatitude, latitude),
)
</code></pre>
<p><img src="%E5%8F%A4%E5%BE%B7%E6%9B%BC%E5%87%BD%E6%95%B0%E9%80%86%E6%8E%A8%E5%AF%BC.svg" alt="墨卡托投影在球面上的y值计算公式" />
这个图必须调成白天模式才能看见www&lt;br&gt;
根据古德曼函数的逆推导，咱们可以计算出y&lt;br&gt;</p>
<pre><code>const mercatorY = 0.5 - Math.log(Math.tan(Math.PI / 4 + clampedLatitude / 2)) / (2 * Math.PI)
</code></pre>
<p>这样我们就有了所有用于渲染纹理的数据</p>
<pre><code>// u/v 分别表示纹理中的水平和垂直位置，范围都在 [0, 1]。
const u = (longitude + Math.PI) / (2 * Math.PI)
const v = clamp01(mercatorY)
</code></pre>
<p>添加光照，这里我们假设基础亮度为$0.28 + 0.72 \times 0.15$，最大亮度为1</p>
<pre><code>const brightness =
  0.28 +
  0.72 * Math.max(0.15, normalizedX * lightX + normalizedY * lightY + normalizedZ * lightZ)
</code></pre>
<p>当前像素在pixels中的起始下标。createImageData函数返回一个图片数组。其中第$4x-4$、$4x-3$、$4x-2$、$4x-1$位分别表示第x个像素的rgba值，</p>
<pre><code>const pixelIndex = (y * width + x) * 4
</code></pre>
<p>对纹理进行采样</p>
<pre><code>// 纹理采样坐标：减去 0.5 可以让采样落在像素中心，而不是像素边缘。
const sampleX = u * globeTextureWidth - 0.5
const sampleY = v * globeTextureHeight - 0.5// 分别采样纹理的 R/G/B 通道，用于还原原始地图颜色。

const sampleR = sampleTextureChannel(
  globeTextureData,
  globeTextureWidth,
  globeTextureHeight,
  sampleX,
  sampleY,
  0,
)
const sampleG = sampleTextureChannel(
  globeTextureData,
  globeTextureWidth,
  globeTextureHeight,
  sampleX,
  sampleY,
  1,
)
const sampleB = sampleTextureChannel(
  globeTextureData,
  globeTextureWidth,
  globeTextureHeight,
  sampleX,
  sampleY,
  2,
)
</code></pre>
<p>这样我们就完成了一个简易的图片渲染器&lt;br&gt;
接下来是鼠标交互，我们可以用unity中学来的经验</p>
<pre><code>// 鼠标/触控拖拽交互：更新 rotation 并重渲染
const onPointerDown = (e: PointerEvent) =&gt; {
  // 编辑模式：左键（或触控）放置图钉，右键继续拖拽地图
  if (isEditingLand.value) {
    if (!globeCanvas.value) return

    if (e.pointerType !== 'touch' &amp;&amp; e.button === 2) {
      isDragging.value = true
      dragStartX.value = e.clientX
      dragStartY.value = e.clientY
      startRotation.value = rotation.value
      startTilt.value = tilt.value
      activePointerId.value = e.pointerId

      try {
        globeCanvas.value.setPointerCapture(e.pointerId)
      } catch {
        // ignore
      }
      return
    }

    if (e.pointerType !== 'touch' &amp;&amp; e.button !== 0) return
    onCanvasPlacePin(e.clientX, e.clientY)
    return
  }

  // 非编辑模式：仅响应右键或触控开始拖拽（将左键保留给编辑）
  if (e.pointerType !== 'touch' &amp;&amp; e.button !== 2) return

  if (!globeCanvas.value) return

  isDragging.value = true
  dragStartX.value = e.clientX
  dragStartY.value = e.clientY
  startRotation.value = rotation.value
  startTilt.value = tilt.value
  activePointerId.value = e.pointerId

  try {
    globeCanvas.value.setPointerCapture(e.pointerId)
  } catch {
    // ignore
  }
}
</code></pre>
<h2>陆地识别</h2>
<p>公理：一个像素，如果不是海洋，它就是陆地&lt;br&gt;
我们可以预先生成陆地的掩码&lt;br&gt;
在watch()中添加</p>
<pre><code>const prepared = await prepareGlobeTexture(src)
if (!prepared) {
  return
}
</code></pre>
<p>prepareGlobeTexture的代码</p>
<pre><code>const prepareGlobeTexture = async (src: string) =&gt; {
  // 增加一个 render token 用于取消旧的异步加载任务（防止竞态）
  const renderToken = ++globeRenderToken

  try {
    // 1) 加载图片资源（DataURL 或外部 URL）
    //    loadImageElement 返回在图片加载成功时解析的 HTMLImageElement。
    const image = await loadImageElement(src)

    // 2) 检查 renderToken：如果在等待期间另一次渲染请求已启动，则放弃本次结果
    if (renderToken !== globeRenderToken) {
      return false
    }

    // 3) 获取图片像素尺寸，用于创建与图片等尺寸的离屏画布
    const sourceWidth = image.naturalWidth || image.width
    const sourceHeight = image.naturalHeight || image.height

    if (!sourceWidth || !sourceHeight) {
      return false
    }

    // 4) 准备离屏 canvas（复用以减少频繁创建）
    if (!globeTextureCanvas) {
      globeTextureCanvas = document.createElement('canvas')
    }

    globeTextureCanvas.width = sourceWidth
    globeTextureCanvas.height = sourceHeight

    // 5) 获取 2D 上下文（将 willReadFrequently 设置为 true 以优化 readPixel 场景）
    const sourceContext = globeTextureCanvas.getContext('2d', { willReadFrequently: true })
    if (!sourceContext) {
      return false
    }

    // 6) 将图片绘制到离屏画布，并读取像素数据用于后续采样
    sourceContext.drawImage(image, 0, 0, sourceWidth, sourceHeight)
    globeTextureData = sourceContext.getImageData(0, 0, sourceWidth, sourceHeight).data
    globeTextureWidth = sourceWidth
    globeTextureHeight = sourceHeight

    // 7) 尝试基于纹理颜色在客户端生成陆地掩码（提高渲染时的判定性能）
    //    若生成失败则保留 globeLandMask 为 null，渲染时退回到按颜色判断。
    globeLandMask = null
    try {
      generateLandMaskClient()
    } catch (err) {
      console.error('客户端生成陆地掩码失败', err)
      globeLandMask = null
    }

    return true
  } catch (error) {
    console.error('Failed to prepare globe texture', error)
    return false
  }
}
</code></pre>
<p>在加载完成后我们调用generateLandMaskClient()，用第六步中保存的globeTextureData生成陆地掩码</p>
<pre><code>const generateLandMaskClient = () =&gt; {
  if (!globeTextureData || !globeTextureWidth || !globeTextureHeight) return

  const w = globeTextureWidth
  const h = globeTextureHeight
  const mask = new Uint8Array(w * h)

  for (let y = 0; y &lt; h; y++) {
    for (let x = 0; x &lt; w; x++) {
      const idx = (y * w + x) * 4
      const r = globeTextureData[idx] ?? 0
      const g = globeTextureData[idx + 1] ?? 0
      const b = globeTextureData[idx + 2] ?? 0
      const a = globeTextureData[idx + 3] ?? 255
      mask[y * w + x] = sampleIsLand(r, g, b, a) ? 1 : 0
    }
  }

  globeLandMask = mask
}
</code></pre>
<p>判断是否为陆地的函数，通过检测颜色的b通道值同r, g的关系来判断是否为陆地</p>
<pre><code>const sampleIsLand = (r: number, g: number, b: number, a = 255) =&gt; {
  const alphaThreshold = 10
  if (a &lt;= alphaThreshold) return false
  const blueRatio = 1.1
  const blueMargin = 24
  const maxrg = Math.max(r, g)
  const blueIsDominant = b &gt;= maxrg * blueRatio &amp;&amp; b &gt;= maxrg + blueMargin
  return !blueIsDominant
}
</code></pre>
<p>现在我们可以在地图上染色了，具体代码也在renderWorldGlobe()函数里</p>
<pre><code>// isLand 表示当前纹理点是否属于陆地；优先使用预计算掩码提高性能。
let isLand = false
if (globeLandMask &amp;&amp; globeTextureWidth &gt; 0 &amp;&amp; globeTextureHeight &gt; 0) {
  // 将采样位置映射到掩码网格中，和纹理坐标保持一致。
  const tx = wrapTextureX(Math.floor(u * globeTextureWidth), globeTextureWidth)
  const ty = clampTextureY(Math.floor(v * globeTextureHeight), globeTextureHeight)
  isLand = !!globeLandMask[ty * globeTextureWidth + tx]
} else {
  // 掩码不可用时，回退到按颜色判断是否为陆地。
  isLand = sampleIsLand(Math.round(sampleR), Math.round(sampleG), Math.round(sampleB), 255)
}
// baseR/baseG/baseB 是经过光照修正后的底色。
const baseR = Math.round(sampleR * brightness)
const baseG = Math.round(sampleG * brightness)
const baseB = Math.round(sampleB * brightness)
if (isLand) {
  // 陆地区域再叠加一层半透明黄色，增强地表区域的辨识度。
  const overlayR = LAND_OVERLAY_R
  const overlayG = LAND_OVERLAY_G
  const overlayB = LAND_OVERLAY_B
  // 通过原底色与叠加色混合，得到最终显示颜色。
  data[pixelIndex] = Math.round(
    baseR * (1 - LAND_OVERLAY_ALPHA) + overlayR * LAND_OVERLAY_ALPHA,
  )
  data[pixelIndex + 1] = Math.round(
    baseG * (1 - LAND_OVERLAY_ALPHA) + overlayG * LAND_OVERLAY_ALPHA,
  )
  data[pixelIndex + 2] = Math.round(
    baseB * (1 - LAND_OVERLAY_ALPHA) + overlayB * LAND_OVERLAY_ALPHA,
  )
  data[pixelIndex + 3] = alpha
} else {
  // 海洋区域只保留原始底色与光照效果。
  data[pixelIndex] = baseR
  data[pixelIndex + 1] = baseG
  data[pixelIndex + 2] = baseB
  data[pixelIndex + 3] = alpha
}
</code></pre>
<h2>编辑领土</h2>
<p>然后我们需要记录给每个国家分配的国土&lt;br&gt;
咱的思路是顺时针在地图上摆放图钉，围起来的地方就是领土&lt;br&gt;
所以领土的数据类包含一个数组，数组的每个元素都是一个坐标数组&lt;br&gt;
其中sx，sy是画布内部坐标，u，v是纹理坐标&lt;br&gt;
canvasPixelToUV是将屏幕坐标映射到纹理坐标的函数</p>
<pre><code>const onCanvasPlacePin = (clientX: number, clientY: number) =&gt; {
  const canvas = globeCanvas.value
  if (!canvas) return
  const rect = canvas.getBoundingClientRect()
  const sx = Math.round(clientX - rect.left)
  const sy = Math.round(clientY - rect.top)
  const uv = canvasPixelToUV(sx, sy)
  if (!uv) return

  // 如果点击靠近第一个图钉，则认为关闭多边形并尝试保存
  if (editingPins.value.length &gt; 0) {
    const first = editingPins.value[0]
    if (first) {
      const canvasEl = globeCanvas.value
      if (canvasEl) {
        const firstScreen = uvToScreen(first.u, first.v, canvasEl)
        const dx = sx - firstScreen.sx
        const dy = sy - firstScreen.sy
        const dist2 = dx * dx + dy * dy
        if (dist2 &lt; 20 * 20) {
          // 结束编辑
          finishEditingPolygon()
          return
        }
      }
    }
  }

  // 存储 uv 即可，屏幕坐标在渲染时实时计算
  editingPins.value.push({ u: uv.u, v: uv.v, sx, sy })
  scheduleRenderWorldGlobe()
}
</code></pre>
<p>其中editingPins是</p>
<pre><code>const editingPins = ref&lt;{ u: number; v: number; sx: number; sy: number }[]&gt;([])
</code></pre>
<p>在选择地块时我们需要渲染图钉&lt;br&gt;</p>
<pre><code>// 绘制当前正在编辑的图钉与连线（编辑态预览）
if (isEditingLand.value &amp;&amp; editingPins.value.length) {
  context.save() // 保存样式状态（随后 restore）
  // 使用强调色与较粗线条让编辑内容显眼
  context.strokeStyle = 'rgba(40,120,240,0.95)'
  context.fillStyle = 'rgba(40,120,240,0.95)'
  context.lineWidth = 2
  // 绘制连线：依次连接每个图钉的当前屏幕位置
  context.beginPath()
  for (let i = 0; i &lt; editingPins.value.length; i++) {
    const p = editingPins.value[i]
    if (!p) continue
    // 优先根据当前画布与投影计算屏幕坐标，若 canvas 不可用则回退到保存的快照坐标
    const { sx: px, sy: py } = globeCanvas.value
      ? uvToScreen(p.u, p.v, globeCanvas.value)
      : { sx: p.sx, sy: p.sy }
    if (i === 0)
      context.moveTo(px, py) // 起点
    else context.lineTo(px, py) // 连接线段
  }
  context.stroke() // 绘制多边形预览的轮廓线
  // 绘制每个图钉的控制点圆点，便于手动拖拽/删除
  for (const p of editingPins.value) {
    const { sx: px, sy: py } = globeCanvas.value
      ? uvToScreen(p.u, p.v, globeCanvas.value)
      : { sx: p.sx, sy: p.sy }
    context.beginPath()
    context.arc(px, py, 6, 0, Math.PI * 2) // 固定半径的圆点标记
    context.fill() // 使用与连线相同的填充色
  }
  context.restore() // 恢复到此前保存的画布样式
}
</code></pre>
<p>然后我们把选中的地块染色&lt;br&gt;
依然在renderWorldGlobe()函数里</p>
<pre><code>// 选中国家名称（如果存在），用于在渲染时高亮对应区域。
const selCountryName = currentCountry.value?.国家名称?.trim() || ''
// 若没有任何国家数据则跳过绘制区域的流程，避免不必要的 canvas 操作。
if (landMap.value &amp;&amp; Object.keys(landMap.value).length) {
  context.save() // 保存画布状态，绘制后使用 restore 恢复
  context.lineWidth = 1.5 // 默认描边宽度（会针对选中状态作调整）
  // 遍历每个国家及其地块
  for (const [countryName, land] of Object.entries(landMap.value)) {
    if (!land || !land.areas) continue // 跳过无效数据
    const isSelected = countryName === selCountryName // 标记是否为被选中国家
    const countryColor = getCountryColor(countryName) // 获取该国的主题颜色
    for (const area of land.areas) {
      if (!area || !area.vertices || !area.vertices.length) continue // 忽略空区域
      // 将每个顶点从纹理坐标 (u,v) 转换为经过视角旋转的平面坐标
      const rotatedPoints = area.vertices
        .filter((vert): vert is [number, number] =&gt; !!vert)
        .map(([u, v]) =&gt; uvToRotatedPoint(u, v))
      // 对多边形边界做插值平滑，减少锯齿状直线感
      const curvedPoints = buildCurvedPolygonPoints(rotatedPoints, 16)
      // 裁剪到当前可见半球，只绘制朝向用户的片段
      const clippedPoints = clipPolygonToFrontHemisphere(curvedPoints)
      if (clippedPoints.length &lt; 3) continue // 不足 3 个点无法构成多边形
      // 计算画布中心与投影半径，用于将旋转坐标映射回屏幕坐标
      const centerX = canvas.width / 2
      const centerY = canvas.height / 2
      const radius = Math.min(canvas.width, canvas.height) * 0.42
      context.beginPath()
      // 将裁剪后的每个点转换为屏幕坐标并绘制路径
      for (let i = 0; i &lt; clippedPoints.length; i += 1) {
        const point = clippedPoints[i]
        if (!point) continue
        const sx = centerX + point.x * radius // x 投影：中心偏移 + 单位坐标 * 半径
        const sy = centerY - point.y * radius // y 投影：注意画布 y 方向与数学坐标相反
        if (i === 0) {
          context.moveTo(sx, sy) // 起点
        } else {
          context.lineTo(sx, sy) // 连续线段
        }
      }
      context.closePath() // 封闭路径，准备填充与描边
      // 根据是否为选中状态选择不同的填充与描边透明度与粗细
      context.fillStyle = isSelected
        ? withColorAlpha(countryColor, 0.22)
        : withColorAlpha(countryColor, 0.2)
      context.strokeStyle = isSelected
        ? withColorAlpha(countryColor, 0.95)
        : withColorAlpha(countryColor, 0.65)
      context.lineWidth = isSelected ? 2 : 1
      context.fill() // 绘制填充
      context.stroke() // 绘制边框
    }
  }
  context.restore() // 恢复画布状态，避免影响后续绘制样式
}
</code></pre>
<p>clipPolygonToFrontHemisphere函数如下：</p>
<pre><code>// 将多边形裁剪到前半球（z &gt;= 0），并在被地平线切割的边界处沿地平线补齐点，
// 以便渲染出被地平线遮挡部分的可视化边界（例如闭合的海平线片段）。
const clipPolygonToFrontHemisphere = (points: RotatedPoint[]) =&gt; {
  // 至少需要 3 个点才能构成多边形，短路返回空结果
  if (points.length &lt; 3) return [] as RotatedPoint[]

  // 容差值：用于判断点是否在前半球（允许极小的数值误差导致 z 略小于 0）
  const epsilon = 1e-6
  // 在两次地平线交点之间插入的弧段细分数，用于在地平线间补齐曲线（越大越平滑）
  const horizonSegments = 16

  // 判定函数：点是否位于前半球（z &gt;= 0）
  const isInside = (point: RotatedPoint) =&gt; point.z &gt;= -epsilon

  /**
   * 在结果数组中添加从 `from` 到 `to` 的地平线弧段点（z = 0）。
   * - 通过 atan2 获得两个点在平面上的角度，沿最短角度差插值生成若干弧段顶点。
   * - 若角度差接近 0（几乎重合），则不插入任何点以避免重复。
   */
  const addHorizonArc = (result: RotatedPoint[], from: RotatedPoint, to: RotatedPoint) =&gt; {
    const fromAngle = Math.atan2(from.y, from.x) // 起点角度
    const toAngle = Math.atan2(to.y, to.x) // 终点角度
    let delta = toAngle - fromAngle

    // 将角度差规约到 (-PI, PI]，以便沿最短方向插值
    while (delta &lt;= -Math.PI) delta += Math.PI * 2
    while (delta &gt; Math.PI) delta -= Math.PI * 2

    // 若差值约为 0，则两点方向一致，不需要插入弧段
    if (Math.abs(delta) &lt; 1e-6) {
      return
    }

    // 将弧沿 angle 从 fromAngle 到 toAngle 分割为若干段，并在 z=0 平面上添加单位向量点
    for (let step = 1; step &lt;= horizonSegments; step += 1) {
      const t = step / horizonSegments
      const angle = fromAngle + delta * t
      result.push({ x: Math.cos(angle), y: Math.sin(angle), z: 0 })
    }
  }

  const result: RotatedPoint[] = []
  // 找到第一个位于前半球的顶点作为起点（从该点开始顺时针/逆时针遍历多边形）
  const startIndex = points.findIndex((point) =&gt; isInside(point))
  if (startIndex &lt; 0) {
    // 若所有点均在背面，则没有需要绘制的前半球片段
    return result
  }

  const startPoint = points[startIndex]
  if (!startPoint) {
    return result
  }

  // 归一化并添加起点（将可能略微偏离单位球的点投影回单位球表面）
  result.push(normalizeRotatedPoint(startPoint))
  // pendingExit 用来记录上一个从前半球到背半球的交点，当再次进入前半球时用它补齐地平线弧
  let pendingExit: RotatedPoint | null = null

  // 从 startIndex 开始遍历多边形的每条边，使用环绕索引处理闭合边
  for (let offset = 1; offset &lt;= points.length; offset += 1) {
    const currentIndex = (startIndex + offset - 1) % points.length
    const nextIndex = (startIndex + offset) % points.length
    const current = points[currentIndex]
    const next = points[nextIndex]
    if (!current || !next) continue

    const currentInside = isInside(current)
    const nextInside = isInside(next)

    // 情形 A：当前点与下一个点都在前半球 —— 直接将下一个点（归一化）加入结果（除非它是起点）
    if (currentInside &amp;&amp; nextInside) {
      if (nextIndex !== startIndex) {
        result.push(normalizeRotatedPoint(next))
      }
      continue
    }

    // 情形 B：当前在前半球，下一个在背半球 —— 边穿过地平线，计算交点并添加到结果，记录为 pendingExit
    if (currentInside &amp;&amp; !nextInside) {
      // 线性插值参数 t，按 z 分量解得当前到 next 在 z=0 处的插值比例
      const t = current.z / (current.z - next.z)
      // 在球面上插值后将点投影到地平线（z=0 单位向量）以获得准确的边界点
      const horizonPoint = projectRotatedPointToHorizon(lerpRotatedPoint(current, next, t))
      result.push(horizonPoint)
      pendingExit = horizonPoint
      continue
    }

    // 情形 C：当前在背半球，下一个进入前半球 —— 边从背面进入前半球，需要：
    //  (1) 计算进入点并添加，(2) 如果之前存在 pendingExit（之前的退出点），在二者间插入沿地平线的弧段，
    //  (3) 添加下一个顶点的归一化投影（除非它是起点）。
    if (!currentInside &amp;&amp; nextInside) {
      const t = current.z / (current.z - next.z)
      const horizonPoint = projectRotatedPointToHorizon(lerpRotatedPoint(current, next, t))
      if (pendingExit) {
        // 在上一次退出点与本次进入点之间补齐沿地平线的弧段
        addHorizonArc(result, pendingExit, horizonPoint)
      }
      result.push(horizonPoint)
      if (nextIndex !== startIndex) {
        result.push(normalizeRotatedPoint(next))
      }
      pendingExit = null // 已处理完 pendingExit
    }
  }

  return result
}
</code></pre>
<p>然后就完成了www</p>
<h2>与模型提供商通信</h2>
<p>首先我们需要准备需要的参数:</p>
<pre><code>const settings = readAiSettingsFromStorage()
const provider = prepared.provider ?? settings?.provider ?? 'OpenAI'
const apiKey = settings?.api ?? ''
const model = prepared.model ?? settings?.model ?? 'gpt-3.5-turbo'
</code></pre>
<p>以deepseek为例（os：我们需要一个国际标准来统一ai的返回结果）</p>
<pre><code>// DeepSeek 分支：同样先尝试 OpenAI 兼容 SDK，再回退到官方 REST endpoint。
if (provider === 'DeepSeek') {
  // DeepSeek REST Chat endpoint (assumed). Uses Bearer token in Authorization header.
  // 没有 API Key 时无法请求 DeepSeek，直接返回 null。
  if (!apiKey) {
    console.error('No API key available for DeepSeek')
    return null
  }
  // 优先尝试使用 OpenAI 官方 SDK（DeepSeek 兼容 OpenAI 格式）。
  try {
    // baseURL 指向 DeepSeek 的兼容入口。
    const sdkResult = await callOpenAiCompatibleSdk('https://api.deepseek.com')
    // 如果 SDK 成功返回了非空字符串，直接作为最终结果。
    if (typeof sdkResult === 'string' &amp;&amp; sdkResult.length &gt; 0) {
      return sdkResult
    }
  } catch (err) {
    // 如果无法导入 SDK（例如在浏览器中），则使用 fetch 回退。
    console.debug('OpenAI SDK not available, using fetch fallback', err)
  }
  // 官方文档推荐的 DeepSeek OpenAI 兼容 endpoint。
  const endpoint = 'https://api.deepseek.com/chat/completions'

  // REST 回退路径：直接请求 DeepSeek 兼容接口。
  try {
    const res = await fetch(endpoint, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${apiKey}`,
      },
      body: JSON.stringify({ model, messages: prepared.messages }),
    })

    // 非成功响应时读取服务端错误信息并返回 null。
    if (!res.ok) {
      const text = await res.text()
      console.error('DeepSeek API error', res.status, text)
      return null
    }

    // 正常返回时解析 JSON 并抽取统一文本结果。
    const data = await res.json()
    return extractCompletionText(data)
  } catch (e) {
    // 任何请求失败都在这里收敛，避免向上抛出未处理异常。
    console.error('Failed to call DeepSeek', e)
    return null
  }
  // 对未实现的 provider 给出明确错误，避免静默失败。
  console.error('Provider not implemented:', provider)
  return null
}

</code></pre>
<p>然后我们需要一个把模型的返回结果转为string类型的函数</p>
<pre><code>// 将模型返回结果统一提取成文本，避免不同 provider 的响应结构差异影响上层调用。
const extractCompletionText = (data: unknown) =&gt; {
  // 非对象数据无法按统一结构解析，直接返回 null。
  if (!data || typeof data !== 'object') {
    return null
  }
  // 以宽松结构接收多种兼容接口返回值，便于处理 OpenAI / DeepSeek / 其他兼容实现。
  const response = data as {
    choices?: Array&lt;{ message?: { content?: unknown }; text?: unknown }&gt;
    output?: unknown
    result?: unknown
  }
  // 优先处理 OpenAI 风格的 choices 数组。
  if (Array.isArray(response.choices) &amp;&amp; response.choices.length &gt; 0) {
    // 收集所有 choice 中的文本片段，最后拼接为一个完整回复。
    const parts: string[] = []
    // 遍历每个 choice，兼容 message.content 与 text 两种常见字段。
    for (const choice of response.choices) {
      // 先读 message.content，若没有则回退到 text。
      const content = choice.message?.content ?? choice.text
      // 只收集字符串内容，避免把对象或其他非文本数据混入结果。
      if (typeof content === 'string') {
        parts.push(content)
      }
    }
    // 若至少提取到一个字符串片段，则按换行连接后返回。
    if (parts.length &gt; 0) {
      return parts.join('\n')
    }
  }
  // 兼容某些接口直接返回 output 字段的情况。
  if (typeof response.output === 'string') return response.output
  // 兼容某些接口直接返回 result 字段的情况。
  if (typeof response.result === 'string') return response.result
  // 如果无法识别具体结构，则返回原始 JSON 字符串，至少保留调试信息。
  return JSON.stringify(data)
}
</code></pre>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="前端开发"></category>
  </entry>
  <entry>
    <title>带有源极交流阻抗的MOS管放大器设计</title>
    <link href="https://s701f.top/posts/%E5%B8%A6%E6%9C%89%E6%BA%90%E6%9E%81%E4%BA%A4%E6%B5%81%E9%98%BB%E6%8A%97%E7%9A%84mos%E7%AE%A1%E6%94%BE%E5%A4%A7%E5%99%A8%E8%AE%BE%E8%AE%A1/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/%E5%B8%A6%E6%9C%89%E6%BA%90%E6%9E%81%E4%BA%A4%E6%B5%81%E9%98%BB%E6%8A%97%E7%9A%84mos%E7%AE%A1%E6%94%BE%E5%A4%A7%E5%99%A8%E8%AE%BE%E8%AE%A1/</id>
    <published>2026-05-20T00:00:00.000Z</published>
    <updated>2026-05-20T00:00:00.000Z</updated>
    <summary>更难了，需要考虑交流阻抗</summary>
    <content type="html"><![CDATA[<h1>任务</h1>
<p>依然是使用<strong>BS170</strong>进行设计，这次要求$V_{DD} = 15V$，$R_L = 26k$，$A_V = 27$</p>
<h1>步骤</h1>
<p>还是需要先确定元件参数
<img src="%E7%94%B5%E8%B7%AF%E5%9B%BE.png" alt="电路图" /></p>
<h2>第一步</h2>
<p>按照<a href="../MOSFET%E5%8A%9F%E6%94%BE%E8%AE%BE%E8%AE%A1%E8%AE%B0%E5%BD%95/">上一次实验</a>的流程，我们搭建一个相同的电路&lt;br&gt;
什么？你问$R_{S1}$和$R_{S2}$是什么东西？&lt;del&gt;咱不知道啊，把它当成源极电阻吧，只需要两个加一起等于算出来的$R_S$就行&lt;/del&gt;&lt;br&gt;
什么？你问$I_D$该怎么选择？&lt;del&gt;为了装作咱真的掌握了MOS管相关内容，一定要和之前选的不一样啊，那就4mA吧&lt;/del&gt;
于是在$V_{GS} = 7.5V, R_{G1} = R_{G2} = 100k, I_D = 4mA$的情况下完成了电路设计&lt;br&gt;
我超$A_V$怎么等于0.2，大失败</p>
<h2>错误分析</h2>
<p>问题出在哪里呢？原因在于先前的实验中交流信号可以直接走电容，不会被$R_S$影响。但现在的设计中交流信号依然要走$R_{S1}$&lt;br&gt;
那么问题来了，真实的增益公式是什么呢？&lt;br&gt;
当然是$A_V = \frac{g_m(R_D//R_L)}{1 + g_mR_{S1} }$啦&lt;br&gt;
让我们重新计算，然后仿真&lt;br&gt;
我超，怎么$V_o$只有几$\mu V$了，还是大失败&lt;br&gt;</p>
<h2>错误分析2</h2>
<p>这次的错误是什么呢？答案在我们之前选取的$R{S1}$上。&lt;br&gt;
由公式可得，$R_{S1}$越大，$R_D//R_L$就越大。由于$R_L$为定值，那么只能增大$R_D$&lt;br&gt;
这就是问题的原因，$V_D = I_DR_D$，我们的$R_{S1} = 390$，这时$R_D≈22k$，对应的分压$V_D = 88V$，而我们的$V_{DD}$只有可怜的15V，导致漏极电压为负&lt;br&gt;</p>
<h2>结果</h2>
<p>让我们重新计算，增加$R_D$的约束条件。顺便把$I_D$降到2mA以降低$R_S$&lt;br&gt;
因为$V_D &gt; V_S + 0.5V$(这个0.5V是工程常数)&lt;br&gt;
又因为我们选取的$V_S = 5.15V$，故$V_D &gt; 5.65V$（这个条件下$R_{G1} = R_{G2}$，分压都是7.5V）&lt;br&gt;
这样可以得到$R_D &lt; \frac{V_{DD} - V_D}{I_D} = 4675$&lt;br&gt;
我们选择一个小一点的$R_{S1} = 50$，这时$R_{S2} = 2390$，解得$R_D = 3009$，符合条件&lt;br&gt;
设计完成
<img src="%E7%BB%93%E6%9E%9C%E5%9B%BE.png" alt="结果图" /></p>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="电子信息工程"></category>
  </entry>
  <entry>
    <title>racknerd服务器搭建</title>
    <link href="https://s701f.top/posts/racknerd%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%90%AD%E5%BB%BA/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/racknerd%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%90%AD%E5%BB%BA/</id>
    <published>2026-05-18T00:00:00.000Z</published>
    <updated>2026-05-18T00:00:00.000Z</updated>
    <summary>一个腾讯云难民的故事</summary>
    <content type="html"><![CDATA[<h1>前言</h1>
<p>在遭到不可抗力因素的打压之后，咱决定从腾讯云跑路到racknerd&lt;br&gt;</p>
<h1>账号注册</h1>
<p>首先是之前说的验证码问题，咱换了一个clash节点就解决了</p>
<h1>racknerd的优势</h1>
<ol>
<li>不需要实名制。这是咱从腾讯云跑路的核心原因。在国内需要把域名转到厂商那里然后实名认证才能让把域名解析到ip</li>
<li>可以直连github</li>
</ol>
<h1>racknerd的劣势</h1>
<p>贵，我租个单核1GB RAM的服务器爆了150块</p>
<h1>在遇到问题之前...</h1>
<p>咱照着之前<a href="../%E5%9F%9F%E5%90%8D%E8%B4%AD%E4%B9%B0%E4%BB%A5%E5%8F%8A%E8%85%BE%E8%AE%AF%E4%BA%91%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%90%AD%E5%BB%BA">在腾讯云上部署的方式</a>重新跑了一遍。一切都很正常，直到咱运行<code>pnpm build</code>&lt;br&gt;
爆内存了...</p>
<h1>解决爆内存的问题</h1>
<p>为什么会爆内存呢？&lt;br&gt;
当然是因为这是个1G RAM的服务器&lt;br&gt;
VUE3项目构建时的最少需要1GB的内存，而咱的破烂服务器在运行nginx之后只有大概600MB的剩余了&lt;br&gt;
所以构建并不能在服务器端进行...&lt;br&gt;
首先的想法是本地构建，然后把dist文件夹传上去。&lt;br&gt;
但考虑到每次更新都要手动上传，咱决定把它作为没有办法的办法&lt;del&gt;（咱这该死的完美主义）&lt;/del&gt;&lt;br&gt;
于是求助群友&lt;br&gt;
&lt;q&gt;github workflow&lt;/q&gt;——Alumopper姐姐&lt;br&gt;</p>
<h1>github workflow</h1>
<p>不会写，遂bing一下&lt;br&gt;
github的官方文档说它遵循yaml语法&lt;del&gt;（什么是yaml）&lt;/del&gt;&lt;br&gt;
那么我们着手开始写&lt;br&gt;</p>
<h2>on</h2>
<p>首先是我们的需求，要在推送到master分支的时候自动触发构建&lt;br&gt;
那么我们的on可以这样写</p>
<pre><code>push:
    branches:
        - main
        - master
</code></pre>
<h2>permission</h2>
<p>我们要将构建出来的dist文件夹写入仓库的pages分支，那么我们需要</p>
<pre><code>permissions:
    contents: write
</code></pre>
<p>这段代码的作用是给予脚本写入权限&lt;br&gt;</p>
<h2>job</h2>
<p>接下来到了重头戏，这是我们脚本的核心功能，以<code>job:</code>开头&lt;br&gt;
job里面是运行流程，github会自上而下执行你编写的流程</p>
<h3>build-and-deploy</h3>
<p>我们首先需要构建代码&lt;br&gt;
由于我们构建出来的网站运行在ubuntu20.04操作系统上，所以我们添加一行</p>
<pre><code>runs-on: ubuntu-latest
</code></pre>
<p>之后是运行流程，在<code>steps</code>标签下&lt;br&gt;</p>
<h4>checkout</h4>
<p>首先我们需要知道github action是怎么运行的&lt;br&gt;
github会给你提供一台临时运行机，然后在这台机器上执行你的流程&lt;br&gt;
但是我们要构建代码，而咱的代码又不会自动下载到虚拟机上&lt;br&gt;
怎么办呢？&lt;br&gt;
当然是前人栽树后人乘凉，</p>
<pre><code>- name: Checkout
  uses: actions/checkout@v4
</code></pre>
<p>搞定</p>
<h4>setup pnpm</h4>
<p>想要运行<code>pnpm build</code>，我们需要什么呢？&lt;br&gt;
当然是需要pnpm了（&lt;br&gt;
我们依然可以用别人写好的构建代码&lt;br&gt;</p>
<pre><code>- name: Setup pnpm
      uses: pnpm/action-setup@v4
      with:
          version: 10.33.0
          run_install: false
</code></pre>
<p>安装的pnpm版本应该与本地的pnpm版本一致，并且不自动执行依赖安装（就是<code>pnpm install</code>）&lt;br&gt;
不自动执行依赖安装的原因是我们还没装node.js</p>
<h4>setup node.js</h4>
<p>仿照上例显然</p>
<pre><code>- name: Setup Node.js
  uses: actions/setup-node@v4
  with:
      node-version: 22.12.0
      cache: pnpm
</code></pre>
<h4>install dependence</h4>
<p>既然已经安装完node.js了，我们终于可以安装依赖了</p>
<pre><code>- name: Install dependencies
  run: pnpm install --frozen-lockfile
</code></pre>
<h4>build site</h4>
<p>就像教程里说的那样，直接</p>
<pre><code>- name: Build site
  run: pnpm build
</code></pre>
<p>就好了</p>
<h4>Deploy to pages branch</h4>
<p>然后我们要把生成的dist文件夹推送到pages分支上&lt;br&gt;
恰巧前人还帮我们写好了&lt;br&gt;
那么最后一步</p>
<pre><code>- name: Deploy to pages branch
  uses: JamesIves/github-pages-deploy-action@v4
  with:
      branch: pages
      folder: dist
      clean: true
</code></pre>
<p>完成</p>
<h4>SSH Connect to remote server</h4>
<p>接下来就是把我们的page分支部署到服务器上面去&lt;br&gt;
ssh链接到服务器，打开目录，然后直接<code>git pull</code>就行&lt;br&gt;
直接使用linux的ssh命令有个问题，就是你只能传给它文件形式的私钥，而我们又不能在github上面明文存储私钥&lt;br&gt;
好在前人的智慧还是起了作用&lt;br&gt;</p>
<pre><code>- name: SSH Connect to remote server
  uses: appleboy/ssh-action@master
  with:
      host: ${{ secrets.DEPLOY_HOST }}
      username: ${{ secrets.DEPLOY_USER }}
      key: |
          ${{ secrets.SSH_PRIVATE_KEY }}
      script: |
          cd ${{secrets.DEPLOY_PATH}}
          git pull
</code></pre>
<p>现在就好啦</p>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="互联网"></category>
  </entry>
  <entry>
    <title>樱羽艾玛cos服测评</title>
    <link href="https://s701f.top/posts/%E6%A8%B1%E7%BE%BD%E8%89%BE%E7%8E%9Bcos%E6%9C%8D%E6%B5%8B%E8%AF%84/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/%E6%A8%B1%E7%BE%BD%E8%89%BE%E7%8E%9Bcos%E6%9C%8D%E6%B5%8B%E8%AF%84/</id>
    <published>2026-05-15T00:00:00.000Z</published>
    <updated>2026-05-15T00:00:00.000Z</updated>
    <summary>第二件cos服</summary>
    <content type="html"><![CDATA[<p>项目做完之后给咱自己挑的奖励也是到货了。本来是从对立cos和一件猫耳常服里面选的，但是不知道为什么脑子一热就买了这套&lt;br&gt;</p>
<h1>外观</h1>
<p>看起来不错，还不还原咱也不清楚（</p>
<h1>穿着体验</h1>
<p>有点紧，还好咱比较瘦。花苞裤的扣子扣不上，不知道是开口太小了还是我腰围太大&lt;br&gt;
一开始不会调腿环，咱还说这腿环怎么这么肥&lt;br&gt;
袜子很紧，和男士袜子完全不一样，很难穿上去&lt;br&gt;</p>
<h1>买家秀</h1>
<p>&lt;del&gt;到时候再说，现在家长在家，没法去全身镜前面拍照。但咱觉得抛开皮肤不谈还是挺好看的&lt;/del&gt;
<img src="%E4%B9%B0%E5%AE%B6%E7%A7%80.jpg" alt="买家秀" /></p>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="cos"></category>
  </entry>
  <entry>
    <title>域名购买以及腾讯云服务器搭建</title>
    <link href="https://s701f.top/posts/%E5%9F%9F%E5%90%8D%E8%B4%AD%E4%B9%B0%E4%BB%A5%E5%8F%8A%E8%85%BE%E8%AE%AF%E4%BA%91%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%90%AD%E5%BB%BA/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/%E5%9F%9F%E5%90%8D%E8%B4%AD%E4%B9%B0%E4%BB%A5%E5%8F%8A%E8%85%BE%E8%AE%AF%E4%BA%91%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%90%AD%E5%BB%BA/</id>
    <published>2026-05-14T00:00:00.000Z</published>
    <updated>2026-05-14T00:00:00.000Z</updated>
    <summary>戈尔巴乔夫先生，推倒那堵墙！</summary>
    <content type="html"><![CDATA[<h1>前言</h1>
<p>既然已经开始写博客了，咱总得把它部署到服务器上对吧。抱着这样的想法买了个域名&lt;br&gt;</p>
<h1>域名购买</h1>
<p>咱是在 <a href="https://www.spaceship.com/zh/">spaceship</a> 上面买的，货比三家之后发现这家买.top域名是最便宜的&lt;br&gt;
最后花了八块钱买了 s701f.top 这个域名。说实话八块钱买到五个字母的域名还是很赚的</p>
<h1>服务器租赁</h1>
<p>接下来是租服务器，本着省钱的思想问了群友。言子的推荐是racknerd，但我注册账号的时候验证码加载不出来，挂了梯子也一样&lt;br&gt;
遂把目光投向国内，Alumopper姐姐推荐腾讯云...但是怎么最便宜的也要500一年啊&lt;br&gt;
突然想起咱是学生，可以用学生优惠，于是获得了78块钱一年的试用期&lt;br&gt;</p>
<h1>搭建环境</h1>
<p>由于咱是笨蛋不会linux命令行，首先需要安装面板。国内的话是有两个面板，一个是宝塔一个是1Panel&lt;br&gt;
可能是嫌弃宝塔这个名字太俗了，咱先选了1Panel，然后发现看不懂教程不会安装nodejs，遂重装系统&lt;br&gt;
接下来换成宝塔。mizuki的教程说要从应用商店下载nginx，nodejs和pm2。nginx好说，在网站那一栏直接安装就行，但是nodejs安装的时候出了很多问题&lt;br&gt;
首先教程里面说的比较模糊，实际上应该在应用商店里面搜索nodejs管理器，然后选择一个有最新版本nodejs的镜像源（别用默认的，那个只到2021年，咱一开始选的这个，走了好多弯路）&lt;br&gt;
<img src="./nodejs.png" alt="nodejs管理器" />&lt;br&gt;
然后到终端里面，运行<code>npm install -g pnpm</code>把pnpm下下来（这个框架的包管理器是pnpm）&lt;br&gt;
啊这，终端说它找不到npm&lt;br&gt;
赶快看看nodejs在不在，输入<code>nodejs -v</code>&lt;br&gt;
啊这，终端说它也找不到nodejs&lt;br&gt;
那么问题来了，咱之前确实下载了nodejs啊，问题出在哪里呢？&lt;br&gt;
原来是环境变量没添加，执行</p>
<pre><code>echo 'export PATH="（你的nodejs路径）:$PATH"' &gt;&gt; ~/.bashrc
source ~/.bashrc
</code></pre>
<p>即可&lt;br&gt;</p>
<h1>部署网页代码</h1>
<p>首先咱把网页传到github上面，这样可以直接在服务器端拉取远端代码&lt;br&gt;
然后咱在O滨兴的长城上撞了个头破血流，github下载不下来&lt;br&gt;
由于这是腾讯云，咱不能下载vpn&lt;br&gt;
所以只能本地打包，然后上传到服务器端&lt;del&gt;流量-1GB&lt;/del&gt;&lt;br&gt;
之后和教程里说的一样，运行</p>
<pre><code>pnpm install #安装依赖包
pnpm build #构建网站
</code></pre>
<p>就行了</p>
<h1>设置域名</h1>
<p>现在咱的网页可以访问了，但只能通过ip地址+端口号的形式&lt;br&gt;
为了成为一个正常的网页，我们需要设置域名&lt;br&gt;
首先在腾讯云界面设置域名解析
<img src="./%E5%9F%9F%E5%90%8D%E8%A7%A3%E6%9E%90.png" alt="域名解析" />
然后回到<a href="https://www.spaceship.com/zh/">spaceship</a>，进入<a href="https://www.spaceship.com/zh/application/advanced-dns-application/manage/">高级DNS</a>，点击创建自定义预设并添加腾讯云给你的记录&lt;br&gt;
<img src="./%E6%B7%BB%E5%8A%A0DNS%E8%AE%B0%E5%BD%95.png" alt="添加DNS记录" />
进入宝塔面板，选择网站，点击添加站点，根目录选择/dist&lt;br&gt;
然后去域名管理，添加你的域名和端口号&lt;br&gt;
<img src="./%E6%B7%BB%E5%8A%A0%E7%BD%91%E7%AB%99%E5%9F%9F%E5%90%8D.png" alt="添加网站域名" />
现在我们可以用域名加端口号的形式访问了&lt;br&gt;
&lt;del&gt;我去怎么要备案&lt;/del&gt;</p>
<h1>设置反向代理</h1>
<p>点击反向代理，然后按照图中内容填写，理论上讲成功之后就可以直接域名访问了&lt;br&gt;
<img src="%E8%AE%BE%E7%BD%AE%E5%8F%8D%E4%BB%A3.png" alt="设置反代" />&lt;br&gt;
感谢Alumopper姐姐的倾力支援&lt;br&gt;</p>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="互联网"></category>
  </entry>
  <entry>
    <title>MOSFET功放设计记录</title>
    <link href="https://s701f.top/posts/mosfet%E5%8A%9F%E6%94%BE%E8%AE%BE%E8%AE%A1%E8%AE%B0%E5%BD%95/" rel="alternate" type="text/html"/>
    <id>https://s701f.top/posts/mosfet%E5%8A%9F%E6%94%BE%E8%AE%BE%E8%AE%A1%E8%AE%B0%E5%BD%95/</id>
    <published>2026-05-13T00:00:00.000Z</published>
    <updated>2026-05-13T00:00:00.000Z</updated>
    <summary>记录一次功放的设计流程</summary>
    <content type="html"><![CDATA[<h1>任务</h1>
<p>使用 <strong>BS170</strong> 设计一个将输入电压放大12倍，$V_{DD} = 10V$ 的功率放大器</p>
<h1>步骤</h1>
<p>我们需要确定元件参数
<img src="./MOSFET%E5%8A%9F%E6%94%BE%E6%A8%A1%E6%9D%BF.png" alt="MOSFET功放模板.png" /></p>
<h2>第一步</h2>
<p>首先确定漏极电流$I_D$: &lt;br&gt;
由于增益$A_V = g_m(R_D||R_L)$, &lt;br&gt;
$g_m = \frac{2I_D}{V_{GS} - V_t}$&lt;br&gt;
实验测得$V_t=2.15V$，$V_{GS} = 2.35V$&lt;br&gt;
由于$A_V$正比于${I_D, I_D}$过大时$R_D$需要一个较小的值，难以调控，故选择较小的${I_D = 2mA}$&lt;br&gt;</p>
<h2>第二步</h2>
<p>为了使MOSFET工作在饱和区，$V_{GS}$ 必须大于 $V_t$&lt;br&gt;
不妨假设$V_S = 2.5V$&lt;br&gt;
则此时$V_G = V_S + V_{GS} = 4.65V$&lt;br&gt;
同时由于$I_S = I_D$，我们可以得到$R_S = \frac{V_S}{I_D} = 1.25kΩ$&lt;br&gt;</p>
<h2>第三步</h2>
<p>现在让我们确定$R_{G1}$和$R_{G2}$&lt;br&gt;
与交流信号相比，栅极的直流分量必须很小，所以$R_{G1}$和$R_{G2}$需要是大电阻。不妨假设$R_{G1} = 400kΩ$&lt;br&gt;
由于对于$V_{DD}$而言$R_{G1}$和$R_{G2}$串联，可以计算出$R_{G2} = \frac {R_{G1} }{V_{DD} - V_G} \times V_G = 347.7kΩ$&lt;br&gt;</p>
<h2>第四步</h2>
<p>接下来让我们计算$R_D$&lt;br&gt;
我们已经可以计算$g_m = 20mS$，将$A_V = 12$代入，解的$R_D = 0.625kΩ$&lt;br&gt;</p>
<h2>第五步</h2>
<p>补充剩下的电路&lt;br&gt;
$C_1$和$C_2$都是隔直电容，将其设为$10\mu C$即可&lt;br&gt;
$C_3$为旁路电容，容抗$X = \frac{1}{2\pi fC}$&lt;br&gt;
为了使增益不受影响，$X$应远小于$R_S$，故最终选择$C_3 = 470\mu C$&lt;br&gt;
出于保护电路考虑，选择$R_1 = 10kΩ$</p>
<h1>结果</h1>
<p><img src="./%E7%BB%93%E6%9E%9C.png" alt="结果" /></p>
]]></content>
    <author>
      <name>五红酱自动机！</name>
    </author>
    <category term="电子信息工程"></category>
  </entry>
</feed>